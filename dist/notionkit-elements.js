var NotionKitElements = (function (exports) {
  'use strict';

  // NotionKit v1.10.0 – Constructable Stylesheet
  // Auto-generated by build-styles-js.mjs – do not edit manually.

  // The :root and [data-theme="dark"] blocks that declare --nk-* …
  const tokensCss = ":root{color-scheme:light;--nk-bg: #ffffff;--nk-bg-sidebar: #f9f8f7;--nk-bg-hover: rgba(55,53,47,.06);--nk-bg-active: rgba(55,53,47,.08);--nk-bg-callout: #f0efed;--nk-bg-code: #f7f6f3;--nk-bg-card: #ffffff;--nk-bg-input: rgba(242,241,238,.6);--nk-text: #2c2c2b;--nk-text-secondary: #7d7a75;--nk-text-tertiary: #91918e;--nk-text-sidebar: #5f5e5b;--nk-border: rgba(28,19,1,.11);--nk-border-strong: rgba(27,21,0,.19);--nk-accent: #2383e2;--nk-on-accent: #ffffff;--nk-danger: #cd3c3a;--nk-tag-gray-bg: #e3e2e0;--nk-tag-gray-text: #32302c;--nk-tag-brown-bg: #eee0da;--nk-tag-brown-text: #442a1e;--nk-tag-orange-bg: #fadec9;--nk-tag-orange-text: #49290e;--nk-tag-yellow-bg: #fdecc8;--nk-tag-yellow-text: #402c1b;--nk-tag-green-bg: #dbeddb;--nk-tag-green-text: #1c3829;--nk-tag-blue-bg: #d3e5ef;--nk-tag-blue-text: #183347;--nk-tag-purple-bg: #e8deee;--nk-tag-purple-text: #412454;--nk-tag-pink-bg: #f5e0e9;--nk-tag-pink-text: #4c2337;--nk-tag-red-bg: #ffe2dd;--nk-tag-red-text: #5d1715;--nk-tint-gray: #f1f1ef;--nk-color-gray: #787774;--nk-tint-brown: #f4eeee;--nk-color-brown: #9f6b53;--nk-tint-orange: #fbecdd;--nk-color-orange: #d9730d;--nk-tint-yellow: #fbf3db;--nk-color-yellow: #cb912f;--nk-tint-green: #edf3ec;--nk-color-green: #448361;--nk-tint-blue: #e7f3f8;--nk-color-blue: #337ea9;--nk-tint-purple: #f8f3fc;--nk-color-purple: #9065b0;--nk-tint-pink: #fcf1f6;--nk-color-pink: #c14c8a;--nk-tint-red: #fdebec;--nk-color-red: #cd3c3a;--nk-decor-purple: #9065b0;--nk-decor-blue: #529cca;--nk-decor-orange: #d9730d;--nk-scrim: rgba(15,15,15,.6);--nk-scrim-soft: rgba(15,15,15,.5);--nk-sidebar-width: 260px;--nk-peek-width: 560px;--nk-tab-bar-height: 58px;--nk-radius: 6px;--nk-font: ui-sans-serif, -apple-system, \"Segoe UI\", Inter, Helvetica, Arial, sans-serif;--nk-font-mono: ui-monospace, \"SF Mono\", Menlo, Consolas, monospace;--nk-shadow-menu: rgba(15,15,15,.05) 0px 0px 0px 1px, rgba(15,15,15,.1) 0px 3px 6px, rgba(15,15,15,.2) 0px 9px 24px;--nk-shadow-card: 0 1px 2px rgba(0,0,0,.04);--nk-shadow-btn: inset 0 0 0 1px rgba(15,15,15,.1), 0 1px 2px rgba(15,15,15,.1);--nk-shadow-knob: 0 1px 2px rgba(0,0,0,.2);--nk-shadow-segment: 0 1px 2px rgba(0,0,0,.08)}[data-theme=dark]{color-scheme:dark;--nk-bg: #191919;--nk-bg-sidebar: #202020;--nk-bg-hover: rgba(255,255,255,.055);--nk-bg-active: rgba(255,255,255,.08);--nk-bg-callout: #252525;--nk-bg-code: #202020;--nk-bg-card: #202020;--nk-bg-input: rgba(255,255,255,.055);--nk-text: rgba(255,255,255,.81);--nk-text-secondary: rgba(255,255,255,.46);--nk-text-tertiary: rgba(255,255,255,.32);--nk-text-sidebar: rgba(255,255,255,.62);--nk-border: rgba(255,255,255,.094);--nk-border-strong: rgba(255,255,255,.16);--nk-accent: #529CCA;--nk-danger: #df5452;--nk-tag-gray-bg: #373737;--nk-tag-gray-text: rgba(255,255,255,.85);--nk-tag-brown-bg: #603b2c;--nk-tag-brown-text: rgba(255,255,255,.85);--nk-tag-orange-bg: #854c1d;--nk-tag-orange-text: rgba(255,255,255,.85);--nk-tag-yellow-bg: #89632a;--nk-tag-yellow-text: rgba(255,255,255,.85);--nk-tag-green-bg: #2b593f;--nk-tag-green-text: rgba(255,255,255,.85);--nk-tag-blue-bg: #28456c;--nk-tag-blue-text: rgba(255,255,255,.85);--nk-tag-purple-bg: #492f64;--nk-tag-purple-text: rgba(255,255,255,.85);--nk-tag-pink-bg: #69314c;--nk-tag-pink-text: rgba(255,255,255,.85);--nk-tag-red-bg: #6e3630;--nk-tag-red-text: rgba(255,255,255,.85);--nk-tint-gray: #252525;--nk-color-gray: #9b9b9b;--nk-tint-brown: #2e2724;--nk-color-brown: #ba856f;--nk-tint-orange: #36291f;--nk-color-orange: #c77d48;--nk-tint-yellow: #372e20;--nk-color-yellow: #ca8f3d;--nk-tint-green: #242b26;--nk-color-green: #529e72;--nk-tint-blue: #1f282d;--nk-color-blue: #5e87c9;--nk-tint-purple: #2a2430;--nk-color-purple: #9d68d3;--nk-tint-pink: #2e2328;--nk-color-pink: #d15796;--nk-tint-red: #332523;--nk-color-red: #df5452;--nk-shadow-menu: rgba(15,15,15,.2) 0px 0px 0px 1px, rgba(15,15,15,.4) 0px 3px 6px, rgba(15,15,15,.6) 0px 9px 24px;--nk-shadow-card: none;--nk-shadow-btn: inset 0 0 0 1px rgba(255,255,255,.1), 0 1px 2px rgba(0,0,0,.3)}";

  // … and everything else, in two pieces around where they were.
  const componentsHead = "[class*=nk-],[class*=nk-] *,[class*=nk-]:before,[class*=nk-]:after,[class*=nk-] *:before,[class*=nk-] *:after{box-sizing:border-box}[class*=nk-] :where(h1,h2,h3,h4,h5,h6,p,figure,blockquote,dl,dd,pre,ul,ol,li){margin:0;padding:0}[class*=nk-] :where(ul,ol){list-style:none}[class*=nk-] :where(button,input,select,textarea,option,optgroup,fieldset,legend),:where(button,input,select,textarea)[class*=nk-]{margin:0;padding:0}[class*=nk-][hidden]:not([hidden=until-found i]){display:none!important}::slotted([hidden]:not([hidden=\"until-found\" i])){display:none!important}";
  const componentsTail = ".nk-body{margin:0;min-height:100vh;min-height:100dvh;font-family:var(--nk-font);font-size:14px;background:var(--nk-bg);color:var(--nk-text);-webkit-font-smoothing:auto;transition:background .25s ease,color .25s ease}.nk-app{display:flex;height:100vh;height:100dvh;overflow:hidden}.nk-sidebar{width:calc(var(--nk-sidebar-width) + env(safe-area-inset-left,0px));min-width:calc(var(--nk-sidebar-width) + env(safe-area-inset-left,0px));padding-left:env(safe-area-inset-left,0px);background:var(--nk-bg-sidebar);display:flex;flex-direction:column;border-right:1px solid var(--nk-border);transition:background .25s ease,margin-left .24s cubic-bezier(.2,.8,.25,1)}.nk-workspace{display:flex;align-items:center;gap:8px;padding:10px 12px;margin:6px 8px 2px;border-radius:var(--nk-radius);cursor:pointer;font-weight:500;user-select:none}.nk-workspace:hover{background:var(--nk-bg-hover)}.nk-workspace .avatar,.nk-workspace ::slotted(.avatar),.nk-workspace ::slotted([slot=\"avatar\"]){width:22px;height:22px;border-radius:4px;background:linear-gradient(135deg,var(--nk-decor-purple),var(--nk-decor-blue));color:var(--nk-on-accent);font-size:11px;font-weight:600;display:flex;align-items:center;justify-content:center}.nk-workspace .chev{margin-left:auto;color:var(--nk-text-tertiary);font-size:11px}.nk-sidebar-scroll{flex:1;overflow-y:auto;padding:4px 8px 16px}.nk-sidebar-scroll::-webkit-scrollbar{width:8px}.nk-sidebar-scroll::-webkit-scrollbar-thumb{background:var(--nk-border-strong);border-radius:4px}.nk-section-label{font-size:12px;font-weight:500;letter-spacing:.01em;color:var(--nk-text-tertiary);padding:14px 10px 4px;display:flex;justify-content:space-between;align-items:center}.nk-section-label .plus{cursor:pointer;border-radius:4px;padding:0 4px;font-size:13px;opacity:0;transition:opacity .15s}.nk-section-label:hover .plus{opacity:1}.nk-section-label .plus:hover{background:var(--nk-bg-hover)}.nk-sidebar-footer{padding:8px;border-top:1px solid var(--nk-border);color:var(--nk-text-sidebar);font-size:14px}.nk-sidebar-footer .nk-tree-item{min-height:26px}.nk-sidebar-backdrop{display:none;position:fixed;inset:0;z-index:59;background:var(--nk-scrim-soft)}.nk-sidebar-toggle{display:none}.nk-sidebar.collapsed{margin-left:calc(0px - var(--nk-sidebar-width) - env(safe-area-inset-left,0px));visibility:hidden;transition:background .25s ease,margin-left .24s cubic-bezier(.2,.8,.25,1),visibility 0s linear .24s}.nk-sidebar-toggle.collapsed{display:inline-flex}.nk-sidebar-head{display:flex;align-items:center}.nk-sidebar-head>.nk-workspace,.nk-sidebar-head ::slotted(.nk-workspace){flex:1;min-width:0}.nk-sidebar-collapse{flex:none;display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;margin:4px 12px 0 -4px;padding:0;border:none;border-radius:var(--nk-radius);background:transparent;color:var(--nk-text-tertiary);cursor:pointer;opacity:0;transition:opacity .15s}.nk-sidebar:hover .nk-sidebar-collapse,.nk-sidebar-collapse:focus-visible{opacity:1}.nk-sidebar-collapse:hover{background:var(--nk-bg-hover);color:var(--nk-text)}.nk-sidebar-collapse svg{width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round}.nk-main{flex:1;display:flex;flex-direction:column;min-width:0}.nk-topbar{height:44px;display:flex;align-items:center;padding:0 max(12px,env(safe-area-inset-right,0px)) 0 max(12px,env(safe-area-inset-left,0px));gap:8px;flex-shrink:0}.nk-breadcrumb{display:flex;align-items:center;gap:4px;font-size:14px}.nk-breadcrumb .crumb,.nk-breadcrumb ::slotted(.crumb){padding:2px 6px;border-radius:4px;cursor:pointer;color:var(--nk-text-sidebar);display:flex;align-items:center;gap:5px}.nk-breadcrumb .crumb:hover{background:var(--nk-bg-hover)}a.crumb{text-decoration:none}.nk-breadcrumb .crumb.current,.nk-breadcrumb ::slotted(.crumb.current){color:var(--nk-text)}.nk-breadcrumb .sep{color:var(--nk-text-tertiary);font-size:12px}.nk-topbar-actions{margin-left:auto;display:flex;align-items:center;gap:4px}.nk-topbar-btn{padding:4px 8px;border-radius:var(--nk-radius);cursor:pointer;color:var(--nk-text-sidebar);font-size:14px;user-select:none;border:none;background:transparent;font-family:inherit}.nk-topbar-btn:hover{background:var(--nk-bg-hover)}.nk-topbar-btn svg,.nk-topbar-btn ::slotted(svg){width:16px;height:16px;flex-shrink:0;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round}.nk-share-btn{font-size:14px;padding:4px 8px}.nk-topbar-meta,.nk-topbar-actions ::slotted(.nk-topbar-meta){padding:4px 8px;font-size:12px;color:var(--nk-text-tertiary);white-space:nowrap}.nk-theme-toggle{font-size:15px;width:30px;height:28px;padding:0}.nk-tab-bar{display:none;flex:none;align-items:stretch;gap:2px;position:sticky;bottom:0;z-index:30;box-sizing:border-box;height:calc(var(--nk-tab-bar-height) - 6px + max(6px,env(safe-area-inset-bottom,0px)));padding:6px max(4px,env(safe-area-inset-right,0px)) max(6px,env(safe-area-inset-bottom,0px)) max(4px,env(safe-area-inset-left,0px));background:var(--nk-bg-sidebar);border-top:1px solid var(--nk-border)}.nk-tab-bar.always{display:flex}.nk-tab-bar.fixed{position:fixed;left:0;right:0;bottom:0}.nk-tab-bar-spacer{display:none;flex:none;height:calc(var(--nk-tab-bar-height) - 6px + max(6px,env(safe-area-inset-bottom,0px)))}.nk-tab-bar.always.fixed+.nk-tab-bar-spacer{display:block}.nk-tab-bar.floating{position:fixed;left:max(12px,env(safe-area-inset-left,0px));right:max(12px,env(safe-area-inset-right,0px));bottom:calc(12px + env(safe-area-inset-bottom,0px));height:var(--nk-tab-bar-height);padding:6px;border:1px solid var(--nk-border);border-radius:16px;box-shadow:var(--nk-shadow-menu)}.nk-tab-bar-item{flex:1 1 0;min-width:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;padding:4px 2px;border:none;border-radius:var(--nk-radius);background:transparent;font-family:inherit;font-size:10.5px;line-height:1.2;color:var(--nk-text-secondary);text-decoration:none;cursor:pointer;user-select:none;-webkit-tap-highlight-color:transparent}.nk-tab-bar-item:hover{color:var(--nk-text);background:var(--nk-bg-hover)}.nk-tab-bar-item.active{color:var(--nk-accent)}.nk-tab-bar-item:disabled{opacity:.5;cursor:not-allowed}.nk-tab-bar-item .icon,.nk-tab-bar-item ::slotted([slot=\"icon\"]){font-size:20px;line-height:1}.nk-tab-bar-item .label,.nk-tab-bar-item ::slotted(.label){max-width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.nk-tree-item{display:flex;align-items:center;gap:6px;padding:3px 8px 3px 10px;border-radius:var(--nk-radius);cursor:pointer;user-select:none;color:var(--nk-text-sidebar);position:relative;min-height:var(--_nk-row, 28px)}.nk-tree-item:hover{background:var(--nk-bg-hover)}.nk-tree-item.active{background:var(--nk-bg-active);color:var(--nk-text);font-weight:500}.nk-tree-item.compact{min-height:26px}.nk-tree-item .icon,.nk-tree-item ::slotted(.icon),.nk-tree-item ::slotted([slot=\"icon\"]){font-size:15px;width:20px;text-align:center;flex-shrink:0}.nk-tree-item .label,.nk-tree-item ::slotted(.label){white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.nk-tree-item .actions{margin-left:auto;display:none;gap:2px;color:var(--nk-text-tertiary)}.nk-tree-item:hover .actions{display:flex}.nk-tree-item .actions span{padding:0 3px;border-radius:3px;font-size:12px}.nk-tree-item .actions span:hover{background:var(--nk-bg-active)}.nk-tree-children{margin-left:14px;border-left:1px solid var(--nk-border);padding-left:4px;overflow:hidden}.nk-tree-children.collapsed{display:none}.nk-toggle-arrow{width:16px;height:16px;flex-shrink:0;display:inline-flex;align-items:center;justify-content:center;font-size:9px;color:var(--nk-text-tertiary);border-radius:3px;transition:transform .15s}.nk-toggle-arrow.open{transform:rotate(90deg)}.nk-kbd-hint{margin-left:auto;display:flex;gap:3px;align-items:center}.nk-kbd{font-family:inherit;font-size:10.5px;background:var(--nk-bg-callout);border:1px solid var(--nk-border-strong);border-bottom-width:2px;border-radius:4px;padding:0 4px}.nk-page-scroll{flex:1;overflow-y:auto}.nk-page-scroll::-webkit-scrollbar{width:10px}.nk-page-scroll::-webkit-scrollbar-thumb{background:var(--nk-border-strong);border-radius:5px;border:2px solid var(--nk-bg)}.nk-cover{height:200px;background:radial-gradient(ellipse at 20% 40%,color-mix(in srgb,var(--nk-decor-purple) 35%,transparent),transparent 55%),radial-gradient(ellipse at 80% 20%,color-mix(in srgb,var(--nk-decor-blue) 35%,transparent),transparent 55%),radial-gradient(ellipse at 60% 90%,color-mix(in srgb,var(--nk-decor-orange) 25%,transparent),transparent 55%),var(--nk-bg-callout)}.nk-cover>img,.nk-cover ::slotted(img){display:block;width:100%;height:100%;object-fit:cover}.nk-page{max-width:760px;margin:0 auto;padding:24px max(64px,env(safe-area-inset-right,0px)) 120px max(64px,env(safe-area-inset-left,0px));font-size:16px;line-height:1.5}.nk-page.full{max-width:none}.nk-page.small{font-size:14px}.nk-cover+.nk-page,.nk-page.covered{padding-top:0}.nk-page-icon,.nk-page ::slotted(.nk-page-icon),.nk-page ::slotted([slot=\"icon\"]){font-size:64px;line-height:1;margin-top:0;cursor:pointer;user-select:none;display:inline-block}.nk-cover+.nk-page .nk-page-icon,.nk-cover+.nk-page ::slotted(.nk-page-icon),.nk-cover+.nk-page ::slotted([slot=\"icon\"]),.nk-page.covered .nk-page-icon,.nk-page.covered ::slotted(.nk-page-icon),.nk-page.covered ::slotted([slot=\"icon\"]){margin-top:-42px}.nk-page-title{font-size:var(--_nk-title, 40px);font-weight:700;line-height:1.2;margin:8px 0 4px;outline:none;letter-spacing:0}.nk-page-meta{color:var(--nk-text-tertiary);font-size:13px;margin-bottom:24px;display:flex;gap:16px}.nk-props{display:flex;flex-direction:column;margin:0 0 20px;font-size:14px;line-height:1.5}.nk-prop{display:flex;align-items:flex-start;min-width:0}.nk-prop .p-name{flex:0 0 160px;min-width:0;min-height:34px;padding:0 6px;display:flex;align-items:center;gap:6px;overflow:hidden;border-radius:var(--nk-radius);color:var(--nk-text-secondary);white-space:nowrap;user-select:none}.nk-prop .p-icon,.nk-prop ::slotted([slot=\"icon\"]){width:16px;flex-shrink:0;text-align:center;font-size:13px}.nk-prop .p-value{flex:1 1 auto;min-width:0;min-height:34px;padding:5px 8px;display:flex;align-items:center;flex-wrap:wrap;gap:4px 6px;border-radius:var(--nk-radius);overflow-wrap:anywhere}.nk-prop .p-name:hover,.nk-prop .p-value:hover{background:var(--nk-bg-hover)}.nk-page p.lead{font-size:1em;line-height:1.6;color:var(--nk-text-secondary);margin-bottom:20px}.nk-page ::slotted(p.lead){font-size:1em;line-height:1.6;color:var(--nk-text-secondary);margin-bottom:20px!important}.nk-heading{font-size:1.5em;font-weight:600;line-height:1.3;margin:32px 0 10px;display:flex;align-items:center;gap:8px}.nk-callout{display:flex;gap:10px;align-items:flex-start;background:var(--nk-bg-callout);border-radius:var(--nk-radius);padding:16px 16px 16px 12px;margin:16px 0;line-height:1.5}.nk-callout .c-icon,.nk-callout ::slotted(.c-icon),.nk-callout ::slotted([slot=\"icon\"]){font-size:17px}.nk-bookmark{display:flex;min-width:0;margin:8px 0;overflow:hidden;border:1px solid var(--nk-border);border-radius:var(--nk-radius);color:var(--nk-text);text-decoration:none;transition:background .12s ease}.nk-bookmark:hover{background:var(--nk-bg-hover)}.nk-bookmark .bm-text{flex:1 1 0;min-width:0;padding:12px 14px 14px;font-size:14px;line-height:20px}.nk-bookmark .bm-title{display:block;min-height:24px;margin-bottom:2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.nk-bookmark .bm-desc{display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:2;overflow:hidden;max-height:32px;font-size:12px;line-height:16px;color:var(--nk-text-secondary)}.nk-bookmark .bm-url{display:flex;align-items:center;gap:6px;margin-top:6px;font-size:12px;line-height:16px;white-space:nowrap}.nk-bookmark .bm-url>span{min-width:0;overflow:hidden;text-overflow:ellipsis}.nk-bookmark .bm-favicon{flex:none;width:16px;height:16px}.nk-bookmark .bm-cover{position:relative;flex:0 0 33%;max-width:240px}.nk-bookmark .bm-cover>img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}.nk-todo{display:flex;align-items:flex-start;gap:8px;padding:4px 0}.nk-todo input[type=checkbox]{appearance:none;width:16px;height:16px;margin-top:3px;border:1.4px solid var(--nk-border-strong);border-radius:2px;cursor:pointer;flex-shrink:0;position:relative;background:var(--nk-bg)}.nk-todo input[type=checkbox]:checked{background:var(--nk-accent);border-color:var(--nk-accent)}.nk-todo input[type=checkbox]:checked:after{content:\"\\2713\";color:var(--nk-on-accent);font-size:11px;position:absolute;inset:0;display:flex;align-items:center;justify-content:center}.nk-todo input:checked+span{text-decoration:line-through;color:var(--nk-text-tertiary)}.nk-todo input:disabled{opacity:.5;cursor:not-allowed}.nk-toggle{margin:6px 0}.nk-toggle summary{cursor:pointer;list-style:none;display:flex;align-items:center;gap:6px;padding:4px 6px;border-radius:var(--nk-radius);font-weight:500;user-select:none}.nk-toggle summary::-webkit-details-marker{display:none}.nk-toggle summary:hover{background:var(--nk-bg-hover)}.nk-toggle summary:before{content:\"\\25b8\";color:var(--nk-text-tertiary);font-size:11px;transition:transform .15s;display:inline-block}.nk-toggle[open] summary:before{transform:rotate(90deg)}.nk-toggle .toggle-body,.nk-toggle ::slotted(.toggle-body){padding:4px 0 8px 26px;color:var(--nk-text-secondary);line-height:1.55}.nk-divider{border:none;border-top:1px solid var(--nk-border);margin:24px 0}.nk-quote{border-left:3px solid var(--nk-text);padding:2px 0 2px 14px;margin:16px 0;color:var(--nk-text-secondary);line-height:1.5}.nk-quote .q-cite{display:block;margin-top:6px;font-size:12.5px;color:var(--nk-text-tertiary)}.nk-mention{display:inline-flex;align-items:center;gap:4px;padding:0 4px;border-radius:4px;font-weight:500;cursor:pointer;white-space:nowrap}.nk-mention:hover{background:var(--nk-bg-hover)}.nk-mention.person{color:var(--nk-text)}.nk-mention.person .mini-avatar,.nk-mention.person .nk-avatar,.nk-mention.person ::slotted(.mini-avatar),.nk-mention.person ::slotted([slot=\"avatar\"]){width:16px;height:16px;border-radius:50%;color:var(--nk-on-accent);font-size:8.5px;font-weight:600;display:inline-flex;align-items:center;justify-content:center}.nk-mention.page{color:var(--nk-text-secondary);text-decoration:underline;text-decoration-color:var(--nk-border-strong);text-underline-offset:2px}.nk-mention.date{color:var(--nk-color-orange)}.nk-inline-code{font-family:var(--nk-font-mono);font-size:85%;color:var(--nk-danger);background:color-mix(in srgb,var(--nk-danger) 8%,transparent);border-radius:4px;padding:1px 5px}.nk-code{background:var(--nk-bg-code);border:1px solid var(--nk-border);border-radius:var(--nk-radius);padding:20px 24px;font-family:var(--nk-font-mono);font-size:13px;line-height:1.6;margin:14px 0;overflow-x:auto;color:var(--nk-text-secondary);white-space:pre;position:relative}.nk-code .lang{position:absolute;top:6px;right:10px;font-size:11px;color:var(--nk-text-tertiary);font-family:var(--nk-font)}.nk-code .tag,.nk-code ::slotted(.tag){color:var(--nk-accent)}.nk-code .attr,.nk-code ::slotted(.attr){color:var(--nk-color-orange)}.nk-database{margin:12px 0 8px}.nk-db-tabs{display:flex;gap:2px;align-items:center;padding:0 0 6px;margin-bottom:0;font-size:14px}.nk-db-tab{padding:4px 8px;cursor:pointer;color:var(--nk-text-secondary);border-radius:var(--nk-radius);user-select:none;line-height:20px;display:flex;align-items:center;gap:6px;flex-shrink:0;white-space:nowrap}.nk-db-tab:hover{color:var(--nk-text);background:var(--nk-bg-hover)}.nk-db-tab.active{color:var(--nk-text);background:var(--nk-bg-active);font-weight:500}.nk-db-tab.add{color:var(--nk-text-tertiary)}.nk-db-tab .badge,.nk-db-tab ::slotted(.badge){background:var(--nk-bg-callout);border-radius:8px;font-size:11px;padding:0 6px;color:var(--nk-text-tertiary)}.nk-db-toolbar{display:flex;align-items:center;gap:8px;min-width:0;padding-bottom:6px}.nk-db-toolbar>.nk-db-tabs{flex:1 1 auto;min-width:0;padding-bottom:0;overflow-x:auto;scrollbar-width:none}.nk-db-toolbar>.nk-db-tabs::-webkit-scrollbar{display:none}.nk-db-toolbar .tools{flex:none;display:flex;align-items:center;gap:4px;margin-left:auto}.nk-db-tool{display:inline-flex;align-items:center;gap:4px;height:28px;padding:0 8px;border:none;border-radius:var(--nk-radius);background:transparent;font-family:inherit;font-size:14px;color:var(--nk-text-secondary);white-space:nowrap;cursor:pointer;user-select:none}.nk-db-tool:hover{background:var(--nk-bg-hover);color:var(--nk-text)}.nk-db-tool.active{color:var(--nk-accent)}.nk-filter-row{display:flex;align-items:center;flex-wrap:wrap;gap:6px;padding:2px 0 8px}.nk-filter-pill{display:inline-flex;align-items:center;gap:4px;height:24px;padding:0 8px;border:1px solid var(--nk-border-strong);border-radius:12px;background:transparent;font-family:inherit;font-size:13px;line-height:1;color:var(--nk-text-secondary);white-space:nowrap;cursor:pointer;user-select:none}.nk-filter-pill:hover{background:var(--nk-bg-hover)}.nk-filter-pill.active{color:var(--nk-accent);border-color:color-mix(in srgb,var(--nk-accent) 35%,transparent);background:color-mix(in srgb,var(--nk-accent) 6%,transparent)}.nk-filter-pill.add{border-color:transparent;color:var(--nk-text-tertiary)}.nk-filter-pill.add:hover{color:var(--nk-text-secondary)}.nk-filter-pill button{padding:0;border:none;background:none;font:inherit;color:inherit;cursor:pointer}.nk-filter-pill .fp-remove{display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;margin-right:-4px;border-radius:50%;font-size:13px}.nk-filter-pill .fp-remove:hover{background:color-mix(in srgb,currentColor 15%,transparent)}.nk-table-wrap{overflow-x:auto}.nk-table{width:100%;border-collapse:collapse;font-size:14px}.nk-table th{text-align:left;font-weight:400;color:var(--nk-text-secondary);height:36px;padding:0 8px;border-bottom:1px solid var(--nk-border);border-right:1px solid var(--nk-border);font-size:14px;white-space:nowrap;user-select:none;cursor:pointer}.nk-table th:hover{background:var(--nk-bg-hover)}.nk-table th .th-icon,.nk-table th ::slotted(.th-icon){margin-right:6px;font-size:13px;color:var(--nk-text-tertiary)}.nk-table td{height:36px;padding:6px 8px;line-height:1.5;border-bottom:1px solid var(--nk-border);border-right:1px solid var(--nk-border);white-space:nowrap;vertical-align:middle}.nk-table th:last-child,.nk-table td:last-child{border-right:none}.nk-table.wrap td,.nk-table td.wrap{white-space:normal;vertical-align:top}.nk-table tbody tr:hover{background:var(--nk-bg-hover)}.nk-table .row-title{display:flex;align-items:center;gap:7px;font-weight:500;cursor:pointer}.nk-table .row-title:hover{text-decoration:underline;text-decoration-color:var(--nk-text-tertiary)}.nk-table .date-cell{color:var(--nk-text-secondary)}.nk-table .num{text-align:right;font-variant-numeric:tabular-nums}.nk-table .person-cell{display:flex;align-items:center;gap:6px;color:var(--nk-text-secondary)}.nk-table .person-cell .mini-avatar,.nk-table .person-cell ::slotted(.mini-avatar){width:20px;height:20px;border-radius:50%;font-size:10px;color:var(--nk-on-accent);display:inline-flex;align-items:center;justify-content:center;font-weight:600}.nk-new-row,.nk-table .new-row{color:var(--nk-text-tertiary);cursor:pointer;padding:8px;display:flex;align-items:center;gap:7px;font-size:14px}.nk-new-row:hover,.nk-table .new-row:hover{background:var(--nk-bg-hover)}.nk-tag{display:inline-flex;align-items:center;max-width:100%;height:20px;padding:0 6px;border-radius:3px;font-size:14px;font-weight:400;line-height:20px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;vertical-align:middle;background:var(--nk-tag-gray-bg);color:var(--nk-tag-gray-text)}.nk-tag.gray{background:var(--nk-tag-gray-bg);color:var(--nk-tag-gray-text)}.nk-tag.brown{background:var(--nk-tag-brown-bg);color:var(--nk-tag-brown-text)}.nk-tag.orange{background:var(--nk-tag-orange-bg);color:var(--nk-tag-orange-text)}.nk-tag.yellow{background:var(--nk-tag-yellow-bg);color:var(--nk-tag-yellow-text)}.nk-tag.green{background:var(--nk-tag-green-bg);color:var(--nk-tag-green-text)}.nk-tag.blue{background:var(--nk-tag-blue-bg);color:var(--nk-tag-blue-text)}.nk-tag.purple{background:var(--nk-tag-purple-bg);color:var(--nk-tag-purple-text)}.nk-tag.pink{background:var(--nk-tag-pink-bg);color:var(--nk-tag-pink-text)}.nk-tag.red{background:var(--nk-tag-red-bg);color:var(--nk-tag-red-text)}.nk-progress{width:110px;height:6px;border-radius:3px;background:var(--nk-tint-blue);overflow:hidden;display:inline-block}.nk-progress>i{display:block;height:100%;background:var(--nk-color-blue);border-radius:3px}.nk-progress.wide{width:100%;flex:1 1 60px}.nk-progress-label{font-size:12px;color:var(--nk-text-tertiary);margin-left:7px;white-space:nowrap}.nk-list{display:flex;flex-direction:column;font-size:14px}.nk-list-item{display:flex;align-items:center;gap:8px;min-width:0;overflow:hidden;min-height:36px;padding:4px 6px;border-bottom:1px solid var(--nk-border);color:inherit;text-decoration:none;cursor:pointer}.nk-list-item:hover{background:var(--nk-bg-hover)}.nk-list>.nk-list-item:last-child,.nk-list ::slotted(.nk-list-item:last-child),.nk-list-item.last{border-bottom:none}.nk-list-item .l-icon,.nk-list-item ::slotted([slot=\"icon\"]){width:20px;flex-shrink:0;text-align:center;font-size:15px}.nk-list-item .l-title,.nk-list-item ::slotted(.l-title){flex:1 1 auto;min-width:0;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.nk-list-item .l-meta,.nk-list-item ::slotted(.l-meta){flex:none;display:flex;align-items:center;gap:8px;margin-left:auto;font-size:12px;color:var(--nk-text-secondary);white-space:nowrap}.nk-calendar-view{font-size:14px}.nk-calendar-view .cv-head{display:flex;align-items:center;gap:4px;padding:2px 0 8px}.nk-calendar-view .cv-title{flex:1;font-weight:600;white-space:nowrap}.nk-calendar-view .cv-grid{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));border-top:1px solid var(--nk-border);border-left:1px solid var(--nk-border)}.nk-calendar-view.weeks .cv-grid{grid-template-columns:32px repeat(7,minmax(0,1fr))}.nk-calendar-view :is(.cv-wd,.cv-week,.cv-day){min-width:0;border-right:1px solid var(--nk-border);border-bottom:1px solid var(--nk-border)}.nk-calendar-view .cv-wd{padding:4px 8px;font-size:12px;color:var(--nk-text-secondary);text-align:center}.nk-calendar-view .cv-week{padding:6px 0;font-size:11px;color:var(--nk-text-tertiary);text-align:center;font-variant-numeric:tabular-nums}.nk-calendar-view .cv-day{display:flex;flex-direction:column;gap:3px;min-height:112px;padding:4px 4px 6px}.nk-calendar-view .cv-day:is(.out,.off){background:var(--nk-bg-sidebar)}.nk-calendar-view .cv-num{align-self:flex-end;min-width:22px;height:22px;padding:0 6px;border-radius:11px;font-size:13px;line-height:22px;text-align:center;color:var(--nk-text-secondary);font-variant-numeric:tabular-nums}.nk-calendar-view .cv-day.out .cv-num{color:var(--nk-text-tertiary)}.nk-calendar-view .cv-day.today .cv-num{background:var(--nk-danger);color:var(--nk-on-accent)}.nk-calendar-view .cv-item{display:block;min-width:0;padding:3px 6px;border-radius:4px;background:var(--nk-bg-card);border:1px solid var(--nk-border);box-shadow:var(--nk-shadow-card);font-size:12.5px;line-height:18px;color:var(--nk-text);text-align:left;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;cursor:pointer}.nk-calendar-view .cv-item:hover{background:var(--nk-bg-hover)}.nk-board{display:none;gap:12px;padding:14px 2px;overflow-x:auto}.nk-board.active{display:flex}.nk-board-col{min-width:220px;width:220px;flex-shrink:0}.nk-board-col-header{display:flex;align-items:center;gap:7px;margin-bottom:8px;font-size:13.5px;font-weight:500}.nk-board-col-header .count{color:var(--nk-text-tertiary);font-weight:400}.nk-card{background:var(--nk-bg-card);border:1px solid var(--nk-border);border-radius:8px;padding:10px 12px;margin-bottom:8px;cursor:grab;box-shadow:var(--nk-shadow-card);transition:border-color .15s}.nk-card:hover{border-color:var(--nk-border-strong)}.nk-card .card-title,.nk-card ::slotted(.card-title){font-weight:500;margin-bottom:7px;display:flex;gap:6px;align-items:center}.nk-card .card-meta,.nk-card ::slotted(.card-meta){display:flex;gap:6px;flex-wrap:wrap;align-items:center;font-size:12px;color:var(--nk-text-secondary)}.nk-field{display:flex;align-items:center;justify-content:space-between;gap:24px;padding:9px 0}.nk-field .f-label,.nk-field ::slotted(.f-label),.nk-field ::slotted([slot=\"label\"]){font-size:14px}.nk-field .f-desc,.nk-field ::slotted(.f-desc),.nk-field ::slotted([slot=\"desc\"]){font-size:12px;color:var(--nk-text-secondary);margin-top:2px;line-height:1.45}.nk-field .f-control,.nk-field ::slotted(.f-control){flex-shrink:0}.nk-field.stacked,.nk-fields>.nk-field{flex-direction:column;align-items:stretch;gap:6px;min-width:0}.nk-field.stacked .f-control,.nk-fields>.nk-field .f-control{width:100%;min-width:0}.nk-field.stacked .nk-input,.nk-field.stacked .nk-textarea,.nk-field.stacked .nk-select,.nk-fields>.nk-field .nk-input,.nk-fields>.nk-field .nk-textarea,.nk-fields>.nk-field .nk-select{width:100%;min-width:0}.nk-field.compact,.nk-fields>.nk-field{padding:0;gap:4px;min-width:0}.nk-field.compact .f-label,.nk-field.compact ::slotted([slot=\"label\"]),.nk-fields>.nk-field .f-label,.nk-fields>.nk-field ::slotted([slot=\"label\"]){font-size:12px;color:var(--nk-text-tertiary)}.nk-fields{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:10px 12px;padding:9px 0}.nk-fields>.nk-field{min-width:0}.nk-input,.nk-select,.nk-textarea{font-family:inherit;font-size:14px;line-height:20px;color:var(--nk-text);background:var(--nk-bg-input);border:1px solid var(--nk-border);border-radius:var(--nk-radius);padding:5px 10px;outline:none;transition:border-color .15s,box-shadow .15s;min-width:210px}.nk-input:hover,.nk-select:hover,.nk-textarea:hover{border-color:var(--nk-border-strong)}.nk-input:focus,.nk-select:focus,.nk-textarea:focus{border-color:color-mix(in srgb,var(--nk-accent) 60%,transparent);box-shadow:0 0 0 2px color-mix(in srgb,var(--nk-accent) 30%,transparent)}.nk-input::placeholder,.nk-textarea::placeholder{color:var(--nk-text-tertiary)}.nk-input.wide,.nk-textarea.wide,.nk-select.wide{width:100%;min-width:0}.nk-input[type=date],.nk-input[type=time],.nk-input[type=datetime-local],.nk-input[type=month]{appearance:none}.nk-input::-webkit-date-and-time-value{text-align:start;min-height:1lh}.nk-select.compact{min-width:120px}.nk-textarea{resize:vertical;min-height:70px;line-height:1.5}.nk-select{appearance:none;cursor:pointer;padding-right:26px;background-image:linear-gradient(45deg,transparent 50%,var(--nk-text-tertiary) 50%),linear-gradient(135deg,var(--nk-text-tertiary) 50%,transparent 50%);background-position:calc(100% - 14px) 55%,calc(100% - 10px) 55%;background-size:4px 4px;background-repeat:no-repeat}.nk-copy-field{display:flex;align-items:center;gap:2px;min-width:210px;max-width:100%;padding:0 3px 0 10px;font-size:14px;line-height:20px;color:var(--nk-text);background:var(--nk-bg-input);border:1px solid var(--nk-border);border-radius:var(--nk-radius)}.nk-copy-field.wide{width:100%;min-width:0}.nk-copy-field.wrap{align-items:flex-start}.nk-copy-field .cf-value{flex:1 1 auto;min-width:0;padding:5px 6px 5px 0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;user-select:all}.nk-copy-field.mono .cf-value{font-family:var(--nk-font-mono);font-size:13px}.nk-copy-field.wrap .cf-value{white-space:pre-wrap;overflow-wrap:anywhere}.nk-copy-field .cf-btn{flex:none;height:24px;margin:3px 0;padding:0 8px;border:none;border-radius:4px;background:transparent;font-family:inherit;font-size:13px;color:var(--nk-text-secondary);white-space:nowrap;cursor:pointer}.nk-copy-field .cf-btn:hover{background:var(--nk-bg-hover);color:var(--nk-text)}.nk-copy-field .cf-btn.copied{color:var(--nk-color-green)}.nk-calendar{display:block;width:max-content;max-width:100%;margin-inline:auto;font-size:14px;user-select:none;-webkit-user-select:none}.nk-calendar .cal-head{display:flex;align-items:center;gap:2px;padding:0 0 6px 6px}.nk-calendar .cal-title{flex:1;font-weight:600;white-space:nowrap}.nk-calendar .cal-nav,.nk-calendar-view .cal-nav{display:inline-flex;align-items:center;justify-content:center;height:28px;min-width:28px;padding:0 6px;border:none;border-radius:var(--nk-radius);background:transparent;font-family:inherit;font-size:13px;color:var(--nk-text-secondary);cursor:pointer}.nk-calendar .cal-nav:hover,.nk-calendar-view .cal-nav:hover{background:var(--nk-bg-hover);color:var(--nk-text)}.nk-calendar .cal-nav svg,.nk-calendar-view .cal-nav svg{width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round}.nk-calendar .cal-grid{display:grid;grid-template-columns:repeat(7,var(--_nk-cell, 36px));row-gap:2px}.nk-calendar.weeks .cal-grid{grid-template-columns:24px repeat(7,var(--_nk-cell, 36px))}.nk-calendar .cal-wd,.nk-calendar .cal-week{display:flex;align-items:center;justify-content:center;font-size:12px;color:var(--nk-text-tertiary);font-variant-numeric:tabular-nums}.nk-calendar .cal-wd{height:24px}.nk-calendar .cal-week{font-size:11px}.nk-calendar .cal-day{position:relative;display:inline-flex;align-items:center;justify-content:center;width:var(--_nk-cell, 36px);height:var(--_nk-cell, 36px);padding:0;border:none;border-radius:var(--nk-radius);background:transparent;font-family:inherit;font-size:14px;color:var(--nk-text);font-variant-numeric:tabular-nums;cursor:pointer}.nk-calendar .cal-day:hover{background:var(--nk-bg-hover)}.nk-calendar .cal-day:focus-visible{outline:2px solid color-mix(in srgb,var(--nk-accent) 60%,transparent);outline-offset:-2px}.nk-calendar .cal-day.out,.nk-calendar .cal-day.off{color:var(--nk-text-tertiary)}.nk-calendar .cal-day.today{color:var(--nk-danger);font-weight:600}.nk-calendar .cal-day.in-range{border-radius:0;background:color-mix(in srgb,var(--nk-accent) 12%,transparent)}.nk-calendar .cal-day:is(.selected,.start,.end){background:var(--nk-accent);color:var(--nk-on-accent);font-weight:500}.nk-calendar .cal-day.start:not(.end){border-radius:var(--nk-radius) 0 0 var(--nk-radius)}.nk-calendar .cal-day.end:not(.start){border-radius:0 var(--nk-radius) var(--nk-radius) 0}.nk-calendar .cal-day[aria-disabled=true]{color:var(--nk-text-tertiary);background:none;opacity:.5;cursor:default}.nk-calendar .cal-marks{position:absolute;left:0;right:0;bottom:3px;display:flex;justify-content:center;gap:2px;pointer-events:none}.nk-calendar .cal-marks i{width:4px;height:4px;border-radius:50%;background:var(--nk-color-gray)}.nk-calendar .cal-day:is(.selected,.start,.end) .cal-marks i{background:var(--nk-on-accent)}.nk-calendar .cal-foot{display:flex;align-items:center;gap:8px;margin-top:8px;padding:8px 2px 0;border-top:1px solid var(--nk-border)}.nk-calendar .cal-foot .nk-input{min-width:0;flex:1 1 auto}.nk-btn{font-family:inherit;font-size:14px;line-height:20px;font-weight:500;border-radius:var(--nk-radius);cursor:pointer;padding:3px 12px;border:1px solid transparent;transition:background .15s,border-color .15s,box-shadow .15s,opacity .15s;display:inline-flex;align-items:center;gap:6px;white-space:nowrap}.nk-btn svg,.nk-btn ::slotted(svg){width:15px;height:15px;flex-shrink:0;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round}.nk-btn.primary{background:var(--nk-accent);color:var(--nk-on-accent)}.nk-btn.primary:hover{opacity:.88}.nk-btn.secondary{background:var(--nk-bg-card);color:var(--nk-text);box-shadow:var(--nk-shadow-btn)}.nk-btn.secondary:hover{background:color-mix(in srgb,var(--nk-bg-card),var(--nk-text) 6%)}.nk-btn.danger{background:var(--nk-bg-card);color:var(--nk-danger);box-shadow:var(--nk-shadow-btn)}.nk-btn.danger:hover{background:color-mix(in srgb,var(--nk-danger) 8%,var(--nk-bg-card))}.nk-btn.danger-solid{background:var(--nk-danger);color:var(--nk-on-accent)}.nk-btn.danger-solid:hover{opacity:.88}.nk-btn.small{padding:1px 8px;font-size:13px;line-height:20px}a.nk-btn{text-decoration:none}.nk-btn:disabled{opacity:.5;cursor:not-allowed;pointer-events:none}.nk-switch{position:relative;width:34px;height:20px;flex-shrink:0;appearance:none;border:none;cursor:pointer;border-radius:20px;background:var(--nk-border-strong);transition:background .2s}.nk-switch:after{content:\"\";position:absolute;top:2px;left:2px;width:16px;height:16px;border-radius:50%;background:var(--nk-on-accent);transition:transform .2s;box-shadow:var(--nk-shadow-knob)}.nk-switch:checked,.nk-switch[aria-checked=true]{background:var(--nk-accent)}.nk-switch:checked:after,.nk-switch[aria-checked=true]:after{transform:translate(14px)}.nk-switch:disabled{opacity:.5;cursor:not-allowed}.nk-switch-label{display:inline-flex;align-items:flex-start;gap:8px;font-size:14px;cursor:pointer}.nk-switch-label>.nk-switch{font-size:inherit;line-height:inherit;margin-top:max(0px,calc((1lh - 20px)/2))}.nk-switch-label>span{padding-top:max(0px,calc((20px - 1lh)/2))}.nk-switch-label:has(.nk-switch:disabled){cursor:not-allowed;color:var(--nk-text-tertiary)}.nk-check{display:flex;align-items:flex-start;gap:8px;padding:4px 0;font-size:14px;cursor:pointer}.nk-check input{appearance:none;width:16px;height:16px;flex-shrink:0;border:1.4px solid var(--nk-border-strong);border-radius:3px;cursor:pointer;position:relative;background:var(--nk-bg);font-size:inherit;line-height:inherit;margin-top:max(0px,calc((1lh - 16px)/2))}.nk-check input[type=radio]{border-radius:50%}.nk-check input:checked{background:var(--nk-accent);border-color:var(--nk-accent)}.nk-check input:checked:after{content:\"\\2713\";color:var(--nk-on-accent);font-size:10.5px;position:absolute;inset:0;display:flex;align-items:center;justify-content:center}.nk-check input[type=radio]:checked:after{content:\"\\25cf\";font-size:7px}.nk-check input:disabled{opacity:.5;cursor:not-allowed}.nk-check input:disabled+*{opacity:.6}.nk-slider{width:210px;accent-color:var(--nk-accent);cursor:pointer}.nk-slider-value{font-size:12.5px;color:var(--nk-text-tertiary);margin-top:3px}.nk-profile-row{display:flex;align-items:center;gap:14px;padding:8px 0}.nk-profile-row .big-avatar,.nk-profile-row ::slotted(.big-avatar),.nk-profile-row ::slotted([slot=\"avatar\"]){width:56px;height:56px;border-radius:50%;background:linear-gradient(135deg,var(--nk-decor-purple),var(--nk-decor-blue));color:var(--nk-on-accent);font-size:20px;font-weight:600;display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0}.nk-profile-row .big-avatar>img{display:block;width:100%;height:100%;object-fit:cover}.nk-profile-row .big-avatar.square,.nk-profile-row ::slotted(.big-avatar.square){border-radius:8px}.nk-profile-row .pr-actions{display:flex;flex-direction:column;gap:6px}.nk-profile-row .pr-remove{color:var(--nk-text-tertiary)}.nk-model-card{border:1px solid var(--nk-border-strong);border-radius:8px;padding:12px 14px;margin-bottom:10px;cursor:pointer;display:flex;align-items:flex-start;gap:11px;transition:border-color .15s,background .15s}.nk-model-card:hover{background:var(--nk-bg-hover)}.nk-model-card.selected{border-color:var(--nk-accent);background:color-mix(in srgb,var(--nk-accent) 6%,transparent)}.nk-model-card .m-radio{width:15px;height:15px;border-radius:50%;flex-shrink:0;margin-top:2px;border:1.4px solid var(--nk-border-strong);position:relative}.nk-model-card.selected .m-radio{border-color:var(--nk-accent)}.nk-model-card.selected .m-radio:after{content:\"\";position:absolute;inset:3px;border-radius:50%;background:var(--nk-accent)}.nk-model-card .m-name,.nk-model-card ::slotted(.m-name),.nk-model-card ::slotted([slot=\"title\"]){font-weight:500;font-size:14px;display:flex;align-items:center;gap:7px}.nk-model-card .m-desc,.nk-model-card ::slotted(.m-desc),.nk-model-card ::slotted([slot=\"desc\"]){font-size:12px;color:var(--nk-text-secondary);margin-top:2px;line-height:1.45}.nk-danger-zone{border:1px solid color-mix(in srgb,var(--nk-danger) 40%,transparent);border-radius:8px;padding:14px 16px;margin-top:26px}.nk-danger-zone .dz-title{color:var(--nk-danger);font-weight:600;font-size:13px;margin-bottom:8px}.nk-member-row{display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:1px solid var(--nk-border)}.nk-member-list>.nk-member-row:last-child,.nk-member-list ::slotted(.nk-member-row:last-child),.nk-member-row.last{border-bottom:none}.nk-member-row .mini-avatar,.nk-member-row .nk-avatar,.nk-member-row ::slotted(.mini-avatar),.nk-member-row ::slotted([slot=\"avatar\"]){width:28px;height:28px;border-radius:50%;color:var(--nk-on-accent);display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:600;flex-shrink:0}.nk-member-row>:has(>.m-mail){min-width:0}.nk-member-row .m-mail{font-size:12px;color:var(--nk-text-secondary);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.nk-member-row .nk-select{min-width:120px;margin-left:auto}.nk-member-row ::slotted([slot=\"role\"]){margin-left:auto}.nk-modal-backdrop{position:fixed;inset:0;background:var(--nk-scrim);padding:env(safe-area-inset-top,0px) env(safe-area-inset-right,0px) env(safe-area-inset-bottom,0px) env(safe-area-inset-left,0px);display:flex;align-items:center;justify-content:center;z-index:100;opacity:0;pointer-events:none;transition:opacity .2s ease}.nk-modal-backdrop.open{opacity:1;pointer-events:auto}.nk-modal{width:min(1150px,92vw);height:min(715px,86vh);max-width:100%;max-height:100%;background:var(--nk-bg);border-radius:10px;box-shadow:var(--nk-shadow-menu);display:flex;overflow:hidden;transform:scale(.98) translateY(6px);transition:transform .2s ease}.nk-modal-backdrop.open .nk-modal{transform:scale(1) translateY(0)}.nk-settings-nav{width:240px;min-width:240px;background:var(--nk-bg-sidebar);border-right:1px solid var(--nk-border);padding:10px 8px;overflow-y:auto;flex-shrink:0}.nk-settings-user{display:flex;align-items:center;gap:9px;padding:6px 10px 12px;margin-bottom:4px}.nk-settings-user .avatar,.nk-settings-user ::slotted(.avatar),.nk-settings-user ::slotted([slot=\"avatar\"]){width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,var(--nk-decor-purple),var(--nk-decor-blue));color:var(--nk-on-accent);font-weight:600;font-size:13px;display:flex;align-items:center;justify-content:center}.nk-settings-user .name{font-weight:500;font-size:14px}.nk-settings-user .mail{font-size:12px;color:var(--nk-text-secondary)}.nk-settings-nav .nk-tree-item{min-height:28px}.nk-settings-nav .nk-tree-item.active{background:var(--nk-bg-active);color:var(--nk-text);font-weight:500}.nk-settings-content{flex:1;overflow-y:auto;padding:36px 60px 80px;min-width:0}.nk-settings-content::-webkit-scrollbar{width:10px}.nk-settings-content::-webkit-scrollbar-thumb{background:var(--nk-border-strong);border-radius:5px;border:2px solid var(--nk-bg)}.nk-settings-pane{display:none;max-width:800px}.nk-settings-pane.active{display:block}.nk-settings-pane>h2{font-size:22px;font-weight:600;line-height:1.3;margin-bottom:20px}.nk-settings-pane>h3{font-size:16px;font-weight:500;padding-bottom:8px;border-bottom:1px solid var(--nk-border);margin:30px 0 12px}.nk-settings-pane ::slotted(h2){font-size:22px;font-weight:600;line-height:1.3;padding:0!important;margin:0 0 20px!important}.nk-settings-pane ::slotted(h3){font-size:16px;font-weight:500;border:0;border-bottom:1px solid var(--nk-border);padding:0 0 8px!important;margin:30px 0 12px!important}.nk-pop{background:var(--nk-bg);border-radius:10px;box-shadow:var(--nk-shadow-menu);padding:10px;width:296px}.nk-emoji-search{width:100%;font-family:inherit;font-size:14px;color:var(--nk-text);background:var(--nk-bg-input);border:1px solid var(--nk-border);border-radius:var(--nk-radius);padding:6px 10px;outline:none;margin-bottom:8px}.nk-emoji-grid{display:grid;grid-template-columns:repeat(8,1fr);gap:2px}.nk-emoji-grid span,.nk-emoji-grid ::slotted(span){font-size:18px;text-align:center;padding:4px 0;border-radius:5px;cursor:pointer;user-select:none}.nk-emoji-grid span:hover{background:var(--nk-bg-hover)}.nk-emoji-cats{display:flex;gap:2px;margin-top:8px;padding-top:8px;border-top:1px solid var(--nk-border)}.nk-emoji-cats span{font-size:14px;padding:3px 7px;border-radius:5px;cursor:pointer;color:var(--nk-text-tertiary);filter:grayscale(1);opacity:.6}.nk-emoji-cats span.active,.nk-emoji-cats span:hover{filter:none;opacity:1;background:var(--nk-bg-hover)}.nk-menu{padding:5px;width:230px}.nk-menu-item{display:flex;align-items:center;gap:9px;padding:4px 8px;min-height:var(--_nk-row, 28px);border-radius:5px;cursor:pointer;font-size:14px;user-select:none}.nk-menu-item:hover{background:var(--nk-bg-hover)}.nk-menu-item .m-icon,.nk-menu-item ::slotted(.m-icon),.nk-menu-item ::slotted([slot=\"icon\"]){width:17px;text-align:center;font-size:14px}.nk-menu-item .m-shortcut,.nk-menu-item ::slotted(.m-shortcut){margin-left:auto;font-size:12px;color:var(--nk-text-tertiary)}.nk-menu-item.danger{color:var(--nk-danger)}.nk-menu-item>.nk-switch{margin-left:auto}.nk-menu-sep{border-top:1px solid var(--nk-border);margin:5px 4px}.nk-menu-label{font-size:12px;color:var(--nk-text-tertiary);padding:5px 8px 2px}.nk-pop.floating{z-index:60;opacity:0;visibility:hidden;pointer-events:none;transform:translateY(-4px) scale(.98);transform-origin:top;transition:opacity .15s ease,transform .15s ease,visibility 0s linear .15s}.nk-pop.floating.open{opacity:1;visibility:visible;pointer-events:auto;transform:none;transition:opacity .15s ease,transform .15s ease,visibility 0s}.nk-cmdk-backdrop{position:fixed;inset:0;background:var(--nk-scrim-soft);display:flex;justify-content:center;align-items:flex-start;padding:max(14vh,env(safe-area-inset-top,0px)) env(safe-area-inset-right,0px) env(safe-area-inset-bottom,0px) env(safe-area-inset-left,0px);z-index:110;opacity:0;pointer-events:none;transition:opacity .18s ease}.nk-cmdk-backdrop.open{opacity:1;pointer-events:auto}.nk-cmdk{max-width:100%;width:min(560px,92vw);background:var(--nk-bg);border-radius:12px;box-shadow:var(--nk-shadow-menu);overflow:hidden;transform:scale(.98);transition:transform .18s ease}.nk-cmdk-backdrop.open .nk-cmdk{transform:scale(1)}.nk-cmdk-input-row{display:flex;align-items:center;gap:10px;padding:14px 16px;border-bottom:1px solid var(--nk-border)}.nk-cmdk-input-row input{flex:1;border:none;outline:none;background:transparent;font-family:inherit;font-size:16px;color:var(--nk-text)}.nk-cmdk-input-row input::placeholder{color:var(--nk-text-tertiary)}.nk-cmdk-list{max-height:320px;overflow-y:auto;padding:6px}.nk-cmdk-group{font-size:12px;color:var(--nk-text-tertiary);padding:8px 10px 3px}.nk-cmdk-item{display:flex;align-items:center;gap:10px;padding:8px 10px;border-radius:6px;cursor:pointer;font-size:14px;user-select:none}.nk-cmdk-item.selected{background:var(--nk-bg-active)}.nk-cmdk-item .m-icon,.nk-cmdk-item ::slotted(.m-icon),.nk-cmdk-item ::slotted([slot=\"icon\"]){width:17px;text-align:center;font-size:14px}.nk-cmdk-item .m-shortcut,.nk-cmdk-item ::slotted(.m-shortcut){margin-left:auto;font-size:12px;color:var(--nk-text-tertiary)}.nk-cmdk-empty{padding:30px;text-align:center;color:var(--nk-text-tertiary);font-size:13.5px}.nk-cmdk-footer{display:flex;gap:14px;padding:8px 16px;border-top:1px solid var(--nk-border);font-size:11.5px;color:var(--nk-text-tertiary)}.nk-sheet-backdrop{position:fixed;inset:0;z-index:100;display:none;flex-direction:column;align-items:center;justify-content:flex-end;padding-top:max(24px,env(safe-area-inset-top,0px));background:var(--nk-scrim-soft);opacity:0;transition:opacity .2s ease,display .25s allow-discrete}.nk-sheet-backdrop.open{display:flex;opacity:1}@starting-style{.nk-sheet-backdrop.open{opacity:0}}.nk-sheet{--_nk-row: 40px;width:100%;max-width:640px;max-height:100%;overflow-y:auto;overscroll-behavior:contain;padding:6px max(10px,env(safe-area-inset-right,0px)) max(16px,env(safe-area-inset-bottom,0px)) max(10px,env(safe-area-inset-left,0px));background:var(--nk-bg);color:var(--nk-text);border-radius:14px 14px 0 0;box-shadow:var(--nk-shadow-menu);translate:0 100%;transition:translate .25s cubic-bezier(.2,.8,.2,1)}.nk-sheet-backdrop.open .nk-sheet{translate:0 0}@starting-style{.nk-sheet-backdrop.open .nk-sheet{translate:0 100%}}.nk-sheet .sh-grabber,.nk-sheet ::slotted(.sh-grabber){width:36px;height:4px;margin:4px auto 10px;border-radius:2px;background:var(--nk-border-strong)}.nk-sheet .sh-title,.nk-sheet ::slotted(.sh-title){font-size:14px;font-weight:600;text-align:center;padding:0 8px 10px}.nk-dialog-backdrop{position:fixed;inset:0;z-index:105;display:none;align-items:center;justify-content:center;padding:max(16px,env(safe-area-inset-top,0px)) max(16px,env(safe-area-inset-right,0px)) max(16px,env(safe-area-inset-bottom,0px)) max(16px,env(safe-area-inset-left,0px));background:var(--nk-scrim-soft);opacity:0;transition:opacity .2s ease,display .2s allow-discrete}.nk-dialog-backdrop.open{display:flex;opacity:1}@starting-style{.nk-dialog-backdrop.open{opacity:0}}.nk-dialog{width:440px;max-width:100%;max-height:100%;overflow-y:auto;overscroll-behavior:contain;padding:20px 24px;border-radius:10px;outline:none;background:var(--nk-bg);color:var(--nk-text);box-shadow:var(--nk-shadow-menu);scale:.98;translate:0 6px;transition:scale .2s ease,translate .2s ease}.nk-dialog.wide{width:560px}.nk-dialog-backdrop.open .nk-dialog{scale:1;translate:0 0}@starting-style{.nk-dialog-backdrop.open .nk-dialog{scale:.98;translate:0 6px}}.nk-dialog .dl-title{font-size:16px;font-weight:600;line-height:1.4;margin-bottom:6px}.nk-dialog .dl-body{font-size:14px;line-height:1.5}.nk-dialog .dl-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:20px}.nk-peek-backdrop{position:fixed;inset:0;z-index:50;display:none;pointer-events:none;transition:background-color .2s ease,display .25s allow-discrete}.nk-peek-backdrop.open{display:block}.nk-peek{--_nk-title: 32px;position:absolute;top:0;right:0;bottom:0;width:min(var(--nk-peek-width),100%);display:flex;flex-direction:column;pointer-events:auto;outline:none;background:var(--nk-bg);color:var(--nk-text);box-shadow:var(--nk-shadow-menu);translate:100% 0;transition:translate .25s cubic-bezier(.2,.8,.2,1)}.nk-peek-backdrop.open .nk-peek{translate:0 0}@starting-style{.nk-peek-backdrop.open .nk-peek{translate:100% 0}}.nk-peek .pk-bar{flex:none;display:flex;align-items:center;gap:2px;height:44px;padding:0 12px}.nk-peek .pk-body{flex:1 1 auto;min-height:0;overflow-y:auto;overscroll-behavior:contain;padding:4px 48px 64px;font-size:16px;line-height:1.5}.nk-peek .pk-resize{position:absolute;top:0;bottom:0;left:-4px;width:8px;z-index:1;cursor:col-resize;touch-action:none;outline:none}.nk-peek .pk-resize:after{content:\"\";position:absolute;top:0;bottom:0;left:3px;width:2px;transition:background .15s}.nk-peek .pk-resize:hover:after,.nk-peek .pk-resize:focus-visible:after,.nk-peek .pk-resize.active:after{background:var(--nk-accent)}.nk-app.peek-inset .nk-main{padding-right:min(var(--nk-peek-width),100%)}.nk-toast{position:fixed;bottom:max(20px,calc(env(safe-area-inset-bottom,0px) + 12px));left:50%;transform:translate(-50%) translateY(80px);z-index:120;width:max-content;max-width:calc(100% - 32px);background:var(--nk-text);color:var(--nk-bg);padding:8px 16px;border-radius:8px;font-size:14px;opacity:0;transition:all .3s ease;pointer-events:none;box-shadow:var(--nk-shadow-menu);display:flex;gap:8px;align-items:center}.nk-toast.show{opacity:1;transform:translate(-50%) translateY(0)}.nk-tooltip{z-index:130;max-width:260px;padding:4px 8px;border-radius:var(--nk-radius);background:var(--nk-text);color:var(--nk-bg);box-shadow:var(--nk-shadow-menu);font-size:12px;font-weight:500;line-height:16px;pointer-events:none;opacity:0;visibility:hidden;transition:opacity .1s ease,visibility 0s linear .1s}.nk-tooltip.open{opacity:1;visibility:visible;transition:opacity .1s ease}.nk-tooltip .tt-key{margin-left:6px;color:color-mix(in srgb,var(--nk-bg) 60%,transparent)}.nk-gallery-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(280px,100%),1fr));gap:20px 32px;margin:12px 0}.nk-g-item>h4{font-size:12px;font-weight:500;color:var(--nk-text-tertiary);text-transform:uppercase;letter-spacing:.04em;margin-bottom:8px}.nk-g-item ::slotted(h4){font-size:12px;font-weight:500;color:var(--nk-text-tertiary);text-transform:uppercase;letter-spacing:.04em;margin-bottom:8px!important}.nk-tabs{display:flex;gap:2px;border-bottom:1px solid var(--nk-border);font-size:14px}.nk-tab{padding:7px 11px;cursor:pointer;color:var(--nk-text-secondary);border-bottom:2px solid transparent;margin-bottom:-1px;user-select:none}.nk-tab:hover{color:var(--nk-text)}.nk-tab.active{color:var(--nk-text);border-bottom-color:var(--nk-text);font-weight:500}.nk-tab-panel{padding:12px 2px;color:var(--nk-text-secondary);line-height:1.55;font-size:14px}.nk-template-btn{display:flex;align-items:center;gap:8px;width:100%;font-family:inherit;font-size:14px;color:var(--nk-text-secondary);background:var(--nk-bg-callout);border:none;border-radius:var(--nk-radius);padding:8px 12px;cursor:pointer;margin-bottom:6px;text-align:left}.nk-template-btn:hover{background:var(--nk-bg-hover)}.nk-stats{display:flex;gap:10px}.nk-stat{flex:1;border:1px solid var(--nk-border);border-radius:8px;padding:12px 14px}.nk-stat .s-label,.nk-stat ::slotted(.s-label),.nk-stat ::slotted([slot=\"label\"]){font-size:12px;color:var(--nk-text-secondary);margin-bottom:4px}.nk-stat .s-value,.nk-stat ::slotted(.s-value),.nk-stat ::slotted([slot=\"value\"]){font-size:22px;font-weight:600;letter-spacing:-.01em}.nk-stat .s-delta,.nk-stat ::slotted(.s-delta),.nk-stat ::slotted([slot=\"delta\"]){font-size:12px;margin-top:3px}.nk-stat .s-delta.up,.nk-stat ::slotted(.s-delta.up),.nk-stat ::slotted([slot=\"delta\"].up){color:var(--nk-color-green)}.nk-stat .s-delta.down,.nk-stat ::slotted(.s-delta.down),.nk-stat ::slotted([slot=\"delta\"].down){color:var(--nk-danger)}.nk-synced{border:1.5px solid color-mix(in srgb,var(--nk-danger) 55%,transparent);border-radius:8px;padding:12px 14px;position:relative;margin:8px 0}.nk-synced .synced-badge{position:absolute;top:-9px;right:10px;font-size:11px;background:var(--nk-danger);color:var(--nk-on-accent);border-radius:8px;padding:1px 8px}.nk-segmented{display:inline-flex;background:var(--nk-bg-callout);border-radius:7px;padding:2px}.nk-segmented button{font-family:inherit;font-size:14px;border:none;background:transparent;color:var(--nk-text-secondary);padding:4px 12px;border-radius:5px;cursor:pointer}.nk-segmented ::slotted(button){font-family:inherit;font-size:14px;border:none;background:transparent;color:var(--nk-text-secondary);padding:4px 12px!important;border-radius:5px;cursor:pointer}.nk-segmented button.active,.nk-segmented ::slotted(button.active){background:var(--nk-bg);color:var(--nk-text);font-weight:500;box-shadow:var(--nk-shadow-segment)}.nk-segmented.wrap{flex-wrap:wrap}.nk-segmented.scroll{max-width:100%;overflow-x:auto;scrollbar-width:none;-webkit-overflow-scrolling:touch}.nk-segmented.scroll::-webkit-scrollbar{display:none}.nk-segmented.scroll button,.nk-segmented.scroll ::slotted(button){flex-shrink:0;white-space:nowrap}.nk-banner{display:flex;align-items:center;gap:10px;font-size:14px;border-radius:8px;padding:10px 14px;margin-bottom:8px;color:var(--nk-text)}.nk-banner.info{background:var(--nk-tint-blue)}.nk-banner.success{background:var(--nk-tint-green)}.nk-banner.warning{background:var(--nk-tint-yellow)}.nk-banner.danger{background:var(--nk-tint-red)}.nk-banner .b-action,.nk-banner ::slotted(.b-action),.nk-banner ::slotted([slot=\"action\"]){margin-left:auto;font-weight:500;cursor:pointer;text-decoration:underline;text-underline-offset:2px}.nk-avatar-group{display:flex}.nk-avatar-group .mini-avatar,.nk-avatar-group .nk-avatar,.nk-avatar-group ::slotted(.mini-avatar),.nk-avatar-group ::slotted(.nk-avatar){width:26px;height:26px;border-radius:50%;color:var(--nk-on-accent);font-size:10.5px;font-weight:600;border:2px solid var(--nk-bg);display:flex;align-items:center;justify-content:center;margin-left:-7px}.nk-avatar-group .mini-avatar:first-child,.nk-avatar-group .nk-avatar:first-child,.nk-avatar-group ::slotted(.mini-avatar:first-child),.nk-avatar-group ::slotted(.nk-avatar:first-child){margin-left:0}.nk-avatar-group .more,.nk-avatar-group ::slotted(.more){background:var(--nk-bg-callout);color:var(--nk-text-secondary)}.nk-avatar{display:inline-flex;align-items:center;justify-content:center;flex-shrink:0;width:24px;height:24px;border-radius:50%;overflow:hidden;vertical-align:middle;font-size:10px;font-weight:600;line-height:1;color:var(--nk-on-accent);user-select:none;background:linear-gradient(135deg,var(--nk-decor-purple),var(--nk-decor-blue));object-fit:cover}.nk-avatar.small{width:20px;height:20px;font-size:9px}.nk-avatar.large{width:32px;height:32px;font-size:12px}.nk-avatar.xlarge{width:56px;height:56px;font-size:20px}.nk-avatar.square{border-radius:4px}.nk-avatar>img,.nk-avatar ::slotted(img){display:block;width:100%;height:100%;object-fit:cover}.nk-avatar.gray,.nk-calendar .cal-marks i.gray{background:var(--nk-color-gray)}.nk-avatar.brown,.nk-calendar .cal-marks i.brown{background:var(--nk-color-brown)}.nk-avatar.orange,.nk-calendar .cal-marks i.orange{background:var(--nk-color-orange)}.nk-avatar.yellow,.nk-calendar .cal-marks i.yellow{background:var(--nk-color-yellow)}.nk-avatar.green,.nk-calendar .cal-marks i.green{background:var(--nk-color-green)}.nk-avatar.blue,.nk-calendar .cal-marks i.blue{background:var(--nk-color-blue)}.nk-avatar.purple,.nk-calendar .cal-marks i.purple{background:var(--nk-color-purple)}.nk-avatar.pink,.nk-calendar .cal-marks i.pink{background:var(--nk-color-pink)}.nk-avatar.red,.nk-calendar .cal-marks i.red{background:var(--nk-color-red)}.nk-panels{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(200px,100%),1fr));gap:12px;margin:12px 0}.nk-panel{display:flex;flex-direction:column;gap:6px;min-width:0;padding:14px 16px;border:1px solid var(--nk-border);border-radius:10px;background:var(--nk-bg-card);box-shadow:var(--nk-shadow-card);font-size:14px;line-height:1.5;color:inherit;text-decoration:none;text-align:left}:is(a,button).nk-panel{cursor:pointer;font-family:inherit;transition:border-color .15s,background .15s}:is(a,button).nk-panel:hover{border-color:var(--nk-border-strong)}.nk-panel>h3{font-size:14px;font-weight:600;line-height:1.4}.nk-panel ::slotted(h3){font-size:14px;font-weight:600;line-height:1.4;margin:0!important}.nk-panel>p{font-size:13px;color:var(--nk-text-secondary)}.nk-panel ::slotted(p){font-size:13px;color:var(--nk-text-secondary);margin:0!important}.nk-panel>.nk-cover{flex:none;height:64px;margin:-14px -16px 2px;border-radius:9px 9px 0 0}.nk-panel>.nk-page-icon{font-size:28px}.nk-panel>.nk-cover+.nk-page-icon{margin-top:-24px}.nk-skeleton{border-radius:5px;margin-bottom:10px;background:linear-gradient(90deg,var(--nk-bg-callout) 25%,var(--nk-bg-hover) 50%,var(--nk-bg-callout) 75%);background-size:200% 100%;animation:nk-shimmer 1.6s infinite}@keyframes nk-shimmer{to{background-position:-200% 0}}.nk-empty{text-align:center;padding:36px 20px;border:1.5px dashed var(--nk-border-strong);border-radius:10px;color:var(--nk-text-tertiary)}.nk-empty .e-icon,.nk-empty ::slotted(.e-icon),.nk-empty ::slotted([slot=\"icon\"]){font-size:30px;margin-bottom:8px}.nk-empty .e-title,.nk-empty ::slotted(.e-title),.nk-empty ::slotted([slot=\"title\"]){font-weight:500;color:var(--nk-text-secondary);margin-bottom:3px;font-size:14px}.nk-empty .e-desc,.nk-empty ::slotted(.e-desc),.nk-empty ::slotted([slot=\"desc\"]){font-size:13px;margin-bottom:12px}.nk-empty .e-actions,.nk-empty ::slotted(.e-actions){display:flex;flex-wrap:wrap;justify-content:center;gap:8px}.nk-steps{display:flex;flex-direction:column;margin:8px 0;padding:0;list-style:none;font-size:14px}.nk-step{position:relative;display:flex;align-items:flex-start;gap:10px;padding-bottom:14px;line-height:20px;color:var(--nk-text-tertiary)}.nk-step:last-child{padding-bottom:0}.nk-step:not(:last-child):after{content:\"\";position:absolute;left:10px;top:24px;bottom:4px;width:1px;background:var(--nk-border)}.nk-step .st-mark{flex:none;width:20px;height:20px;border-radius:50%;display:inline-flex;align-items:center;justify-content:center;border:1px solid var(--nk-border-strong);background:var(--nk-bg);font-size:11px;font-weight:600;line-height:1}.nk-step .st-desc{display:block;font-size:12px;line-height:18px;color:var(--nk-text-tertiary);font-weight:400}.nk-step.done{color:var(--nk-text-secondary)}.nk-step.done .st-mark{background:var(--nk-tag-green-bg);color:var(--nk-tag-green-text);border-color:transparent}.nk-step.current{color:var(--nk-text);font-weight:500}.nk-step.current .st-mark{border-color:var(--nk-accent);color:var(--nk-accent)}.nk-comments{border-left:1px solid var(--nk-border);padding:4px 0 4px 18px;margin:8px 0 8px 8px}.nk-comment{display:flex;gap:9px;margin-bottom:14px}.nk-comment .mini-avatar,.nk-comment ::slotted(.mini-avatar),.nk-comment ::slotted([slot=\"avatar\"]){width:24px;height:24px;border-radius:50%;color:var(--nk-on-accent);flex-shrink:0;font-size:10px;font-weight:600;display:flex;align-items:center;justify-content:center}.nk-comment .c-head,.nk-comment ::slotted(.c-head){font-size:12px;color:var(--nk-text-tertiary);margin-bottom:2px}.nk-comment .c-head b{color:var(--nk-text);font-weight:600}.nk-comment .c-body,.nk-comment ::slotted(.c-body){font-size:14px;line-height:1.5}.nk-comment-input{display:flex;gap:8px;align-items:center;margin-top:4px}.nk-comment-input input{min-width:0;flex:1}.nk-ai-thread{display:flex;flex-direction:column;gap:14px}.nk-ai-msg{display:flex;gap:9px}.nk-ai-msg .mini-avatar,.nk-ai-msg ::slotted(.mini-avatar),.nk-ai-msg ::slotted([slot=\"avatar\"]){width:24px;height:24px;border-radius:50%;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:12px;background:var(--nk-bg-callout)}.nk-ai-msg.user .mini-avatar,.nk-ai-msg.user ::slotted(.mini-avatar),.nk-ai-msg.user ::slotted([slot=\"avatar\"]){background:linear-gradient(135deg,var(--nk-decor-purple),var(--nk-decor-blue));color:var(--nk-on-accent);font-size:9.5px;font-weight:600}.nk-ai-msg .a-body,.nk-ai-msg ::slotted(.a-body){font-size:14px;line-height:1.55;padding-top:2px}.nk-ai-msg .a-name,.nk-ai-msg ::slotted(.a-name){font-size:12px;font-weight:600;margin-bottom:2px}.nk-ai-msg .a-name span{color:var(--nk-text-tertiary);font-weight:400}.nk-ai-msg.user.bubble{justify-content:flex-end}.nk-ai-msg.bubble .a-body,.nk-ai-msg.bubble ::slotted(.a-body){max-width:85%;padding:6px 12px;border-radius:16px;background:var(--nk-bg-callout)}.nk-ai-actions{display:flex;gap:4px;margin-top:6px}.nk-ai-actions button{font-family:inherit;font-size:12px;color:var(--nk-text-secondary);background:transparent;border:1px solid var(--nk-border);border-radius:5px;padding:2px 8px;cursor:pointer}.nk-ai-actions ::slotted(button){font-family:inherit;font-size:12px;color:var(--nk-text-secondary);background:transparent;border:1px solid var(--nk-border);border-radius:5px;padding:2px 8px!important;cursor:pointer}.nk-ai-actions button:hover,.nk-ai-actions ::slotted(button:hover){background:var(--nk-bg-hover);color:var(--nk-text)}.nk-ai-input-row{display:flex;align-items:center;gap:8px;margin-top:14px;border:1px solid var(--nk-border-strong);border-radius:10px;padding:8px 12px}.nk-ai-input-row:focus-within{border-color:var(--nk-accent)}.nk-ai-input-row input{flex:1;border:none;outline:none;background:transparent;font-family:inherit;font-size:14px;color:var(--nk-text);min-width:0}.nk-ai-input-row input::placeholder{color:var(--nk-text-tertiary)}.nk-ai-send{border:none;background:var(--nk-accent);color:var(--nk-on-accent);border-radius:6px;width:26px;height:26px;cursor:pointer;font-size:13px;flex-shrink:0}.nk-block-host{position:relative;border-radius:var(--nk-radius);margin:8px 0;padding:2px 0 2px 2px;transition:background .15s,box-shadow .15s}.nk-block-host:hover{background:var(--nk-bg-hover)}.nk-block-host:focus-within{background:transparent;box-shadow:0 0 0 2px color-mix(in srgb,var(--nk-accent) 25%,transparent)}.nk-block-host.nk-drop-target{box-shadow:0 -2px 0 0 var(--nk-accent);background:transparent}.nk-block-host .nk-block-handle,.nk-block-host ::slotted(.nk-block-handle){position:absolute;left:-26px;top:4px;width:18px;height:20px;border-radius:4px;display:flex;align-items:center;justify-content:center;color:var(--nk-text-tertiary);font-size:13px;cursor:grab;opacity:0;transition:opacity .15s;user-select:none}.nk-block-host:hover .nk-block-handle{opacity:1}.nk-block-host .nk-block-actions{position:absolute;display:flex;gap:1px;z-index:2;opacity:0;transition:opacity .15s;pointer-events:none}.nk-block-host .nk-block-actions.show{opacity:1;pointer-events:auto}.nk-block-host .nk-block-actions button,.nk-block-host .nk-block-actions ::slotted(button){width:20px;height:22px;padding:0;border:none;border-radius:4px;background:transparent;color:var(--nk-text-tertiary);font-family:inherit;font-size:14px;line-height:1;cursor:pointer;display:flex;align-items:center;justify-content:center}.nk-block-host .nk-block-actions button:hover{background:var(--nk-bg-hover);color:var(--nk-text)}.nk-block-host .nk-block-actions .drag{cursor:grab}.nk-block-host .nk-block-actions .drag:active{cursor:grabbing}.nk-prose,.nk-block-host .ProseMirror{font-family:var(--nk-font);line-height:1.5;color:var(--nk-text);overflow-wrap:break-word}.nk-block-host .ProseMirror{outline:none;min-height:60px;padding:4px 2px}.nk-prose>*+*,.nk-block-host .ProseMirror>*+*{margin-top:6px}.nk-prose h1,.nk-block-host .ProseMirror h1{font-size:1.875em;font-weight:600;line-height:1.3;letter-spacing:-.01em;margin:32px 0 4px}.nk-prose h2,.nk-block-host .ProseMirror h2{font-size:1.5em;font-weight:600;line-height:1.3;margin:24px 0 4px}.nk-prose h3,.nk-block-host .ProseMirror h3{font-size:1.25em;font-weight:600;line-height:1.3;margin:16px 0 2px}.nk-prose :is(h4,h5,h6),.nk-block-host .ProseMirror :is(h4,h5,h6){font-size:1em;font-weight:600;margin:12px 0 0}.nk-prose>:first-child,.nk-block-host .ProseMirror>:first-child{margin-top:0}.nk-prose>:last-child,.nk-block-host .ProseMirror>:last-child{margin-bottom:0}.nk-prose p:empty:before{content:\"\";display:inline-block}.nk-prose :is(strong,b),.nk-block-host .ProseMirror :is(strong,b){font-weight:600}.nk-prose :is(ul,ol),.nk-block-host .ProseMirror :is(ul,ol){padding-left:24px;margin:4px 0}.nk-prose ul,.nk-block-host .ProseMirror ul{list-style:disc}.nk-prose ol,.nk-block-host .ProseMirror ol{list-style:decimal}.nk-prose li,.nk-block-host .ProseMirror li{margin:2px 0}.nk-prose li::marker,.nk-block-host .ProseMirror li::marker{color:var(--nk-text-tertiary)}.nk-prose ul[data-type=taskList],.nk-block-host .ProseMirror ul[data-type=taskList]{list-style:none;padding-left:2px}.nk-prose ul[data-type=taskList] li,.nk-block-host .ProseMirror ul[data-type=taskList] li{display:flex;gap:8px;align-items:flex-start}.nk-prose ul[data-type=taskList] li>label,.nk-block-host .ProseMirror ul[data-type=taskList] li>label{flex-shrink:0;margin-top:max(0px,calc((1lh - 16px)/2));user-select:none}.nk-prose ul[data-type=taskList] li>div,.nk-block-host .ProseMirror ul[data-type=taskList] li>div{flex:1;min-width:0}.nk-prose ul[data-type=taskList] input[type=checkbox],.nk-block-host .ProseMirror ul[data-type=taskList] input[type=checkbox]{appearance:none;width:16px;height:16px;margin:0;border:1.4px solid var(--nk-border-strong);border-radius:2px;cursor:pointer;position:relative;background:var(--nk-bg);display:block}.nk-prose ul[data-type=taskList] input[type=checkbox]:checked,.nk-block-host .ProseMirror ul[data-type=taskList] input[type=checkbox]:checked{background:var(--nk-accent);border-color:var(--nk-accent)}.nk-prose ul[data-type=taskList] input[type=checkbox]:checked:after,.nk-block-host .ProseMirror ul[data-type=taskList] input[type=checkbox]:checked:after{content:\"\\2713\";color:var(--nk-on-accent);font-size:11px;position:absolute;inset:0;display:flex;align-items:center;justify-content:center}.nk-prose li[data-checked=true]>div,.nk-block-host .ProseMirror li[data-checked=true]>div{text-decoration:line-through;color:var(--nk-text-tertiary)}.nk-prose blockquote,.nk-block-host .ProseMirror blockquote{border-left:3px solid var(--nk-text);padding:2px 0 2px 14px;margin:12px 0;color:var(--nk-text-secondary)}.nk-prose code,.nk-block-host .ProseMirror code{font-family:var(--nk-font-mono);font-size:85%;color:var(--nk-danger);background:color-mix(in srgb,var(--nk-danger) 8%,transparent);border-radius:4px;padding:1px 5px}.nk-prose pre,.nk-block-host .ProseMirror pre{background:var(--nk-bg-code);border:1px solid var(--nk-border);border-radius:var(--nk-radius);padding:16px 20px;margin:12px 0;font-family:var(--nk-font-mono);font-size:13px;line-height:1.6;color:var(--nk-text-secondary);overflow-x:auto}.nk-prose pre code,.nk-block-host .ProseMirror pre code{background:none;color:inherit;padding:0;font-size:inherit}.nk-prose hr,.nk-block-host .ProseMirror hr{border:none;border-top:1px solid var(--nk-border);margin:20px 0}.nk-prose a,.nk-block-host .ProseMirror a{color:var(--nk-accent);text-decoration:underline;text-underline-offset:2px}.nk-prose img{max-width:100%;height:auto}.nk-prose table{display:block;width:max-content;max-width:100%;overflow-x:auto;border-collapse:collapse;font-size:14px;margin:12px 0}.nk-prose :is(th,td){padding:6px 10px;border:1px solid var(--nk-border);text-align:left;vertical-align:top}.nk-prose th{font-weight:600}.nk-block-host .ProseMirror-gapcursor:after{border-top-color:var(--nk-text)}.nk-block-host .ProseMirror .ProseMirror-selectednode{background:color-mix(in srgb,var(--nk-accent) 8%,transparent)}.nk-block-host .ProseMirror-selectednode{box-shadow:0 0 0 2px color-mix(in srgb,var(--nk-accent) 35%,transparent);border-radius:3px}.nk-block-host .ProseMirror ::selection{background:color-mix(in srgb,var(--nk-accent) 25%,transparent)}.nk-block-host .ProseMirror p.is-editor-empty:first-child:before,.nk-block-host .ProseMirror .is-empty:before{content:attr(data-placeholder);color:var(--nk-text-tertiary);float:left;height:0;pointer-events:none}.nk-block-host .bn-container{font-family:var(--nk-font);color:var(--nk-text);background:transparent}.nk-block-host .bn-editor{padding:4px 2px}.nk-block-host .bn-block-content{font-size:14px;line-height:1.6}.nk-block-host .bn-side-menu{color:var(--nk-text-tertiary)}.nk-block-host .bn-drag-handle{border-radius:4px}.nk-block-host .bn-drag-handle:hover{background:var(--nk-bg-hover)}.nk-block-host .novel-editor{font-family:var(--nk-font);color:var(--nk-text)}.nk-block-host .novel-prose{max-width:none}.nk-slash-menu,.nk-block-host .bn-suggestion-menu,.nk-block-host .tippy-box{background:var(--nk-bg);border-radius:10px;box-shadow:var(--nk-shadow-menu);padding:6px;width:280px;max-height:320px;overflow-y:auto;font-family:var(--nk-font);font-size:14px;color:var(--nk-text)}.nk-slash-menu-label{font-size:12px;color:var(--nk-text-tertiary);padding:6px 9px 2px}.nk-slash-item{display:flex;align-items:center;gap:10px;padding:6px 9px;border-radius:5px;cursor:pointer;user-select:none}.nk-slash-item:hover,.nk-slash-item.selected{background:var(--nk-bg-hover)}.nk-slash-item .m-icon,.nk-slash-item ::slotted(.m-icon){width:28px;height:28px;flex-shrink:0;border-radius:4px;background:var(--nk-bg-callout);border:1px solid var(--nk-border);display:flex;align-items:center;justify-content:center;font-size:15px}.nk-slash-item .m-desc{font-size:12px;color:var(--nk-text-tertiary)}.nk-bubble-menu{display:flex;align-items:center;gap:2px;background:var(--nk-bg);border-radius:8px;box-shadow:var(--nk-shadow-menu);padding:4px}.nk-bubble-menu button{font-family:inherit;font-size:13px;border:none;background:transparent;color:var(--nk-text-secondary);padding:4px 8px;border-radius:5px;cursor:pointer}.nk-bubble-menu ::slotted(button){font-family:inherit;font-size:13px;border:none;background:transparent;color:var(--nk-text-secondary);padding:4px 8px!important;border-radius:5px;cursor:pointer}.nk-bubble-menu button:hover,.nk-bubble-menu ::slotted(button:hover){background:var(--nk-bg-hover);color:var(--nk-text)}.nk-bubble-menu button.active,.nk-bubble-menu ::slotted(button.active){color:var(--nk-accent)}@media(max-width:860px){.nk-sidebar{display:none;position:fixed;inset:0 auto 0 0;z-index:60;transform:translate(-100%);transition:background .25s ease,transform .24s cubic-bezier(.2,.8,.25,1),display .24s allow-discrete}.nk-sidebar.open{display:flex;transform:none}@starting-style{.nk-sidebar.open{transform:translate(-100%)}}.nk-sidebar-backdrop{opacity:0;transition:opacity .24s ease,display .24s allow-discrete}.nk-sidebar-backdrop.open{display:block;opacity:1}@starting-style{.nk-sidebar-backdrop.open{opacity:0}}.nk-sidebar-toggle{display:inline-flex}.nk-sidebar.collapsed{margin-left:0;visibility:visible;transition:background .25s ease,transform .24s cubic-bezier(.2,.8,.25,1),display .24s allow-discrete}.nk-sidebar-collapse{display:none}.nk-tab-bar{display:flex}.nk-tab-bar.fixed+.nk-tab-bar-spacer{display:block}.nk-page{padding-left:max(24px,env(safe-area-inset-left,0px));padding-right:max(24px,env(safe-area-inset-right,0px));padding-bottom:80px}.nk-settings-nav{width:60px;min-width:60px}.nk-settings-nav .label,.nk-settings-nav .nk-section-label,.nk-settings-user .u-text{display:none}.nk-settings-content{padding:24px}.nk-cmdk-backdrop{padding-top:max(6vh,env(safe-area-inset-top,0px))}.nk-cmdk{width:min(560px,96vw)}.nk-stats{flex-wrap:wrap}.nk-breadcrumb{min-width:0}.nk-breadcrumb>.crumb:not(:last-child),.nk-breadcrumb ::slotted(.crumb:not(:last-child)),.nk-breadcrumb>.sep,.nk-breadcrumb ::slotted(.sep),.nk-topbar-meta,.nk-topbar-actions ::slotted(.nk-topbar-meta){display:none}.nk-breadcrumb>.crumb:last-child,.nk-breadcrumb ::slotted(.crumb:last-child){display:block;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.nk-topbar-actions{flex-shrink:0}.nk-field{flex-wrap:wrap;row-gap:6px}.nk-field .f-control,.nk-field ::slotted(.f-control){max-width:100%;min-width:0}.nk-input,.nk-select,.nk-textarea,.nk-slider{max-width:100%}.nk-prop{flex-direction:column;align-items:stretch}.nk-prop .p-name{flex:none;min-height:26px}.nk-prop .p-value{min-height:30px;padding:3px 6px 7px}.nk-pop.sheet{position:fixed!important;inset:auto 0 0!important;width:auto!important;transform:none!important;z-index:100;--_nk-row: 40px;max-height:85dvh;overflow-y:auto;overscroll-behavior:contain;border-radius:14px 14px 0 0;padding:6px max(8px,env(safe-area-inset-right,0px)) max(12px,env(safe-area-inset-bottom,0px)) max(8px,env(safe-area-inset-left,0px));box-shadow:0 0 0 100vmax var(--nk-scrim-soft),var(--nk-shadow-menu)}.nk-pop.sheet:before{content:\"\";display:block;width:36px;height:4px;margin:4px auto 8px;border-radius:2px;background:var(--nk-border-strong)}.nk-pop.sheet.floating{translate:0 100%;transition:opacity .2s ease,translate .25s cubic-bezier(.2,.8,.2,1),visibility 0s linear .25s}.nk-pop.sheet.floating.open{translate:0 0;transition:opacity .2s ease,translate .25s cubic-bezier(.2,.8,.2,1),visibility 0s}.nk-pop.sheet,.nk-sheet{--_nk-cell: 44px}.nk-calendar-view .cv-day{min-height:64px;padding:2px 2px 4px}.nk-calendar-view .cv-item{padding:1px 4px;font-size:11px;line-height:16px}.nk-peek-backdrop{pointer-events:auto}.nk-peek-backdrop.open{background-color:var(--nk-scrim-soft)}@starting-style{.nk-peek-backdrop.open{background-color:transparent}}.nk-peek{--_nk-title: 28px;top:auto;left:0;width:auto;max-height:92dvh;border-radius:14px 14px 0 0;translate:0 100%}.nk-peek-backdrop.open .nk-peek{translate:0 0}@starting-style{.nk-peek-backdrop.open .nk-peek{translate:0 100%}}.nk-peek:before{content:\"\";flex:none;width:36px;height:4px;margin:10px auto 0;border-radius:2px;background:var(--nk-border-strong)}.nk-peek .pk-bar{height:40px;padding:0 max(8px,env(safe-area-inset-right,0px)) 0 max(8px,env(safe-area-inset-left,0px))}.nk-peek .pk-body{padding:0 max(20px,env(safe-area-inset-right,0px)) max(24px,env(safe-area-inset-bottom,0px)) max(20px,env(safe-area-inset-left,0px))}.nk-peek .pk-resize{display:none}.nk-app.peek-inset .nk-main{padding-right:0}.nk-dialog-backdrop{flex-direction:column;justify-content:flex-end;padding:max(24px,env(safe-area-inset-top,0px)) 0 0}.nk-dialog{width:100%;max-width:640px;border-radius:14px 14px 0 0;scale:none;translate:0 100%;padding:6px max(20px,env(safe-area-inset-right,0px)) max(16px,env(safe-area-inset-bottom,0px)) max(20px,env(safe-area-inset-left,0px));transition:translate .25s cubic-bezier(.2,.8,.2,1)}.nk-dialog.wide{width:100%}.nk-dialog-backdrop.open .nk-dialog{translate:0 0}@starting-style{.nk-dialog-backdrop.open .nk-dialog{translate:0 100%}}.nk-dialog:before{content:\"\";display:block;width:36px;height:4px;margin:4px auto 14px;border-radius:2px;background:var(--nk-border-strong)}.nk-dialog .dl-actions{flex-direction:column-reverse}:root:has(.nk-tab-bar:not([hidden])) .nk-toast{bottom:calc(var(--nk-tab-bar-height) + max(6px,env(safe-area-inset-bottom,0px)) + 6px)}:root:has(.nk-tab-bar.floating:not([hidden])) .nk-toast{bottom:calc(var(--nk-tab-bar-height) + 24px + env(safe-area-inset-bottom,0px))}}@media(prefers-reduced-motion:reduce){.nk-skeleton{animation:none;background:var(--nk-bg-callout)}[class*=nk-],[class*=nk-]:before,[class*=nk-]:after{animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important;scroll-behavior:auto!important}}\n/*# sourceMappingURL=notionkit.min.css.map */\n";

  const componentsCss = componentsHead + componentsTail;
  const css = componentsHead + tokensCss + componentsTail;

  const nkSheet = new CSSStyleSheet();
  nkSheet.replaceSync(css);

  const tokensSheet = new CSSStyleSheet();
  tokensSheet.replaceSync(tokensCss);

  const componentsSheet = new CSSStyleSheet();
  componentsSheet.replaceSync(componentsCss);

  // ============================================================
  // NotionKit Elements – base classes
  //
  // Every <nk-*> element is a thin shell around the markup notionkit.css already
  // styles. The shadow root adopts the *components* half of that stylesheet and
  // nothing else; the element ships no visual CSS of its own.
  // ============================================================

  // ── Design tokens ──
  // The shadow roots deliberately adopt componentsSheet only. The full sheet
  // would bring the :root / [data-theme] token blocks along, and those match the
  // .nk-wrapper below — every --nk-* token would then be re-declared inside each
  // shadow root, where a matching rule always beats an inherited value. A page's
  // own `:root { --nk-accent: … }` would never arrive.
  //
  // So the token defaults go on the document once, and every shadow root
  // inherits them like any other custom property. They sit in a cascade layer so
  // an ordinary (unlayered) brand stylesheet — and notionkit.css itself, if the
  // page loads it — wins over them, no matter the load order.

  const TOKENS_INJECTED = '__nkDefaultTokensInjected';

  function injectDefaultTokens() {
    if (typeof document === 'undefined') return;              // SSR / non-DOM
    if (globalThis[TOKENS_INJECTED]) return;                   // another bundle copy did it
    globalThis[TOKENS_INJECTED] = true;

    const sheet = new CSSStyleSheet();
    sheet.replaceSync(`@layer notionkit-defaults { ${tokensCss} }`);
    // Append — never assign — so an app's own adopted sheets survive.
    document.adoptedStyleSheets = [...document.adoptedStyleSheets, sheet];
  }

  injectDefaultTokens();

  // ── Global theme sync ──
  // data-theme on <html> is the only source of truth. One MutationObserver
  // mirrors it onto every registered instance; no element sets a theme itself.

  const instances = new Set();

  function getCurrentTheme() {
    return document.documentElement.getAttribute('data-theme') || 'light';
  }

  function syncAllThemes() {
    const theme = getCurrentTheme();
    for (const instance of instances) instance._syncTheme(theme);
  }

  if (typeof window !== 'undefined' && typeof MutationObserver !== 'undefined') {
    new MutationObserver(syncAllThemes).observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['data-theme']
    });
  }

  // ── Host stylesheets ──
  // Layout plumbing only: how the host box participates in the outer layout, and
  // that `hidden` keeps working. :host([hidden]) has a higher specificity than
  // :host, so it wins regardless of order. The wrapper is layout-transparent.

  const hostSheets = new Map();

  function hostSheetFor(display) {
    let sheet = hostSheets.get(display);
    if (!sheet) {
      sheet = new CSSStyleSheet();
      sheet.replaceSync(`
      :host { display: ${display}; }
      :host([hidden]) { display: none; }
      .nk-wrapper { display: contents; }
    `);
      hostSheets.set(display, sheet);
    }
    return sheet;
  }

  // ── Base class ──

  class NkElement extends HTMLElement {

    /**
     * How the host takes part in the outer layout. Default 'contents': the host
     * generates no box, so the inner .nk-* element sits in the parent's layout
     * exactly where the class markup would – a button stays inline, a sidebar
     * is a direct flex child of the app, a tree row and its children box are
     * siblings. That is what makes the pixel parity with the class markup hold.
     * Consequence: style the parent or the tokens, not the host; `hidden` on
     * the host still works. Override with 'block' / 'inline-block' only when a
     * host box is genuinely needed.
     */
    static get display() { return 'contents'; }

    /**
     * Optional CSSStyleSheet with component-specific host rules, adopted after
     * the shared sheets. Positioning and sizing of the host only — never colour,
     * spacing or typography; those belong to notionkit.css.
     */
    static get hostStyles() { return null; }

    /**
     * Opt in when the component *copies* light-DOM children into its shadow tree
     * (options, breadcrumb crumbs). A MutationObserver then calls
     * projectLightDom() again whenever those children change, so a framework
     * that swaps them keeps the rendered element in step. Components that merely
     * *slot* their children use `slotchange` instead.
     */
    static get observesLightDom() { return false; }

    /** What the light-DOM observer watches. Drop `subtree` for shallow copies. */
    static get lightDomObserverInit() {
      return { childList: true, subtree: true, characterData: true, attributes: true };
    }

    /**
     * 'named' (default) or 'manual'. Manual assignment lets a component route
     * light-DOM nodes itself – text nodes into a label slot, nested elements
     * into a children slot – so <nk-tree-item icon="🏠">Home</nk-tree-item>
     * reads naturally. With 'manual' the component calls assignSlots() on
     * every childList change.
     */
    static get slotAssignment() { return 'named'; }

    static get observedAttributes() { return []; }

    constructor() {
      super();
      this._initialized = false;
      this._shadow = this.attachShadow({ mode: 'open', slotAssignment: this.constructor.slotAssignment });
      const sheets = [componentsSheet, hostSheetFor(this.constructor.display)];
      const extra = this.constructor.hostStyles;
      if (extra) sheets.push(extra);
      this._shadow.adoptedStyleSheets = sheets;
    }

    connectedCallback() {
      if (!this._initialized) {
        // A framework may have set properties on the instance before the
        // definition arrived (hybrids binds checked="${bool}" as a property
        // while the element is still a plain HTMLElement). Such an own property
        // shadows the accessor on the prototype for good; re-apply it through
        // the setter so the attribute – and with it render() – sees the value.
        this._upgradeOwnProperties();
        this._initialized = true;

        // The wrapper mirrors data-theme from <html> so [data-theme]-keyed
        // component rules apply inside the root. Token *values* need no
        // mirroring — they inherit.
        this._wrapper = document.createElement('div');
        this._wrapper.className = 'nk-wrapper';
        this._wrapper.setAttribute('data-theme', getCurrentTheme());
        this._shadow.appendChild(this._wrapper);

        this.render();
      } else {
        this._wrapper.setAttribute('data-theme', getCurrentTheme());
      }

      // Everything below runs on every connect, not just the first. Moving an
      // element in the DOM disconnects and reconnects it, and disconnectedCallback
      // tears all of this down — without re-arming it here a moved element would
      // keep its markup but silently stop reacting.
      this.setupEvents();
      instances.add(this);

      if (this.constructor.observesLightDom) {
        this._lightDomObserver ??= new MutationObserver(records => this.projectLightDom(records));
        this._lightDomObserver.observe(this, this.constructor.lightDomObserverInit);
      }
      if (this.constructor.slotAssignment === 'manual') {
        this.assignSlots();
        this._slotObserver ??= new MutationObserver(() => this.assignSlots());
        this._slotObserver.observe(this, { childList: true });
      }
    }

    disconnectedCallback() {
      instances.delete(this);
      this._lightDomObserver?.disconnect();
      this._slotObserver?.disconnect();
      this.teardownEvents();
    }

    attributeChangedCallback(name, oldValue, newValue) {
      if (!this._initialized) return;
      if (oldValue === newValue) return;
      this.onAttributeChanged(name, oldValue, newValue);
    }

    _syncTheme(theme) {
      this._wrapper?.setAttribute('data-theme', theme);
    }

    _upgradeOwnProperties() {
      for (const key of Object.keys(this)) {
        if (key.startsWith('_')) continue;
        let proto = Object.getPrototypeOf(this), desc;
        while (proto && proto !== HTMLElement.prototype && !(desc = Object.getOwnPropertyDescriptor(proto, key))) proto = Object.getPrototypeOf(proto);
        if (!desc?.set) continue;
        const value = this[key];
        delete this[key];
        this[key] = value;
      }
    }

    /** Subclasses override to build the inner DOM inside this._wrapper. Runs once. */
    render() {}

    /** Subclasses override to attach event listeners. Runs on every connect. */
    setupEvents() {}

    /** Subclasses override to remove event listeners. Runs on every disconnect. */
    teardownEvents() {}

    /** Subclasses override to react to attribute changes after render(). */
    onAttributeChanged(name, oldValue, newValue) {}

    /**
     * Subclasses that set observesLightDom override this to (re-)copy their
     * light-DOM children into the shadow tree. Runs on every change to those
     * children, so it has to be safe to call repeatedly.
     */
    projectLightDom() {}

    /** Components with manual slot assignment override this. */
    assignSlots() {}

    /**
     * Escape hatch: re-copy the light-DOM children now. The observer covers the
     * ordinary cases; this is for the ones it cannot see, so nobody has to reach
     * into element.shadowRoot.
     */
    refresh() { this.projectLightDom(); if (this.constructor.slotAssignment === 'manual') this.assignSlots(); }

    // ── Utilities ──

    /**
     * For elements whose heading arrives as `title` – nk-settings-pane,
     * nk-danger-zone, nk-empty, nk-model-card. `title` is also the global HTML
     * attribute, and a title on the host shows as a browser tooltip over the
     * whole element. So the value is read into this._titleText and the
     * attribute is taken off the host; a later setAttribute('title') arrives
     * through attributeChangedCallback and is taken the same way. The removal
     * fires the callback with null, which is ignored – only '' clears the
     * heading. The element's own `title` accessor answers from the stored
     * value, so a framework that binds `title` as a property never puts an
     * attribute on the host at all. Returns whether a value was taken.
     */
    takeTitle(value = this.getAttribute('title')) {
      if (value === null) return false;
      this._titleText = value;
      this.removeAttribute('title');
      return true;
    }

    getBoolAttr(name) {
      return this.hasAttribute(name);
    }

    setBoolAttr(name, value) {
      if (value) this.setAttribute(name, '');
      else this.removeAttribute(name);
    }

    createElement(tag, classes = [], attrs = {}) {
      const el = document.createElement(tag);
      if (classes.length) el.classList.add(...classes);
      for (const [key, val] of Object.entries(attrs)) {
        if (val !== null && val !== undefined && val !== false) el.setAttribute(key, val === true ? '' : val);
      }
      return el;
    }

    /** Dispatches a composed, bubbling CustomEvent — the nk-* event contract. */
    emit(eventName, detail = null) {
      return this.dispatchEvent(new CustomEvent(eventName, {
        bubbles: true,
        composed: true,
        cancelable: true,
        detail
      }));
    }
  }

  // ── Form-associated base class ──
  // ElementInternals gives the element a place in the surrounding <form>:
  // FormData, reset, validation and :disabled through <fieldset>.

  class NkFormElement extends NkElement {

    static formAssociated = true;

    constructor() {
      super();
      this._internals = this.attachInternals();
    }

    get form() { return this._internals.form; }
    get validationMessage() { return this._internals.validationMessage; }
    get validity() { return this._internals.validity; }
    get willValidate() { return this._internals.willValidate; }

    checkValidity() { return this._internals.checkValidity(); }
    reportValidity() { return this._internals.reportValidity(); }

    connectedCallback() {
      const first = !this._initialized;
      super.connectedCallback();
      // A `disabled` attribute in the markup fires formDisabledCallback on
      // parse or upgrade – before the first connect, when nothing is rendered
      // yet. The state was remembered below; apply it now that render() ran.
      if (first && this._formDisabled) this.onFormDisabled(true);
    }

    formResetCallback() { this.resetValue(); }
    formStateRestoreCallback(state, mode) { this.restoreValue(state, mode); }
    formDisabledCallback(disabled) {
      this._formDisabled = disabled;
      if (this._initialized) this.onFormDisabled(disabled);
    }

    /** Subclasses override. */
    resetValue() {}
    restoreValue(state, mode) {}
    onFormDisabled(disabled) {}

    setFormValue(value, state) {
      this._internals.setFormValue(value, state);
    }

    setValidity(flags, message, anchor) {
      this._internals.setValidity(flags, message, anchor);
    }

    /** Mirrors the native control's validity onto the host, so required works. */
    syncValidityFrom(control) {
      if (!control) return;
      if (control.validity.valid) this._internals.setValidity({});
      else this._internals.setValidity(control.validity, control.validationMessage, control);
    }
  }

  // <nk-btn variant="primary" small>Save</nk-btn>   →  <button class="nk-btn primary small">
  // <nk-btn href="/docs" variant="secondary">Docs</nk-btn>  →  <a class="nk-btn secondary">
  // <nk-btn variant="topbar">⭐</nk-btn>  →  <button class="nk-topbar-btn">
  // <nk-btn variant="tool" active>Filter</nk-btn>  →  <button class="nk-db-tool active">  (slot="tools" of nk-database)
  // <nk-btn variant="sidebar" aria-label="Menu">☰</nk-btn>  →  <button class="nk-topbar-btn nk-sidebar-toggle">:
  //   on a phone it opens the page's <nk-sidebar> as a drawer; on the desktop
  //   it shows while that sidebar is collapsed (.collapsed) and expands it.
  //
  // Modifier classes become attributes. A slotted <svg> is sized by the
  // `.nk-btn ::slotted(svg)` twin; pass the icon itself, never wrapped.
  const VARIANTS$1 = ['primary', 'secondary', 'danger', 'danger-solid', 'topbar', 'share'];

  class NkBtn extends NkElement {
    static get observedAttributes() { return ['variant', 'small', 'disabled', 'type', 'href', 'title', 'aria-label', 'aria-haspopup', 'active']; }

    render() {
      this._build();
    }

    _build() {
      const href = this.getAttribute('href');
      const el = href ? this.createElement('a', [], { href }) : this.createElement('button', [], { type: this.getAttribute('type') || 'button' });
      el.className = this._computeClasses().join(' ');
      el.appendChild(document.createElement('slot'));
      this._applyState(el);
      if (this._btn) {
        this._btn.removeEventListener('click', this._onClick);
        this._btn.replaceWith(el);
      } else {
        this._wrapper.appendChild(el);
      }
      this._btn = el;
      if (this._onClick) el.addEventListener('click', this._onClick);
    }

    _applyState(el) {
      const disabled = this.getBoolAttr('disabled');
      if (el.tagName === 'BUTTON') el.disabled = disabled;
      else el.setAttribute('aria-disabled', disabled ? 'true' : 'false');
      const title = this.getAttribute('title');
      title ? el.setAttribute('title', title) : el.removeAttribute('title');
      // The accessible name and the popup hint belong on the button itself.
      for (const name of ['aria-label', 'aria-haspopup']) {
        const v = this.getAttribute(name);
        v ? el.setAttribute(name, v) : el.removeAttribute(name);
      }
      if (this.getAttribute('variant') === 'sidebar') el.setAttribute('aria-expanded', this._sidebar()?.open ? 'true' : 'false');
      else el.removeAttribute('aria-expanded');
    }

    /** The sidebar the ☰ opens: the one in its own app, else the page's. */
    _sidebar() { return this.closest('nk-app')?.querySelector('nk-sidebar') ?? document.querySelector('nk-sidebar'); }
    _sidebarCollapsed() { return this.getAttribute('variant') === 'sidebar' && !!this._sidebar()?.hasAttribute('collapsed'); }

    setupEvents() {
      this._onClick = (e) => {
        if (this.getBoolAttr('disabled')) { e.preventDefault(); e.stopPropagation(); return; }
        if (this.getAttribute('variant') === 'sidebar') {
          const sidebar = this._sidebar();
          if (sidebar?.hasAttribute('collapsed') && !matchMedia('(max-width: 860px)').matches) sidebar.expand?.();
          else sidebar?.toggle?.();
          return;
        }
        // A button inside a shadow root is not a submit button of the outer
        // form; forward the intent to the form the host sits in.
        const type = this.getAttribute('type');
        if (type === 'submit' || type === 'reset') {
          const form = this.closest('form');
          if (form) type === 'submit' ? form.requestSubmit() : form.reset();
        }
      };
      // The ☰ says whether its drawer is open, however the drawer closed.
      this._onToggle = (e) => {
        if (this.getAttribute('variant') === 'sidebar' && e.target === this._sidebar()) this._btn.setAttribute('aria-expanded', e.detail.open ? 'true' : 'false');
      };
      // On the desktop the ☰ shows while its sidebar is collapsed.
      this._onCollapse = (e) => {
        if (this.getAttribute('variant') === 'sidebar' && e.target === this._sidebar()) this._btn.classList.toggle('collapsed', e.detail.collapsed);
      };
      this._btn.addEventListener('click', this._onClick);
      document.addEventListener('nk-toggle', this._onToggle);
      document.addEventListener('nk-collapse', this._onCollapse);
      if (this.getAttribute('variant') === 'sidebar') this._btn.className = this._computeClasses().join(' ');
    }

    teardownEvents() {
      this._btn?.removeEventListener('click', this._onClick);
      document.removeEventListener('nk-toggle', this._onToggle);
      document.removeEventListener('nk-collapse', this._onCollapse);
    }

    onAttributeChanged(name) {
      if (!this._btn) return;
      switch (name) {
        case 'href':
          this._build();
          break;
        case 'variant':
          this._btn.className = this._computeClasses().join(' ');
          this._applyState(this._btn);
          break;
        case 'small':
        case 'active':
          this._btn.className = this._computeClasses().join(' ');
          break;
        case 'type':
          if (this._btn.tagName === 'BUTTON') this._btn.setAttribute('type', this.getAttribute('type') || 'button');
          break;
        default:
          this._applyState(this._btn);
      }
    }

    _computeClasses() {
      const variant = this.getAttribute('variant');
      if (variant === 'topbar') return ['nk-topbar-btn'];
      if (variant === 'share') return ['nk-topbar-btn', 'nk-share-btn'];
      if (variant === 'sidebar') return this._sidebarCollapsed() ? ['nk-topbar-btn', 'nk-sidebar-toggle', 'collapsed'] : ['nk-topbar-btn', 'nk-sidebar-toggle'];
      if (variant === 'tool') return this.getBoolAttr('active') ? ['nk-db-tool', 'active'] : ['nk-db-tool'];
      const classes = ['nk-btn'];
      if (VARIANTS$1.includes(variant)) classes.push(variant);
      if (this.getBoolAttr('small')) classes.push('small');
      return classes;
    }

    focus(options) { this._btn?.focus(options); }
    click() { this._btn?.click(); }

    get variant() { return this.getAttribute('variant'); }
    set variant(v) { v ? this.setAttribute('variant', v) : this.removeAttribute('variant'); }
    get active() { return this.getBoolAttr('active'); }
    set active(v) { this.setBoolAttr('active', v); }
    get small() { return this.getBoolAttr('small'); }
    set small(v) { this.setBoolAttr('small', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get type() { return this.getAttribute('type') || 'button'; }
    set type(v) { this.setAttribute('type', v); }
    get href() { return this.getAttribute('href'); }
    set href(v) { v ? this.setAttribute('href', v) : this.removeAttribute('href'); }
  }

  customElements.define('nk-btn', NkBtn);

  // <nk-input name="title" value="…" placeholder="…" wide></nk-input>  →  <input class="nk-input wide">
  // Takes part in the surrounding <form> through ElementInternals.
  const FORWARDED$2 = ['type', 'placeholder', 'name', 'min', 'max', 'step', 'autocomplete', 'pattern', 'maxlength', 'minlength', 'inputmode', 'list', 'aria-label'];
  const BOOLEANS$1 = ['required', 'disabled', 'readonly'];

  class NkInput extends NkFormElement {
    static get observedAttributes() { return ['value', 'wide', ...FORWARDED$2, ...BOOLEANS$1]; }

    render() {
      this._input = this.createElement('input', ['nk-input']);
      for (const attr of FORWARDED$2) this._forward(attr);
      for (const attr of BOOLEANS$1) this._forward(attr);
      this._input.value = this.getAttribute('value') ?? '';
      this._defaultValue = this._input.value;
      this._syncWide();
      this._wrapper.appendChild(this._input);
      this._syncFormValue();
    }

    _forward(attr) {
      if (BOOLEANS$1.includes(attr)) {
        this._input[attr === 'readonly' ? 'readOnly' : attr] = this.getBoolAttr(attr) || (attr === 'disabled' && !!this._formDisabled);
        return;
      }
      const v = this.getAttribute(attr);
      if (v === null) this._input.removeAttribute(attr);
      else this._input.setAttribute(attr, v);
    }

    _syncWide() {
      this._input.classList.toggle('wide', this.getBoolAttr('wide'));
    }

    setupEvents() {
      this._onInput = () => {
        this._syncFormValue();
        this.emit('nk-input', { value: this._input.value, name: this.name });
      };
      this._onChange = () => {
        this._syncFormValue();
        this.emit('nk-change', { value: this._input.value, name: this.name });
        this.dispatchEvent(new Event('change', { bubbles: true }));
      };
      this._input.addEventListener('input', this._onInput);
      this._input.addEventListener('change', this._onChange);
    }

    teardownEvents() {
      this._input?.removeEventListener('input', this._onInput);
      this._input?.removeEventListener('change', this._onChange);
    }

    onAttributeChanged(name) {
      if (!this._input) return;
      if (name === 'value') { this._input.value = this.getAttribute('value') ?? ''; this._syncFormValue(); return; }
      if (name === 'wide') { this._syncWide(); return; }
      this._forward(name);
      this._syncFormValue();
    }

    _syncFormValue() {
      this.setFormValue(this._input.value);
      this.syncValidityFrom(this._input);
    }

    resetValue() {
      this._input.value = this.getAttribute('value') ?? '';
      this._syncFormValue();
    }

    restoreValue(state) {
      if (typeof state === 'string') { this._input.value = state; this._syncFormValue(); }
    }

    onFormDisabled(disabled) {
      this._input.disabled = disabled || this.getBoolAttr('disabled');
    }

    focus(options) { this._input?.focus(options); }
    blur() { this._input?.blur(); }
    select() { this._input?.select(); }

    get value() { return this._input?.value ?? (this.getAttribute('value') ?? ''); }
    set value(v) {
      if (this._input) { this._input.value = v ?? ''; this._syncFormValue(); }
      else this.setAttribute('value', v ?? '');
    }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get type() { return this.getAttribute('type') || 'text'; }
    set type(v) { this.setAttribute('type', v); }
    get placeholder() { return this.getAttribute('placeholder'); }
    set placeholder(v) { this.setAttribute('placeholder', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get required() { return this.getBoolAttr('required'); }
    set required(v) { this.setBoolAttr('required', v); }
    get readonly() { return this.getBoolAttr('readonly'); }
    set readonly(v) { this.setBoolAttr('readonly', v); }
    get wide() { return this.getBoolAttr('wide'); }
    set wide(v) { this.setBoolAttr('wide', v); }
  }

  customElements.define('nk-input', NkInput);

  // <nk-textarea name="bio" rows="3" wide>Text</nk-textarea>  →  <textarea class="nk-textarea wide">
  // The initial value is the `value` attribute, or the element's text content.
  const FORWARDED$1 = ['placeholder', 'name', 'rows', 'maxlength', 'minlength', 'autocomplete', 'aria-label'];
  const BOOLEANS = ['required', 'disabled', 'readonly'];

  class NkTextarea extends NkFormElement {
    static get observedAttributes() { return ['value', 'wide', ...FORWARDED$1, ...BOOLEANS]; }

    render() {
      this._input = this.createElement('textarea', ['nk-textarea']);
      for (const attr of [...FORWARDED$1, ...BOOLEANS]) this._forward(attr);
      this._input.value = this.getAttribute('value') ?? this.textContent.trim();
      this._syncWide();
      this._wrapper.appendChild(this._input);
      this._syncFormValue();
    }

    _forward(attr) {
      if (BOOLEANS.includes(attr)) {
        this._input[attr === 'readonly' ? 'readOnly' : attr] = this.getBoolAttr(attr) || (attr === 'disabled' && !!this._formDisabled);
        return;
      }
      const v = this.getAttribute(attr);
      if (v === null) this._input.removeAttribute(attr);
      else this._input.setAttribute(attr, v);
    }

    _syncWide() { this._input.classList.toggle('wide', this.getBoolAttr('wide')); }

    setupEvents() {
      this._onInput = () => {
        this._syncFormValue();
        this.emit('nk-input', { value: this._input.value, name: this.name });
      };
      this._onChange = () => {
        this._syncFormValue();
        this.emit('nk-change', { value: this._input.value, name: this.name });
        this.dispatchEvent(new Event('change', { bubbles: true }));
      };
      this._input.addEventListener('input', this._onInput);
      this._input.addEventListener('change', this._onChange);
    }

    teardownEvents() {
      this._input?.removeEventListener('input', this._onInput);
      this._input?.removeEventListener('change', this._onChange);
    }

    onAttributeChanged(name) {
      if (!this._input) return;
      if (name === 'value') { this._input.value = this.getAttribute('value') ?? ''; this._syncFormValue(); return; }
      if (name === 'wide') { this._syncWide(); return; }
      this._forward(name);
      this._syncFormValue();
    }

    _syncFormValue() {
      this.setFormValue(this._input.value);
      this.syncValidityFrom(this._input);
    }

    resetValue() {
      this._input.value = this.getAttribute('value') ?? this.textContent.trim();
      this._syncFormValue();
    }

    restoreValue(state) {
      if (typeof state === 'string') { this._input.value = state; this._syncFormValue(); }
    }

    onFormDisabled(disabled) { this._input.disabled = disabled || this.getBoolAttr('disabled'); }

    focus(options) { this._input?.focus(options); }
    blur() { this._input?.blur(); }

    get value() { return this._input?.value ?? (this.getAttribute('value') ?? ''); }
    set value(v) {
      if (this._input) { this._input.value = v ?? ''; this._syncFormValue(); }
      else this.setAttribute('value', v ?? '');
    }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get required() { return this.getBoolAttr('required'); }
    set required(v) { this.setBoolAttr('required', v); }
    get wide() { return this.getBoolAttr('wide'); }
    set wide(v) { this.setBoolAttr('wide', v); }
  }

  customElements.define('nk-textarea', NkTextarea);

  // <nk-select name="role" value="editor">
  //   <option value="viewer">Viewer</option>
  //   <option value="editor">Editor</option>
  // </nk-select>
  //
  // The light-DOM <option>s are copied into the shadow <select>. A
  // MutationObserver keeps the copy in step when a framework swaps them; the
  // empty string is a valid value; the live selection survives a rebuild.
  class NkSelect extends NkFormElement {
    static get observedAttributes() { return ['value', 'name', 'disabled', 'required', 'compact', 'wide', 'aria-label']; }
    static get observesLightDom() { return true; }

    render() {
      this._select = this.createElement('select', ['nk-select']);
      this._forwardAll();
      this._wrapper.appendChild(this._select);
      this._defaultValue = this.getAttribute('value');
      // Children may not be parsed yet when connectedCallback runs.
      this.projectLightDom();
      requestAnimationFrame(() => this.projectLightDom());
    }

    _forwardAll() {
      const name = this.getAttribute('name');
      name ? this._select.setAttribute('name', name) : this._select.removeAttribute('name');
      const label = this.getAttribute('aria-label');
      label ? this._select.setAttribute('aria-label', label) : this._select.removeAttribute('aria-label');
      this._select.disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      this._select.required = this.getBoolAttr('required');
      this._select.classList.toggle('compact', this.getBoolAttr('compact'));
      this._select.classList.toggle('wide', this.getBoolAttr('wide'));
    }

    projectLightDom() {
      if (!this._select) return;
      // The options are pure data — the clones carry no listeners — so skipping
      // an unchanged rebuild is safe, and it keeps an open dropdown from
      // snapping shut on light-DOM churn elsewhere.
      const nodes = [...this.children].filter(n => n.matches('option, optgroup'));
      const signature = nodes.map(n => n.outerHTML).join('');
      if (signature === this._optionSignature) return;
      this._optionSignature = signature;

      // innerHTML = '' drops the selection; remember it first. selectedIndex -1
      // means "nothing selected", which is not an option whose value is "".
      const previous = this._select.selectedIndex >= 0 ? this._select.value : null;

      this._select.innerHTML = '';
      for (const node of nodes) this._select.appendChild(node.cloneNode(true));

      // Keep the live selection when it survived; otherwise the value attribute;
      // otherwise the browser default (first option / `selected`).
      if (!this._applyValue(previous)) this._applyValue(this.getAttribute('value'));
      this._syncFormValue();
    }

    /** Selects `value` if an option carries it, and reports whether it did. */
    _applyValue(value) {
      if (value === null) return false;
      if (![...this._select.options].some(o => o.value === value)) return false;
      this._select.value = value;
      return true;
    }

    setupEvents() {
      this._onChange = () => {
        this._syncFormValue();
        this.emit('nk-change', { value: this._select.value, name: this.name });
        this.dispatchEvent(new Event('change', { bubbles: true }));
      };
      this._select.addEventListener('change', this._onChange);
    }

    teardownEvents() {
      this._select?.removeEventListener('change', this._onChange);
    }

    onAttributeChanged(name) {
      if (!this._select) return;
      if (name === 'value') {
        this._applyValue(this.getAttribute('value'));
        this._syncFormValue();
        return;
      }
      this._forwardAll();
      this._syncFormValue();
    }

    _syncFormValue() {
      this.setFormValue(this._select.value);
      this.syncValidityFrom(this._select);
    }

    resetValue() {
      if (!this._applyValue(this._defaultValue)) this._select.selectedIndex = 0;
      this._syncFormValue();
    }

    restoreValue(state) {
      if (typeof state === 'string') { this._applyValue(state); this._syncFormValue(); }
    }

    onFormDisabled(disabled) { this._select.disabled = disabled || this.getBoolAttr('disabled'); }

    focus(options) { this._select?.focus(options); }

    get value() { return this._select?.value ?? ''; }
    set value(v) {
      if (this._select) { this._select.value = v; this._syncFormValue(); }
      else this.setAttribute('value', v);
    }
    get selectedIndex() { return this._select?.selectedIndex ?? -1; }
    get options() { return this._select?.options ?? []; }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get required() { return this.getBoolAttr('required'); }
    set required(v) { this.setBoolAttr('required', v); }
    get compact() { return this.getBoolAttr('compact'); }
    set compact(v) { this.setBoolAttr('compact', v); }
    get wide() { return this.getBoolAttr('wide'); }
    set wide(v) { this.setBoolAttr('wide', v); }
  }

  customElements.define('nk-select', NkSelect);

  // <nk-switch name="notify" checked></nk-switch>
  // → <button class="nk-switch" role="switch" aria-checked="true">
  // notionkit.css styles the state through [aria-checked]; the toggling is here.
  //
  // <nk-switch checked>Waiting</nk-switch>   (or text="Waiting")
  // → <label class="nk-switch-label"><button class="nk-switch" …></button><span>Waiting</span></label>
  // Visible text beside the switch, for several switches in one row where no
  // nk-field row names them. Without text the label wrapper is layout-
  // transparent (display: contents), so the bare button stays the bare button.
  class NkSwitch extends NkFormElement {
    static get observedAttributes() { return ['checked', 'disabled', 'name', 'value', 'label', 'text']; }

    render() {
      this._row = this.createElement('label', ['nk-switch-label']);
      this._btn = this.createElement('button', ['nk-switch'], { type: 'button', role: 'switch' });
      this._text = document.createElement('span');
      this._slot = document.createElement('slot');
      this._textNode = document.createTextNode('');
      this._slot.appendChild(this._textNode);
      this._text.appendChild(this._slot);
      this._row.append(this._btn, this._text);
      this._defaultChecked = this.getBoolAttr('checked');
      this._apply();
      this._syncText();
      this._wrapper.appendChild(this._row);
      this._syncFormValue();
    }

    _apply() {
      this._btn.setAttribute('aria-checked', this.getBoolAttr('checked') ? 'true' : 'false');
      this._btn.disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      const label = this.getAttribute('label');
      label ? this._btn.setAttribute('aria-label', label) : this._btn.removeAttribute('aria-label');
    }

    _syncText() {
      this._textNode.data = this.getAttribute('text') ?? '';
      const hasText = !!(this.getAttribute('text') || this._slot.assignedNodes().some(n => n.nodeType === Node.ELEMENT_NODE || n.data.trim()));
      this._text.hidden = !hasText;
      this._row.style.display = hasText ? '' : 'contents';
    }

    setupEvents() {
      this._onClick = () => this.toggle();
      this._onSlot = () => this._syncText();
      this._btn.addEventListener('click', this._onClick);
      this._slot.addEventListener('slotchange', this._onSlot);
      this._syncText();
    }

    teardownEvents() {
      this._btn?.removeEventListener('click', this._onClick);
      this._slot?.removeEventListener('slotchange', this._onSlot);
    }

    toggle() {
      if (this.getBoolAttr('disabled') || this._formDisabled) return;
      this.checked = !this.checked;
      this.emit('nk-change', { checked: this.checked, value: this.getAttribute('value') || 'on', name: this.name });
      this.dispatchEvent(new Event('change', { bubbles: true }));
    }

    onAttributeChanged(name) {
      if (!this._btn) return;
      if (name === 'text') { this._syncText(); return; }
      this._apply();
      this._syncFormValue();
    }

    _syncFormValue() {
      const val = this.getAttribute('value') || 'on';
      this.setFormValue(this.getBoolAttr('checked') ? val : null, this.getBoolAttr('checked') ? 'checked' : 'unchecked');
    }

    resetValue() { this.setBoolAttr('checked', this._defaultChecked); }
    restoreValue(state) { if (state === 'checked' || state === 'unchecked') this.setBoolAttr('checked', state === 'checked'); }
    onFormDisabled(disabled) { this._formDisabled = disabled; this._apply(); }

    focus(options) { this._btn?.focus(options); }

    get checked() { return this.getBoolAttr('checked'); }
    set checked(v) { this.setBoolAttr('checked', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get value() { return this.getAttribute('value') || 'on'; }
    set value(v) { this.setAttribute('value', v); }
    get text() { return this.getAttribute('text'); }
    set text(v) { v == null ? this.removeAttribute('text') : this.setAttribute('text', v); }
  }

  customElements.define('nk-switch', NkSwitch);

  // <nk-check name="mail" value="weekly" checked>Weekly digest</nk-check>
  // → <label class="nk-check"><input type="checkbox"> Weekly digest</label>
  class NkCheck extends NkFormElement {
    static get observedAttributes() { return ['checked', 'disabled', 'name', 'value', 'text', 'indeterminate', 'required']; }

    render() {
      const label = this.createElement('label', ['nk-check']);
      this._input = this.createElement('input', [], { type: 'checkbox' });
      const slot = document.createElement('slot');
      this._text = document.createTextNode('');
      slot.appendChild(this._text);
      this._apply();
      this._defaultChecked = this.getBoolAttr('checked');
      label.appendChild(this._input);
      label.appendChild(slot);
      this._wrapper.appendChild(label);
      this._syncFormValue();
    }

    _apply() {
      this._input.checked = this.getBoolAttr('checked');
      this._input.indeterminate = this.getBoolAttr('indeterminate');
      this._input.disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      this._input.required = this.getBoolAttr('required');
      const name = this.getAttribute('name');
      name ? this._input.setAttribute('name', name) : this._input.removeAttribute('name');
      this._input.value = this.getAttribute('value') || 'on';
      if (this._text) this._text.data = this.getAttribute('text') ?? '';
    }

    setupEvents() {
      this._onChange = () => {
        this._syncing = true;
        this.setBoolAttr('checked', this._input.checked);
        this.removeAttribute('indeterminate');
        this._syncing = false;
        this._syncFormValue();
        this.emit('nk-change', { checked: this._input.checked, value: this._input.value, name: this.name });
        this.dispatchEvent(new Event('change', { bubbles: true }));
      };
      this._input.addEventListener('change', this._onChange);
    }

    teardownEvents() {
      this._input?.removeEventListener('change', this._onChange);
    }

    onAttributeChanged() {
      if (this._syncing || !this._input) return;
      this._apply();
      this._syncFormValue();
    }

    _syncFormValue() {
      this.setFormValue(this._input.checked ? this._input.value : null, this._input.checked ? 'checked' : 'unchecked');
      this.syncValidityFrom(this._input);
    }

    resetValue() { this.setBoolAttr('checked', this._defaultChecked); }
    restoreValue(state) { if (state === 'checked' || state === 'unchecked') this.setBoolAttr('checked', state === 'checked'); }
    onFormDisabled(disabled) { this._formDisabled = disabled; this._apply(); }

    focus(options) { this._input?.focus(options); }

    get checked() { return this._input?.checked ?? this.getBoolAttr('checked'); }
    set checked(v) { this.setBoolAttr('checked', v); }
    get indeterminate() { return this.getBoolAttr('indeterminate'); }
    set indeterminate(v) { this.setBoolAttr('indeterminate', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get text() { return this.getAttribute('text'); }
    set text(v) { v == null ? this.removeAttribute('text') : this.setAttribute('text', v); }
    get value() { return this.getAttribute('value') || 'on'; }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-check', NkCheck);

  // <nk-radio name="style" value="concise" checked>Concise</nk-radio>
  // → <label class="nk-check"><input type="radio"> Concise</label>
  //
  // ── Grouping ──
  // Every <nk-radio> keeps its <input type="radio"> in its own shadow root, and
  // native radio grouping works per tree — it does not reach across shadow
  // boundaries. Two <nk-radio name="x"> would therefore both stay checked. So
  // the group is kept here, following the native definition: same `name`,
  // same containing tree, same form owner. One tab stop per group, arrow keys
  // move within it (wrapping, skipping disabled), and select as they go.

  const ARROW_KEYS = { ArrowDown: 1, ArrowRight: 1, ArrowUp: -1, ArrowLeft: -1 };

  /** Form owner, also for a peer that has not been upgraded yet. */
  function ownerForm(el) {
    return (el.form !== undefined ? el.form : el.closest('form')) ?? null;
  }

  /** Only the selected radio is a tab stop; arrow keys move within the group. */
  function syncGroupTabIndex(group) {
    const enabled = group.filter(el => !el.disabled);
    if (!enabled.length) return;
    const focusable = enabled.find(el => el.checked) || enabled[0];
    for (const el of group) {
      if (el._input) el._input.tabIndex = el === focusable ? 0 : -1;
    }
  }

  class NkRadio extends NkFormElement {
    static get observedAttributes() { return ['checked', 'disabled', 'name', 'value', 'text', 'required']; }

    render() {
      const label = this.createElement('label', ['nk-check']);
      this._input = this.createElement('input', [], { type: 'radio' });
      const slot = document.createElement('slot');
      this._text = document.createTextNode('');
      slot.appendChild(this._text);
      // `name` on the inner input is inert (it is alone in its tree); it stays
      // as a devtools courtesy. The group above is the real one.
      this._apply();
      this._defaultChecked = this.getBoolAttr('checked');
      label.appendChild(this._input);
      label.appendChild(slot);
      this._wrapper.appendChild(label);
      this._syncFormValue();
      // Elements upgrade in document order, so the last `checked` one in the
      // markup wins the group — the same outcome native radios produce.
      if (this._input.checked) this._uncheckPeers();
      syncGroupTabIndex(this._group());
    }

    _apply() {
      this._input.checked = this.getBoolAttr('checked');
      this._input.disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      this._input.required = this.getBoolAttr('required');
      const name = this.getAttribute('name');
      name ? this._input.setAttribute('name', name) : this._input.removeAttribute('name');
      this._input.value = this.getAttribute('value') || '';
      if (this._text) this._text.data = this.getAttribute('text') ?? '';
    }

    setupEvents() {
      this._onChange = () => this._applyChange();
      this._onKeyDown = (e) => {
        const dir = ARROW_KEYS[e.key];
        if (!dir || e.ctrlKey || e.metaKey || e.altKey) return;
        const group = this._group().filter(el => !el.disabled);
        if (group.length < 2) return;
        e.preventDefault();
        const next = group[(group.indexOf(this) + dir + group.length) % group.length];
        next._input.focus();
        next._input.checked = true;
        next._applyChange();
      };
      this._input.addEventListener('change', this._onChange);
      this._input.addEventListener('keydown', this._onKeyDown);
      syncGroupTabIndex(this._group());
    }

    teardownEvents() {
      this._input?.removeEventListener('change', this._onChange);
      this._input?.removeEventListener('keydown', this._onKeyDown);
    }

    /** Shared by user change and arrow-key selection, so both look identical. */
    _applyChange() {
      if (this._input.checked) this._uncheckPeers();
      this._syncing = true;
      this.setBoolAttr('checked', this._input.checked);
      this._syncing = false;
      this._syncFormValue();
      syncGroupTabIndex(this._group());
      this.emit('nk-change', { checked: this._input.checked, value: this._input.value, name: this.name });
      this.dispatchEvent(new Event('change', { bubbles: true }));
    }

    /** Same name, same tree, same form owner — in document order, this element included. */
    _group() {
      const name = this.getAttribute('name');
      const root = this.getRootNode();
      if (!name || typeof root?.querySelectorAll !== 'function') return [this];
      const form = ownerForm(this);
      return [...root.querySelectorAll('nk-radio')].filter(
        el => el.getAttribute('name') === name && ownerForm(el) === form
      );
    }

    _uncheckPeers() {
      for (const el of this._group()) {
        if (el !== this && el.checked) el.checked = false;
      }
    }

    onAttributeChanged(name) {
      if (this._syncing || !this._input) return;
      this._apply();
      if (name === 'checked' && this._input.checked) this._uncheckPeers();
      this._syncFormValue();
      syncGroupTabIndex(this._group());
    }

    _syncFormValue() {
      const val = this.getAttribute('value') || '';
      this.setFormValue(this._input.checked ? val : null, this._input.checked ? 'checked' : 'unchecked');
      this.syncValidityFrom(this._input);
    }

    resetValue() {
      this._input.checked = this._defaultChecked;
      this.setBoolAttr('checked', this._defaultChecked);
      this._syncFormValue();
      syncGroupTabIndex(this._group());
    }

    restoreValue(state) {
      if (state === 'checked') this.checked = true;
    }

    onFormDisabled(disabled) { this._formDisabled = disabled; this._apply(); syncGroupTabIndex(this._group()); }

    focus(options) { this._input?.focus(options); }

    get checked() { return this._input?.checked ?? this.getBoolAttr('checked'); }
    set checked(v) {
      if (this._input) this._input.checked = v;
      // Also covers the case where the attribute is already present, so
      // setBoolAttr stays silent and onAttributeChanged never runs.
      if (v) this._uncheckPeers();
      this.setBoolAttr('checked', v);
      if (this._input) { this._syncFormValue(); syncGroupTabIndex(this._group()); }
    }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get text() { return this.getAttribute('text'); }
    set text(v) { v == null ? this.removeAttribute('text') : this.setAttribute('text', v); }
    get value() { return this.getAttribute('value') || ''; }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-radio', NkRadio);

  // <nk-slider name="size" min="12" max="18" value="14" show-value></nk-slider>
  // → <input type="range" class="nk-slider"> <div class="nk-slider-value">14</div>
  const FORWARDED = ['name', 'min', 'max', 'step', 'aria-label'];

  class NkSlider extends NkFormElement {
    static get observedAttributes() { return ['value', 'disabled', 'show-value', 'unit', ...FORWARDED]; }

    render() {
      this._input = this.createElement('input', ['nk-slider'], { type: 'range' });
      this._out = this.createElement('div', ['nk-slider-value']);
      this._apply();
      this._input.value = this.getAttribute('value') ?? this._input.value;
      this._defaultValue = this._input.value;
      this._wrapper.appendChild(this._input);
      this._wrapper.appendChild(this._out);
      this._syncOut();
      this._syncFormValue();
    }

    _apply() {
      for (const attr of FORWARDED) {
        const v = this.getAttribute(attr);
        v === null ? this._input.removeAttribute(attr) : this._input.setAttribute(attr, v);
      }
      this._input.disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      this._out.style.display = this.getBoolAttr('show-value') ? '' : 'none';
    }

    _syncOut() {
      this._out.textContent = `${this._input.value}${this.getAttribute('unit') || ''}`;
    }

    setupEvents() {
      this._onInput = () => {
        this._syncOut();
        this._syncFormValue();
        this.emit('nk-input', { value: Number(this._input.value), name: this.name });
      };
      this._onChange = () => {
        this._syncFormValue();
        this.emit('nk-change', { value: Number(this._input.value), name: this.name });
        this.dispatchEvent(new Event('change', { bubbles: true }));
      };
      this._input.addEventListener('input', this._onInput);
      this._input.addEventListener('change', this._onChange);
    }

    teardownEvents() {
      this._input?.removeEventListener('input', this._onInput);
      this._input?.removeEventListener('change', this._onChange);
    }

    onAttributeChanged(name) {
      if (!this._input) return;
      if (name === 'value') this._input.value = this.getAttribute('value') ?? '';
      else this._apply();
      this._syncOut();
      this._syncFormValue();
    }

    _syncFormValue() { this.setFormValue(this._input.value); }
    resetValue() { this._input.value = this._defaultValue; this._syncOut(); this._syncFormValue(); }
    restoreValue(state) { if (typeof state === 'string') { this._input.value = state; this._syncOut(); this._syncFormValue(); } }
    onFormDisabled(disabled) { this._formDisabled = disabled; this._apply(); }

    focus(options) { this._input?.focus(options); }

    get value() { return Number(this._input?.value ?? this.getAttribute('value')); }
    set value(v) {
      if (this._input) { this._input.value = v; this._syncOut(); this._syncFormValue(); }
      else this.setAttribute('value', v);
    }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get showValue() { return this.getBoolAttr('show-value'); }
    set showValue(v) { this.setBoolAttr('show-value', v); }
  }

  customElements.define('nk-slider', NkSlider);

  // <nk-field label="Display name" desc="Shown next to your comments">
  //   <nk-input value="Marcel"></nk-input>
  // </nk-field>
  // → <div class="nk-field"><div><div class="f-label">…</div><div class="f-desc">…</div></div>
  //                         <div class="f-control">…</div></div>
  // Rich label / description content goes through slot="label" / slot="desc"
  // (twins `.nk-field ::slotted([slot="label"])` etc. in notionkit.css).
  //
  // `stacked` puts the label above a full-width control, `compact` shrinks the
  // label and drops the row padding; inside <nk-fields> both are on by
  // default. A stacked field sets `wide` on its nk-input / nk-textarea /
  // nk-select, since the stylesheet's `.nk-field.stacked .nk-input` cannot
  // reach into the control's shadow root.
  const WIDE = ['nk-input', 'nk-textarea', 'nk-select'];

  class NkField extends NkElement {
    static get observedAttributes() { return ['label', 'desc', 'stacked', 'compact']; }

    render() {
      this._field = this.createElement('div', ['nk-field']);
      const text = document.createElement('div');
      this._label = this.createElement('div', ['f-label']);
      const labelSlot = this.createElement('slot', [], { name: 'label' });
      labelSlot.appendChild(this._label);
      this._desc = this.createElement('div', ['f-desc']);
      const descSlot = this.createElement('slot', [], { name: 'desc' });
      descSlot.appendChild(this._desc);
      text.appendChild(labelSlot);
      text.appendChild(descSlot);
      const control = this.createElement('div', ['f-control']);
      this._slot = document.createElement('slot');
      control.appendChild(this._slot);
      this._field.appendChild(text);
      this._field.appendChild(control);
      this._wrapper.appendChild(this._field);
      this._widened = new Set();
      this._sync();
    }

    setupEvents() {
      this._onSlot = () => this._syncWide();
      this._slot.addEventListener('slotchange', this._onSlot);
      this._sync();
    }

    teardownEvents() {
      this._slot?.removeEventListener('slotchange', this._onSlot);
    }

    onAttributeChanged() { this._sync(); }

    get stacked() { return this.getBoolAttr('stacked') || !!this.closest('nk-fields'); }
    set stacked(v) { this.setBoolAttr('stacked', v); }
    get compact() { return this.getBoolAttr('compact') || !!this.closest('nk-fields'); }
    set compact(v) { this.setBoolAttr('compact', v); }

    _sync() {
      if (!this._label) return;
      this._label.textContent = this.getAttribute('label') || '';
      const desc = this.getAttribute('desc');
      this._desc.textContent = desc || '';
      this._desc.style.display = desc ? '' : 'none';
      this._field.classList.toggle('stacked', this.stacked);
      this._field.classList.toggle('compact', this.compact);
      this._syncWide();
    }

    _syncWide() {
      if (!this._slot) return;
      const stacked = this.stacked;
      for (const el of this._slot.assignedElements()) {
        if (!WIDE.includes(el.localName)) continue;
        if (stacked && !el.hasAttribute('wide')) { el.setAttribute('wide', ''); this._widened.add(el); }
        else if (!stacked && this._widened.has(el)) { el.removeAttribute('wide'); this._widened.delete(el); }
      }
    }

    get label() { return this.getAttribute('label'); }
    set label(v) { this.setAttribute('label', v); }
    get desc() { return this.getAttribute('desc'); }
    set desc(v) { v == null ? this.removeAttribute('desc') : this.setAttribute('desc', v); }
  }

  customElements.define('nk-field', NkField);

  // <nk-fields>
  //   <nk-field label="Name"><nk-input name="name"></nk-input></nk-field>
  //   <nk-field label="Email"><nk-input name="email"></nk-input></nk-field>
  // </nk-fields>
  // → <div class="nk-fields">…</div>
  // A grid of short fields side by side, wrapping as the width allows. Every
  // nk-field inside renders itself stacked and compact (12px label above a
  // full-width control) – the class markup does the same through
  // `.nk-fields > .nk-field`, which cannot cross the shadow boundary here.
  class NkFields extends NkElement {
    render() {
      const grid = this.createElement('div', ['nk-fields']);
      grid.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(grid);
    }
  }

  customElements.define('nk-fields', NkFields);

  // <nk-tag color="blue">Text</nk-tag>  —  modifier class .blue becomes color="blue".
  // Notion's nine select colours; without `color` the stylesheet draws the grey tag.
  const COLORS$1 = ['gray', 'brown', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink', 'red'];

  class NkTag extends NkElement {
    static get observedAttributes() { return ['color']; }

    render() {
      this._tag = this.createElement('span', this._computeClasses());
      this._tag.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(this._tag);
    }

    onAttributeChanged() {
      if (this._tag) this._tag.className = this._computeClasses().join(' ');
    }

    _computeClasses() {
      const classes = ['nk-tag'];
      const color = this.getAttribute('color');
      if (COLORS$1.includes(color)) classes.push(color);
      return classes;
    }

    get color() { return this.getAttribute('color'); }
    set color(v) { v ? this.setAttribute('color', v) : this.removeAttribute('color'); }
  }

  customElements.define('nk-tag', NkTag);

  // <nk-progress value="72" label="72%"></nk-progress>
  // <nk-progress value="72" label="72 %" wide></nk-progress>   → fills its row, the label stays beside it
  // The bar is `.nk-progress > i` — a child combinator, so both nodes live here.
  class NkProgress extends NkElement {
    static get observedAttributes() { return ['value', 'max', 'label', 'wide']; }

    render() {
      this._bar = this.createElement('span', ['nk-progress'], { role: 'progressbar' });
      this._fill = document.createElement('i');
      this._bar.appendChild(this._fill);
      this._label = this.createElement('span', ['nk-progress-label']);
      this._wrapper.appendChild(this._bar);
      this._wrapper.appendChild(this._label);
      this._sync();
    }

    onAttributeChanged() { this._sync(); }

    _sync() {
      if (!this._bar) return;
      const max = Number(this.getAttribute('max')) || 100;
      const value = Math.min(max, Math.max(0, Number(this.getAttribute('value')) || 0));
      this._fill.style.width = `${(value / max) * 100}%`;
      this._bar.classList.toggle('wide', this.getBoolAttr('wide'));
      this._bar.setAttribute('aria-valuenow', value);
      this._bar.setAttribute('aria-valuemin', 0);
      this._bar.setAttribute('aria-valuemax', max);
      const label = this.getAttribute('label');
      this._label.textContent = label ?? '';
      this._label.style.display = label === null ? 'none' : '';
    }

    get value() { return Number(this.getAttribute('value')) || 0; }
    set value(v) { this.setAttribute('value', v); }
    get max() { return Number(this.getAttribute('max')) || 100; }
    set max(v) { this.setAttribute('max', v); }
    get label() { return this.getAttribute('label'); }
    set label(v) { v == null ? this.removeAttribute('label') : this.setAttribute('label', v); }
  }

  customElements.define('nk-progress', NkProgress);

  // <nk-callout icon="💡">text</nk-callout>
  // <nk-callout><span slot="icon">📌</span>text</nk-callout>
  // The .c-icon rule has ::slotted(.c-icon) / ::slotted([slot="icon"]) twins in
  // notionkit.css – those size an icon passed in from the light DOM. Pass the
  // icon node itself, never wrapped: ::slotted() only matches the assigned node.
  class NkCallout extends NkElement {
    static get observedAttributes() { return ['icon']; }

    render() {
      this._box = this.createElement('div', ['nk-callout']);
      this._icon = this.createElement('span', ['c-icon']);
      const iconSlot = this.createElement('slot', [], { name: 'icon' });
      iconSlot.appendChild(this._icon);
      const body = document.createElement('div');
      body.appendChild(document.createElement('slot'));
      this._box.appendChild(iconSlot);
      this._box.appendChild(body);
      this._wrapper.appendChild(this._box);
      this._syncIcon();
    }

    onAttributeChanged() { this._syncIcon(); }

    _syncIcon() {
      if (this._icon) this._icon.textContent = this.getAttribute('icon') || '💡';
    }

    get icon() { return this.getAttribute('icon'); }
    set icon(v) { v == null ? this.removeAttribute('icon') : this.setAttribute('icon', v); }
  }

  customElements.define('nk-callout', NkCallout);

  // <nk-divider></nk-divider>  →  <hr class="nk-divider">
  class NkDivider extends NkElement {
    render() {
      this._wrapper.appendChild(this.createElement('hr', ['nk-divider']));
    }
  }

  customElements.define('nk-divider', NkDivider);

  // <nk-heading level="2">Section</nk-heading>  →  <h2 class="nk-heading">
  // The heading level is real markup (h1–h4), so the outline stays honest.
  class NkHeading extends NkElement {
    static get observedAttributes() { return ['level']; }

    render() {
      this._build();
    }

    _build() {
      const level = Math.min(4, Math.max(1, Number(this.getAttribute('level')) || 2));
      const h = this.createElement(`h${level}`, ['nk-heading']);
      h.appendChild(document.createElement('slot'));
      if (this._heading) this._heading.replaceWith(h);
      else this._wrapper.appendChild(h);
      this._heading = h;
    }

    onAttributeChanged() { this._build(); }

    get level() { return Number(this.getAttribute('level')) || 2; }
    set level(v) { this.setAttribute('level', v); }
  }

  customElements.define('nk-heading', NkHeading);

  // <nk-toggle label="Details" open>Body…</nk-toggle>
  // → <details class="nk-toggle" open><summary>Details</summary><div class="toggle-body">…</div></details>
  // The summary marker is a pseudo-element and cannot be styled on slotted
  // content, so the summary is rendered here (label attribute or slot="label").
  class NkToggle extends NkElement {
    static get observedAttributes() { return ['label', 'open']; }

    render() {
      this._details = this.createElement('details', ['nk-toggle']);
      const summary = document.createElement('summary');
      this._label = document.createElement('span');
      const labelSlot = this.createElement('slot', [], { name: 'label' });
      labelSlot.appendChild(this._label);
      summary.appendChild(labelSlot);
      const body = this.createElement('div', ['toggle-body']);
      body.appendChild(document.createElement('slot'));
      this._details.appendChild(summary);
      this._details.appendChild(body);
      this._details.open = this.getBoolAttr('open');
      this._wrapper.appendChild(this._details);
      this._sync();
    }

    setupEvents() {
      this._onToggle = () => {
        this._syncing = true;
        this.setBoolAttr('open', this._details.open);
        this._syncing = false;
        this.emit('nk-toggle', { open: this._details.open });
      };
      this._details.addEventListener('toggle', this._onToggle);
    }

    teardownEvents() {
      this._details?.removeEventListener('toggle', this._onToggle);
    }

    onAttributeChanged(name) {
      if (!this._details) return;
      if (name === 'open') { if (!this._syncing) this._details.open = this.getBoolAttr('open'); return; }
      this._sync();
    }

    _sync() { this._label.textContent = this.getAttribute('label') || ''; }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get label() { return this.getAttribute('label'); }
    set label(v) { this.setAttribute('label', v); }
  }

  customElements.define('nk-toggle', NkToggle);

  // <nk-todo name="task" value="1" checked>Write the docs</nk-todo>
  // → <label class="nk-todo"><input type="checkbox"><span>Write the docs</span></label>
  // `.nk-todo input:checked + span` strikes the text through — both siblings
  // are rendered here so the combinator works; the text is slotted into <span>.
  class NkTodo extends NkFormElement {
    static get observedAttributes() { return ['checked', 'disabled', 'name', 'value']; }

    render() {
      const label = this.createElement('label', ['nk-todo']);
      this._input = this.createElement('input', [], { type: 'checkbox' });
      this._apply();
      this._defaultChecked = this.getBoolAttr('checked');
      const text = document.createElement('span');
      text.appendChild(document.createElement('slot'));
      label.appendChild(this._input);
      label.appendChild(text);
      this._wrapper.appendChild(label);
      this._syncFormValue();
    }

    _apply() {
      this._input.checked = this.getBoolAttr('checked');
      this._input.disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      const name = this.getAttribute('name');
      name ? this._input.setAttribute('name', name) : this._input.removeAttribute('name');
      this._input.value = this.getAttribute('value') || 'on';
    }

    setupEvents() {
      this._onChange = () => {
        this._syncing = true;
        this.setBoolAttr('checked', this._input.checked);
        this._syncing = false;
        this._syncFormValue();
        this.emit('nk-change', { checked: this._input.checked, value: this._input.value, name: this.name });
        this.dispatchEvent(new Event('change', { bubbles: true }));
      };
      this._input.addEventListener('change', this._onChange);
    }

    teardownEvents() {
      this._input?.removeEventListener('change', this._onChange);
    }

    onAttributeChanged() {
      if (this._syncing || !this._input) return;
      this._apply();
      this._syncFormValue();
    }

    _syncFormValue() {
      this.setFormValue(this._input.checked ? this._input.value : null, this._input.checked ? 'checked' : 'unchecked');
    }

    resetValue() { this.setBoolAttr('checked', this._defaultChecked); }
    restoreValue(state) { if (state === 'checked' || state === 'unchecked') this.setBoolAttr('checked', state === 'checked'); }
    onFormDisabled(disabled) { this._formDisabled = disabled; this._apply(); }

    get checked() { return this._input?.checked ?? this.getBoolAttr('checked'); }
    set checked(v) { this.setBoolAttr('checked', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get value() { return this.getAttribute('value') || 'on'; }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-todo', NkTodo);

  // <nk-kbd>⌘</nk-kbd> <nk-kbd>K</nk-kbd>  →  <kbd class="nk-kbd">
  class NkKbd extends NkElement {

    render() {
      const kbd = this.createElement('kbd', ['nk-kbd']);
      kbd.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(kbd);
    }
  }

  customElements.define('nk-kbd', NkKbd);

  // Minimal HTML highlighter emitting the .tag / .attr hooks .nk-code styles.
  // Mirrors tools/highlight.mjs in the NotionKit foundation – no third-party
  // highlighter, no other languages. Anything that is not HTML is escaped only.
  function escapeHtml(s) {
    return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
  }

  function highlightHtml(source) {
    return escapeHtml(source).replace(/&lt;(\/?)([a-zA-Z][\w-]*)([^&]*?)(\/?)&gt;/g, (m, close, tag, attrs, self) => {
      const attrHtml = attrs.replace(/([\w-]+)(=)("[^"]*"|'[^']*')?/g,
        (a, name, eq, val) => `<span class="attr">${name}</span>${eq}${val ?? ''}`);
      return `&lt;${close}<span class="tag">${tag}</span>${attrHtml}${self}&gt;`;
    });
  }

  // <nk-code lang="html">&lt;div class="nk-btn"&gt;…</nk-code>
  // The block keeps whatever whitespace it is given (white-space: pre). Text
  // content is slotted as-is; add `highlight` to colour HTML tags/attributes,
  // which copies the text into the shadow tree (light DOM is then ignored).
  class NkCode extends NkElement {
    static get observedAttributes() { return ['lang', 'highlight']; }
    static get observesLightDom() { return true; }

    render() {
      this._box = this.createElement('div', ['nk-code']);
      this._lang = this.createElement('span', ['lang']);
      this._slot = document.createElement('slot');
      this._pre = this.createElement('span', ['hl']);
      this._box.appendChild(this._lang);
      this._box.appendChild(this._slot);
      this._box.appendChild(this._pre);
      this._wrapper.appendChild(this._box);
      this._sync();
      this.projectLightDom();
    }

    onAttributeChanged() { this._sync(); this.projectLightDom(); }

    _sync() {
      if (!this._lang) return;
      const lang = this.getAttribute('lang');
      this._lang.textContent = lang || '';
      this._lang.style.display = lang ? '' : 'none';
    }

    projectLightDom() {
      if (!this._pre) return;
      const highlight = this.hasAttribute('highlight');
      this._slot.style.display = highlight ? 'none' : '';
      this._pre.style.display = highlight ? '' : 'none';
      if (!highlight) return;
      const text = this.textContent.replace(/^\n/, '');
      if (text === this._lastText) return;
      this._lastText = text;
      this._pre.innerHTML = highlightHtml(text);
    }

    get lang() { return this.getAttribute('lang'); }
    set lang(v) { v == null ? this.removeAttribute('lang') : this.setAttribute('lang', v); }
  }

  customElements.define('nk-code', NkCode);

  // <nk-quote cite="Ada Lovelace">The Analytical Engine weaves…</nk-quote>
  // .q-cite has no ::slotted() twin, so the citation is rendered here.
  class NkQuote extends NkElement {
    static get observedAttributes() { return ['cite']; }

    render() {
      this._quote = this.createElement('blockquote', ['nk-quote']);
      this._quote.appendChild(document.createElement('slot'));
      this._cite = this.createElement('cite', ['q-cite']);
      this._quote.appendChild(this._cite);
      this._wrapper.appendChild(this._quote);
      this._sync();
    }

    onAttributeChanged() { this._sync(); }

    _sync() {
      if (!this._cite) return;
      const cite = this.getAttribute('cite');
      this._cite.textContent = cite || '';
      this._cite.style.display = cite ? '' : 'none';
    }

    get cite() { return this.getAttribute('cite'); }
    set cite(v) { v == null ? this.removeAttribute('cite') : this.setAttribute('cite', v); }
  }

  customElements.define('nk-quote', NkQuote);

  // <nk-app>
  //   <nk-sidebar slot="sidebar">…</nk-sidebar>
  //   <nk-topbar>…</nk-topbar>
  //   <nk-page>…</nk-page>
  // </nk-app>
  // → <div class="nk-app"><aside class="nk-sidebar">…</aside><main class="nk-main">…</main></div>
  // The outermost element of a workspace app: a full-height flex row.
  // `peek-inset` (NotionKit 1.10.0) gives the main column a right padding as
  // wide as the side peek – <nk-peek inset> sets it while it is open.
  class NkApp extends NkElement {
    static get observedAttributes() { return ['peek-inset']; }

    render() {
      const app = this._app = this.createElement('div', ['nk-app']);
      app.classList.toggle('peek-inset', this.getBoolAttr('peek-inset'));
      app.appendChild(this.createElement('slot', [], { name: 'sidebar' }));
      const main = this.createElement('main', ['nk-main']);
      main.appendChild(document.createElement('slot'));
      app.appendChild(main);
      this._wrapper.appendChild(app);
    }

    onAttributeChanged() { this._app.classList.toggle('peek-inset', this.getBoolAttr('peek-inset')); }
  }

  customElements.define('nk-app', NkApp);

  // Overlay plumbing shared by nk-modal, nk-sheet, nk-cmdk and nk-menu: body
  // scroll lock with a refcount, `inert` on everything outside the overlay so
  // focus and assistive technology stay inside – nested shadow roots included –
  // and the focused element behind shadow hosts, to return focus to.
  let locks = 0, previousOverflow = '';

  function lockScroll() {
    if (locks++ === 0) {
      previousOverflow = document.documentElement.style.overflow;
      document.documentElement.style.overflow = 'hidden';
    }
  }

  function unlockScroll() {
    if (locks > 0 && --locks === 0) document.documentElement.style.overflow = previousOverflow;
  }

  /** Makes every sibling along `el`'s ancestor chain inert (shadow hosts included); returns the undo. */
  function inertOutside(el) {
    const made = [];
    let node = el;
    while (node && node !== document.body) {
      const parent = node.parentNode instanceof ShadowRoot ? node.parentNode : node.parentNode;
      if (!parent) break;
      for (const sib of parent.children) {
        if (sib === node || sib.inert || /^(SCRIPT|STYLE|LINK|TEMPLATE)$/.test(sib.tagName)) continue;
        sib.inert = true;
        made.push(sib);
      }
      node = parent instanceof ShadowRoot ? parent.host : parent;
    }
    return () => { for (const c of made) c.inert = false; };
  }

  /** The focused element itself, looking into shadow roots (document.activeElement stops at the host). */
  function deepActiveElement() {
    let el = document.activeElement;
    while (el?.shadowRoot?.activeElement) el = el.shadowRoot.activeElement;
    return el;
  }

  /** The first tabbable element inside a light-DOM subtree, looking into shadow roots. */
  function firstFocusable(root) {
    const SEL = 'a[href], button:not([disabled]), input:not([disabled]):not([type=hidden]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"]), [contenteditable="true"], [contenteditable="plaintext-only"]';
    const walk = (node) => {
      for (const el of node.querySelectorAll('*')) {
        if (el.matches(SEL) && el.getClientRects().length) return el;
        if (el.shadowRoot) { const inner = walk(el.shadowRoot); if (inner) return inner; }
      }
      return null;
    };
    return walk(root);
  }

  // <nk-sidebar slot="sidebar">
  //   <nk-workspace-switcher slot="workspace" name="Acme"></nk-workspace-switcher>
  //   <nk-tree>…</nk-tree>
  //   <nk-tree-item slot="footer" icon="⚙️">Settings</nk-tree-item>
  // </nk-sidebar>
  // → <aside class="nk-sidebar"><div class="nk-workspace">…</div>
  //     <div class="nk-sidebar-scroll">…</div><div class="nk-sidebar-footer">…</div></aside>
  //
  // The host is display:contents so the <aside> is a direct flex child of
  // .nk-app, exactly like the class markup. Below 860px notionkit.css hides the
  // aside; `open` brings it back as a drawer over the page. The drawer is the
  // stylesheet's since NotionKit 1.7.0 (.nk-sidebar.open, .nk-sidebar-backdrop
  // .open) – the sidebar slides and the scrim fades by the same rules as the
  // class markup; the element toggles the two classes. <nk-btn variant="sidebar">
  // in the topbar is the ☰ that opens it.
  //
  // `collapsible` (NotionKit 1.10.0): on the desktop Notion's « beside the
  // workspace row collapses the sidebar – it slides out to the left, the main
  // column takes the width, nothing in it takes focus – and the topbar's ☰
  // brings it back; ⌘\ or Ctrl+\ toggles it (on a phone the drawer).
  // `collapsed` is the state, collapse() / expand() / toggleCollapsed() the
  // API, nk-collapse { collapsed } the moment to keep it. collapse-label names
  // the « (default "Close sidebar") and is its tooltip for <nk-tooltip>.
  const COLLAPSE_ICON = '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="m18 17-5-5 5-5M11 17l-5-5 5-5"/></svg>';
  const phone$1 = () => matchMedia('(max-width: 860px)').matches;

  class NkSidebar extends NkElement {
    static get observedAttributes() { return ['open', 'collapsible', 'collapsed', 'collapse-label']; }

    render() {
      this._backdrop = this.createElement('div', ['nk-sidebar-backdrop']);
      this._aside = this.createElement('aside', ['nk-sidebar']);
      const head = this.createElement('div', ['nk-sidebar-head']);
      this._collapse = this.createElement('button', ['nk-sidebar-collapse'], { type: 'button', 'data-tooltip-key': '⌘\\' });
      this._collapse.innerHTML = COLLAPSE_ICON;
      head.append(this.createElement('slot', [], { name: 'workspace' }), this._collapse);
      this._aside.appendChild(head);
      const scroll = this.createElement('div', ['nk-sidebar-scroll']);
      scroll.appendChild(document.createElement('slot'));
      this._aside.appendChild(scroll);
      this._footer = this.createElement('div', ['nk-sidebar-footer']);
      this._footerSlot = this.createElement('slot', [], { name: 'footer' });
      this._footer.appendChild(this._footerSlot);
      this._aside.appendChild(this._footer);
      this._wrapper.appendChild(this._backdrop);
      this._wrapper.appendChild(this._aside);
      this._syncFooter();
      this._syncOpen();
      this._syncCollapse();
    }

    _syncCollapse() {
      const label = this.getAttribute('collapse-label') || 'Close sidebar';
      this._collapse.hidden = !this.getBoolAttr('collapsible');
      this._collapse.setAttribute('aria-label', label);
      this._collapse.dataset.tooltip = label;
      this._aside.classList.toggle('collapsed', this.getBoolAttr('collapsed'));
    }

    /** The ☰ that brings the sidebar back: the one in its own app, else the page's. */
    _toggleButton() {
      const sel = 'nk-btn[variant="sidebar"]';
      return this.closest('nk-app')?.querySelector(sel) ?? document.querySelector(sel);
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      this._aside.classList.toggle('open', open);
      this._backdrop.classList.toggle('open', open);
    }

    _syncFooter() {
      const nodes = this._footerSlot.assignedElements();
      // The footer box carries padding and a border; only render it when used.
      this._footer.style.display = nodes.length ? '' : 'none';
      // Footer rows are 26px in the class version (.nk-sidebar-footer .nk-tree-item).
      for (const el of nodes) if (el.localName === 'nk-tree-item') el.setAttribute('compact', '');
    }

    setupEvents() {
      this._onSlot = () => this._syncFooter();
      this._onBackdrop = () => this.close();
      this._onKey = (e) => {
        if (e.key === 'Escape' && this.getBoolAttr('open')) this.close();
        // Notion's ⌘\: the collapse on the desktop, the drawer on a phone.
        if (e.key === '\\' && (e.metaKey || e.ctrlKey) && this.getBoolAttr('collapsible') && !e.defaultPrevented) {
          e.preventDefault();
          phone$1() ? this.toggle() : this.toggleCollapsed();
        }
      };
      this._onCollapse = () => this.collapse();
      this._footerSlot.addEventListener('slotchange', this._onSlot);
      this._backdrop.addEventListener('click', this._onBackdrop);
      this._collapse.addEventListener('click', this._onCollapse);
      document.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._footerSlot?.removeEventListener('slotchange', this._onSlot);
      this._backdrop?.removeEventListener('click', this._onBackdrop);
      this._collapse?.removeEventListener('click', this._onCollapse);
      document.removeEventListener('keydown', this._onKey);
    }

    onAttributeChanged(name) {
      if (name === 'open') {
        this._syncOpen();
        this.emit('nk-toggle', { open: this.getBoolAttr('open') });
        return;
      }
      // Focus follows to the control that is left: the ☰ after collapsing, the « after expanding.
      const active = deepActiveElement();
      const fromSidebar = active && (this.contains(active) || this._shadow.contains(active));
      const fromToggle = active?.closest?.('.nk-sidebar-toggle');
      this._syncCollapse();
      if (name !== 'collapsed') return;
      const collapsed = this.getBoolAttr('collapsed');
      this.emit('nk-collapse', { collapsed });
      // Two frames later: with reduced motion every nk- element transitions for
      // .01ms, the inherited visibility included, so the « is focusable only then.
      const later = fn => requestAnimationFrame(() => requestAnimationFrame(fn));
      if (collapsed && fromSidebar) later(() => this._toggleButton()?.focus({ preventScroll: true }));
      if (!collapsed && fromToggle && !this._collapse.hidden) later(() => this._collapse.focus({ preventScroll: true }));
    }

    show() { this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }

    collapse() { this.setBoolAttr('collapsed', true); }
    expand() { this.setBoolAttr('collapsed', false); }
    toggleCollapsed() { this.setBoolAttr('collapsed', !this.getBoolAttr('collapsed')); }
    get collapsed() { return this.getBoolAttr('collapsed'); }
    set collapsed(v) { this.setBoolAttr('collapsed', v); }
  }

  customElements.define('nk-sidebar', NkSidebar);

  // <nk-workspace-switcher slot="workspace" name="Acme" avatar="A">
  //   <nk-menu slot="menu">…</nk-menu>
  // </nk-workspace-switcher>
  // → <div class="nk-workspace"><div class="avatar">A</div><span>Acme</span><span class="chev">⌄</span></div>
  // A click toggles `open` and shows whatever sits in the menu slot below the
  // row; clicking outside or pressing Escape closes it.
  const menuSheet = new CSSStyleSheet();
  menuSheet.replaceSync(`
  .nk-ws-anchor { position: relative; flex: 1 1 auto; min-width: 0; }
  .nk-ws-menu { display: none; position: absolute; left: 8px; top: calc(100% - 2px); z-index: 50; }
  :host([open]) .nk-ws-menu { display: block; }
`);

  class NkWorkspaceSwitcher extends NkElement {
    static get hostStyles() { return menuSheet; }
    static get observedAttributes() { return ['name', 'avatar', 'open']; }

    render() {
      this._row = this.createElement('div', ['nk-workspace'], { role: 'button', tabindex: '0', 'aria-haspopup': 'menu' });
      this._avatar = this.createElement('div', ['avatar']);
      const avatarSlot = this.createElement('slot', [], { name: 'avatar' });
      avatarSlot.appendChild(this._avatar);
      this._name = document.createElement('span');
      const chev = this.createElement('span', ['chev']);
      chev.textContent = '⌄';
      this._row.append(avatarSlot, this._name, chev);
      this._menu = this.createElement('div', ['nk-ws-menu']);
      this._menu.appendChild(this.createElement('slot', [], { name: 'menu' }));
      const anchor = this.createElement('div', ['nk-ws-anchor']);
      anchor.append(this._row, this._menu);
      this._wrapper.appendChild(anchor);
      this._sync();
    }

    _sync() {
      const name = this.getAttribute('name') || '';
      this._name.textContent = name;
      this._avatar.textContent = this.getAttribute('avatar') || name.trim().charAt(0).toUpperCase();
      this._row.setAttribute('aria-expanded', this.getBoolAttr('open') ? 'true' : 'false');
    }

    setupEvents() {
      this._onClick = () => this.toggle();
      this._onKey = (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); this.toggle(); } };
      this._onDocClick = (e) => { if (this.getBoolAttr('open') && !e.composedPath().includes(this)) this.close(); };
      this._onDocKey = (e) => { if (e.key === 'Escape' && this.getBoolAttr('open')) this.close(); };
      this._onMenuSelect = () => this.close();
      this._row.addEventListener('click', this._onClick);
      this._row.addEventListener('keydown', this._onKey);
      this._menu.addEventListener('nk-select', this._onMenuSelect);
      document.addEventListener('click', this._onDocClick);
      document.addEventListener('keydown', this._onDocKey);
    }

    teardownEvents() {
      this._row?.removeEventListener('click', this._onClick);
      this._row?.removeEventListener('keydown', this._onKey);
      this._menu?.removeEventListener('nk-select', this._onMenuSelect);
      document.removeEventListener('click', this._onDocClick);
      document.removeEventListener('keydown', this._onDocKey);
    }

    onAttributeChanged(name) {
      this._sync();
      if (name === 'open') this.emit('nk-toggle', { open: this.getBoolAttr('open') });
    }

    show() { this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }

    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
  }

  customElements.define('nk-workspace-switcher', NkWorkspaceSwitcher);

  // <nk-section-label addable>Favourites</nk-section-label>
  // → <div class="nk-section-label">Favourites <span class="plus">＋</span></div>
  // The ＋ appears on hover (a hover-parent rule, so it lives in the same tree)
  // and fires nk-action { action: 'add' }.
  class NkSectionLabel extends NkElement {
    static get observedAttributes() { return ['label', 'addable']; }

    render() {
      this._box = this.createElement('div', ['nk-section-label']);
      this._text = document.createElement('span');
      this._text.appendChild(document.createElement('slot'));
      this._plus = this.createElement('span', ['plus'], { role: 'button', title: '+' });
      this._plus.textContent = '＋';
      this._box.append(this._text, this._plus);
      this._wrapper.appendChild(this._box);
      this._sync();
    }

    _sync() {
      const label = this.getAttribute('label');
      if (label !== null) this._text.textContent = label;
      this._plus.style.display = this.getBoolAttr('addable') ? '' : 'none';
    }

    setupEvents() {
      this._onPlus = (e) => { e.stopPropagation(); this.emit('nk-action', { action: 'add' }); };
      this._plus.addEventListener('click', this._onPlus);
    }

    teardownEvents() { this._plus?.removeEventListener('click', this._onPlus); }

    onAttributeChanged() { this._sync(); }

    get addable() { return this.getBoolAttr('addable'); }
    set addable(v) { this.setBoolAttr('addable', v); }
  }

  customElements.define('nk-section-label', NkSectionLabel);

  // <nk-tree>
  //   <nk-section-label addable>Favourites</nk-section-label>
  //   <nk-tree-item icon="📊" open>Project overview
  //     <nk-tree-item icon="🚀" active>NotionKit MVP</nk-tree-item>
  //   </nk-tree-item>
  // </nk-tree>
  //
  // A container for tree items: keeps a single `active` item (listening to
  // nk-select from any depth), gives the group one tab stop with arrow-key
  // navigation, and can render items from data:
  //   tree.data = [{ label, icon, value, href, open, active, children: [...] }]
  const KEYS$2 = new Set(['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'Home', 'End']);

  class NkTree extends NkElement {
    static get observedAttributes() { return ['manual']; }

    render() {
      const box = this.createElement('div', [], { role: 'tree' });
      box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(box);
      this._box = box;
    }

    /** Makes `item` the single active item (called by the item after nk-select was not cancelled). */
    setActive(item) {
      if (this.getBoolAttr('manual')) return;
      for (const other of this.querySelectorAll('nk-tree-item[active]')) if (other !== item) other.removeAttribute('active');
      item.setAttribute('active', '');
      this._syncTabIndex(item);
    }

    setupEvents() {
      this._onKey = (e) => {
        if (!KEYS$2.has(e.key) || e.ctrlKey || e.metaKey || e.altKey) return;
        const item = e.target.closest?.('nk-tree-item');
        if (!item) return;
        const visible = this.visibleItems();
        const i = visible.indexOf(item);
        if (i < 0) return;
        let next = null;
        switch (e.key) {
          case 'ArrowDown': next = visible[i + 1]; break;
          case 'ArrowUp': next = visible[i - 1]; break;
          case 'Home': next = visible[0]; break;
          case 'End': next = visible[visible.length - 1]; break;
          case 'ArrowRight':
            if (item.hasChildren && !item.open) item.open = true;
            else if (item.hasChildren) next = item.querySelector('nk-tree-item');
            break;
          case 'ArrowLeft':
            if (item.hasChildren && item.open) item.open = false;
            else next = item.parentElement?.closest('nk-tree-item');
            break;
        }
        e.preventDefault();
        if (next) { this._syncTabIndex(next); next.focus(); }
      };
      this._onSlot = () => this._syncTabIndex();
      this.addEventListener('keydown', this._onKey);
      this._box.querySelector('slot').addEventListener('slotchange', this._onSlot);
      this._syncTabIndex();
    }

    teardownEvents() {
      this.removeEventListener('keydown', this._onKey);
      this._box?.querySelector('slot').removeEventListener('slotchange', this._onSlot);
    }

    /** Items whose every ancestor item inside this tree is open – i.e. rendered. */
    visibleItems() {
      return [...this.querySelectorAll('nk-tree-item')].filter(item => {
        for (let p = item.parentElement?.closest('nk-tree-item'); p && this.contains(p); p = p.parentElement?.closest('nk-tree-item')) {
          if (!p.open) return false;
        }
        return true;
      });
    }

    /** Items upgrade after the tree; each one asks for a resync once rendered. */
    requestSync() {
      if (this._syncQueued) return;
      this._syncQueued = true;
      queueMicrotask(() => { this._syncQueued = false; this._syncTabIndex(); });
    }

    _syncTabIndex(focusable) {
      const items = [...this.querySelectorAll('nk-tree-item')];
      if (!items.length) return;
      const visible = this.visibleItems();
      const target = focusable || visible.find(i => i.active) || visible[0];
      // A method call, not a property write: assigning a property to an element
      // that has not been upgraded yet would create an own property that shadows
      // the accessor forever.
      for (const item of items) item.setTabbable?.(item === target);
    }

    /** Renders items from data into the light DOM (replacing existing items). */
    set data(list) {
      this._data = list;
      const build = (nodes) => nodes.map(n => {
        const item = document.createElement('nk-tree-item');
        if (n.icon) item.setAttribute('icon', n.icon);
        if (n.value != null) item.setAttribute('value', n.value);
        if (n.href) item.setAttribute('href', n.href);
        if (n.open) item.setAttribute('open', '');
        if (n.active) item.setAttribute('active', '');
        item.append(document.createTextNode(n.label ?? ''), ...build(n.children || []));
        return item;
      });
      for (const old of this.querySelectorAll(':scope > nk-tree-item')) old.remove();
      this.append(...build(list || []));
    }
    get data() { return this._data; }

    get activeItem() { return this.querySelector('nk-tree-item[active]'); }
    get value() { return this.activeItem?.value ?? null; }
  }

  customElements.define('nk-tree', NkTree);

  // <nk-tree-item icon="📊" value="overview" open>
  //   Project overview
  //   <nk-tree-item icon="🚀" value="mvp" active>NotionKit MVP</nk-tree-item>
  // </nk-tree-item>
  //
  // → <div class="nk-tree-item active"><span class="nk-toggle-arrow open">▸</span>
  //     <span class="icon">📊</span><span class="label">…</span>
  //     <span class="actions"><span>＋</span><span>⋯</span></span></div>
  //   <div class="nk-tree-children">…</div>
  //
  // Both halves – the row and the .nk-tree-children box that must be its next
  // sibling – are rendered here. Slots are assigned manually: text becomes the
  // label, nested <nk-tree-item>s become the children, `slot="icon"` /
  // `slot="end"` nodes go where they say. The hover actions are shadow-internal
  // (`.nk-tree-item:hover .actions` is a hover-parent rule) and report through
  // nk-action.
  class NkTreeItem extends NkElement {
    static get slotAssignment() { return 'manual'; }
    static get observedAttributes() { return ['label', 'icon', 'value', 'href', 'active', 'open', 'compact', 'no-actions']; }

    render() {
      this._row = this.createElement('div', ['nk-tree-item'], { role: 'treeitem', tabindex: '-1' });
      this._arrow = this.createElement('span', ['nk-toggle-arrow'], { 'aria-hidden': 'true' });
      this._arrow.textContent = '▸';
      this._iconSlot = this.createElement('slot', [], { name: 'icon' });
      this._icon = this.createElement('span', ['icon']);
      this._iconSlot.appendChild(this._icon);
      this._label = this.createElement('span', ['label']);
      this._labelSlot = this.createElement('slot', [], { name: 'label' });
      this._labelText = document.createTextNode('');
      this._labelSlot.appendChild(this._labelText);
      this._label.appendChild(this._labelSlot);
      this._actions = this.createElement('span', ['actions']);
      for (const [action, glyph, title] of [['add', '＋', 'Add'], ['more', '⋯', 'More']]) {
        const b = this.createElement('span', [], { 'data-action': action, role: 'button', title });
        b.textContent = glyph;
        this._actions.appendChild(b);
      }
      this._endSlot = this.createElement('slot', [], { name: 'end' });
      this._row.append(this._arrow, this._iconSlot, this._label, this._actions, this._endSlot);
      this._children = this.createElement('div', ['nk-tree-children'], { role: 'group' });
      this._childSlot = this.createElement('slot', [], { name: 'children' });
      this._children.appendChild(this._childSlot);
      this._wrapper.append(this._row, this._children);
      this._sync();
      this.closest('nk-tree')?.requestSync?.();
    }

    /** Routes light-DOM nodes: nested items → children, slot="icon"/"end" → there, the rest → label. */
    assignSlots() {
      if (!this._row) return;
      const icon = [], end = [], children = [], label = [];
      for (const node of this.childNodes) {
        if (node.nodeType === Node.ELEMENT_NODE) {
          const slot = node.getAttribute('slot');
          if (slot === 'icon') icon.push(node);
          else if (slot === 'end') end.push(node);
          else if (node.localName === 'nk-tree-item') children.push(node);
          else label.push(node);
        } else if (node.nodeType === Node.TEXT_NODE && node.data.trim()) {
          label.push(node);
        }
      }
      this._iconSlot.assign(...icon);
      this._endSlot.assign(...end);
      this._childSlot.assign(...children);
      this._labelSlot.assign(...(this.hasAttribute('label') ? [] : label));
      this._hasChildren = children.length > 0;
      this._hasEnd = end.length > 0;
      this._sync();
    }

    _sync() {
      const active = this.getBoolAttr('active'), open = this.getBoolAttr('open');
      this._row.classList.toggle('active', active);
      this._row.classList.toggle('compact', this.getBoolAttr('compact'));
      this._row.setAttribute('aria-selected', active ? 'true' : 'false');
      this._arrow.classList.toggle('open', open);
      this._arrow.style.display = this._hasChildren ? '' : 'none';
      if (this._hasChildren) this._row.setAttribute('aria-expanded', open ? 'true' : 'false');
      else this._row.removeAttribute('aria-expanded');
      this._children.classList.toggle('collapsed', !open || !this._hasChildren);
      this._icon.textContent = this.getAttribute('icon') || '';
      this._icon.style.display = this.getAttribute('icon') ? '' : 'none';
      this._labelText.data = this.getAttribute('label') || '';
      // .actions is display:none until hover (a hover-parent rule); `hidden`
      // would lose to that rule, so the block is detached instead.
      const wantActions = !this.getBoolAttr('no-actions') && !this._hasEnd;
      if (wantActions && !this._actions.isConnected) this._row.insertBefore(this._actions, this._endSlot);
      if (!wantActions && this._actions.isConnected) this._actions.remove();
      const href = this.getAttribute('href');
      href ? this._row.setAttribute('data-href', href) : this._row.removeAttribute('data-href');
    }

    setupEvents() {
      this._onRowClick = (e) => {
        const action = e.target.closest?.('[data-action]');
        if (action) { e.stopPropagation(); this.emit('nk-action', { action: action.dataset.action, value: this.value, label: this.label }); return; }
        if (e.target === this._arrow || this._arrow.contains(e.target)) { e.stopPropagation(); this.toggle(); return; }
        this.select();
      };
      this._onKey = (e) => {
        if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); this.select(); }
      };
      this._row.addEventListener('click', this._onRowClick);
      this._row.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._row?.removeEventListener('click', this._onRowClick);
      this._row?.removeEventListener('keydown', this._onKey);
    }

    onAttributeChanged(name) {
      if (name === 'label') this.assignSlots();
      else this._sync();
      if (name === 'open') this.emit('nk-toggle', { open: this.getBoolAttr('open'), value: this.value });
    }

    /** Fires nk-select; a listener may preventDefault to keep the item from becoming active. */
    select() {
      const ok = this.emit('nk-select', { value: this.value, label: this.label, href: this.getAttribute('href'), item: this });
      if (!ok) return;
      const tree = this.closest('nk-tree');
      if (tree) tree.setActive(this);
      else this.setBoolAttr('active', true);
      if (this.getAttribute('href')) location.assign(this.getAttribute('href'));
    }

    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }
    focus(options) { this._row?.focus(options); }
    /** Roving tabindex, managed by <nk-tree>. */
    setTabbable(v) { if (this._row) this._row.tabIndex = v ? 0 : -1; }
    get tabbable() { return this._row?.tabIndex === 0; }
    get hasChildren() { return !!this._hasChildren; }

    get label() { return this.getAttribute('label') ?? this._labelSlot?.assignedNodes().map(n => n.textContent).join('').trim() ?? ''; }
    set label(v) { this.setAttribute('label', v); }
    get value() { return this.getAttribute('value') ?? this.label; }
    set value(v) { this.setAttribute('value', v); }
    get icon() { return this.getAttribute('icon'); }
    set icon(v) { v == null ? this.removeAttribute('icon') : this.setAttribute('icon', v); }
    get active() { return this.getBoolAttr('active'); }
    set active(v) { this.setBoolAttr('active', v); }
    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get compact() { return this.getBoolAttr('compact'); }
    set compact(v) { this.setBoolAttr('compact', v); }
  }

  customElements.define('nk-tree-item', NkTreeItem);

  // <nk-topbar>
  //   <nk-breadcrumb>…</nk-breadcrumb>
  //   <nk-btn slot="actions" variant="share">Share</nk-btn>
  //   <nk-theme-toggle slot="actions"></nk-theme-toggle>
  // </nk-topbar>
  // → <div class="nk-topbar">…<div class="nk-topbar-actions">…</div></div>
  // display:contents host, so the bar is a direct flex child of .nk-main.
  class NkTopbar extends NkElement {

    render() {
      const bar = this.createElement('div', ['nk-topbar']);
      bar.appendChild(document.createElement('slot'));
      const actions = this.createElement('div', ['nk-topbar-actions']);
      actions.appendChild(this.createElement('slot', [], { name: 'actions' }));
      bar.appendChild(actions);
      this._wrapper.appendChild(bar);
    }
  }

  customElements.define('nk-topbar', NkTopbar);

  // <nk-breadcrumb>
  //   <a href="/overview">📊 Project overview</a>
  //   <span>🚀 NotionKit MVP</span>
  // </nk-breadcrumb>
  // → <nav class="nk-breadcrumb"><a class="crumb">…</a><span class="sep">/</span><span class="crumb current">…</span></nav>
  //
  // The children are *cloned* into the shadow tree so the separators can be
  // interleaved and `.crumb:hover` (no ::slotted twin) applies. A
  // MutationObserver keeps the copy in step. Clicking a clone fires nk-select
  // and, unless prevented, forwards the click to the original – so an <a>
  // navigates exactly once and framework listeners see it.
  class NkBreadcrumb extends NkElement {
    static get observedAttributes() { return ['separator']; }
    static get observesLightDom() { return true; }

    render() {
      this._nav = this.createElement('nav', ['nk-breadcrumb'], { 'aria-label': 'Breadcrumb' });
      this._wrapper.appendChild(this._nav);
      this.projectLightDom();
    }

    projectLightDom() {
      if (!this._nav) return;
      const originals = [...this.children];
      const sep = this.getAttribute('separator') ?? '/';
      const signature = originals.map(o => o.outerHTML).join('') + sep;
      if (signature === this._signature) return;
      this._signature = signature;
      this._nav.innerHTML = '';
      const explicitCurrent = originals.some(o => o.hasAttribute('current') || o.classList.contains('current'));
      originals.forEach((original, i) => {
        if (i > 0) {
          const s = this.createElement('span', ['sep'], { 'aria-hidden': 'true' });
          s.textContent = sep;
          this._nav.appendChild(s);
        }
        const clone = original.cloneNode(true);
        clone.removeAttribute('slot');
        clone.classList.add('crumb');
        const current = explicitCurrent ? (original.hasAttribute('current') || original.classList.contains('current')) : i === originals.length - 1;
        clone.classList.toggle('current', current);
        if (current) clone.setAttribute('aria-current', 'page');
        clone.dataset.index = i;
        this._nav.appendChild(clone);
      });
    }

    setupEvents() {
      this._onClick = (e) => {
        const clone = e.target.closest('.crumb');
        if (!clone) return;
        e.preventDefault();
        const index = Number(clone.dataset.index);
        // Look the original up at click time: a framework may have replaced it.
        const original = this.children[index];
        const ok = this.emit('nk-select', {
          index, label: clone.textContent.trim(), href: original?.getAttribute('href') ?? null,
          value: original?.dataset.value ?? original?.getAttribute('href') ?? clone.textContent.trim(),
          current: clone.classList.contains('current'),
        });
        if (ok) original?.click();
      };
      this._nav.addEventListener('click', this._onClick);
    }

    teardownEvents() { this._nav?.removeEventListener('click', this._onClick); }

    onAttributeChanged() { this.projectLightDom(); }

    get separator() { return this.getAttribute('separator') ?? '/'; }
    set separator(v) { this.setAttribute('separator', v); }
  }

  customElements.define('nk-breadcrumb', NkBreadcrumb);

  // <nk-theme-toggle slot="actions"></nk-theme-toggle>
  // → <button class="nk-topbar-btn nk-theme-toggle">🌙</button>
  // Flips data-theme on <html> (the only theme source), remembers the choice in
  // localStorage (`storage-key`, default nk-theme), applies a stored or
  // system preference on first connect when <html> carries no theme yet, and
  // accepts postMessage({ nkTheme }) from a parent page (the landing page's
  // embedded demo uses that).
  class NkThemeToggle extends NkElement {
    static get observedAttributes() { return ['storage-key', 'title'];

    }

    render() {
      this._btn = this.createElement('button', ['nk-topbar-btn', 'nk-theme-toggle'], { type: 'button' });
      this._btn.title = this.getAttribute('title') || 'Toggle light / dark';
      this._wrapper.appendChild(this._btn);
      const root = document.documentElement;
      let stored = null;
      try { stored = localStorage.getItem(this.storageKey); } catch {}
      if (stored === 'light' || stored === 'dark') root.setAttribute('data-theme', stored);
      else if (!root.hasAttribute('data-theme')) root.setAttribute('data-theme', matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      this._syncGlyph();
    }

    _syncGlyph() {
      if (this._btn) this._btn.textContent = getCurrentTheme() === 'dark' ? '☀️' : '🌙';
    }

    _syncTheme(theme) {
      super._syncTheme(theme);
      this._syncGlyph();
    }

    setupEvents() {
      this._onClick = () => this.apply(getCurrentTheme() === 'dark' ? 'light' : 'dark');
      this._onMessage = (e) => { const t = e.data?.nkTheme; if (t === 'light' || t === 'dark') this.apply(t); };
      this._btn.addEventListener('click', this._onClick);
      window.addEventListener('message', this._onMessage);
    }

    teardownEvents() {
      this._btn?.removeEventListener('click', this._onClick);
      window.removeEventListener('message', this._onMessage);
    }

    onAttributeChanged(name) {
      if (name === 'title' && this._btn) this._btn.title = this.getAttribute('title') || 'Toggle light / dark';
    }

    /** Sets the theme on <html>, persists it and reports it. */
    apply(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      try { localStorage.setItem(this.storageKey, theme); } catch {}
      this._syncGlyph();
      this.emit('nk-change', { value: theme });
    }

    get storageKey() { return this.getAttribute('storage-key') || 'nk-theme'; }
    get value() { return getCurrentTheme(); }
    set value(v) { this.apply(v); }
  }

  customElements.define('nk-theme-toggle', NkThemeToggle);

  // <nk-tab-bar value="inbox">
  //   <nk-tab-bar-item icon="🏠" value="home">Home</nk-tab-bar-item>
  //   <nk-tab-bar-item icon="📥" value="inbox">Inbox</nk-tab-bar-item>
  //   <nk-tab-bar-item icon="☰" drawer>More</nk-tab-bar-item>
  // </nk-tab-bar>
  // → <nav class="nk-tab-bar">…</nav>
  //
  // The thumb-reachable twin of the sidebar for phones and installed PWAs. As
  // the last child of <nk-app> it is slotted into .nk-main below the scrolling
  // page, so it never moves and needs no bottom padding anywhere. The
  // stylesheet hides it above 860px – there the sidebar is the navigation –
  // and shows it below; `always` shows it at every width (previews, phone
  // frames), `fixed` pins it to the viewport (standalone PWAs) with a spacer
  // holding its place, `floating` makes it a capsule. Exactly one item is `active`; a
  // `drawer` item opens the sidebar instead of becoming active.
  class NkTabBar extends NkElement {
    static get observedAttributes() { return ['value', 'always', 'fixed', 'floating', 'label']; }

    render() {
      this._bar = this.createElement('nav', ['nk-tab-bar']);
      this._slot = document.createElement('slot');
      this._bar.appendChild(this._slot);
      // With `fixed` the bar leaves the flow; the spacer (styled by the
      // stylesheet, shown only then) keeps its height so the page ends above it.
      this._spacer = this.createElement('div', ['nk-tab-bar-spacer']);
      this._wrapper.append(this._bar, this._spacer);
      this._syncBar();
      this._sync();
    }

    get items() { return [...this.querySelectorAll(':scope > nk-tab-bar-item')]; }

    // Attribute access only: on the first connect the children are in the DOM
    // but not yet upgraded, so their accessors do not exist.
    _valueOf(item) { return item.getAttribute('value') ?? item.getAttribute('label') ?? item.textContent.trim(); }

    _syncBar() {
      this._bar.classList.toggle('always', this.getBoolAttr('always'));
      this._bar.classList.toggle('fixed', this.getBoolAttr('fixed'));
      this._bar.classList.toggle('floating', this.getBoolAttr('floating'));
      const label = this.getAttribute('label');
      label ? this._bar.setAttribute('aria-label', label) : this._bar.removeAttribute('aria-label');
    }

    _sync() {
      const items = this.items.filter(i => !i.hasAttribute('drawer'));
      if (!items.length) return;
      let value = this.getAttribute('value');
      if (value === null || !items.some(i => this._valueOf(i) === value)) {
        const preset = items.find(i => i.hasAttribute('active')) || items[0];
        value = this._valueOf(preset);
      }
      for (const item of items) {
        if (this._valueOf(item) === value) item.setAttribute('active', ''); else item.removeAttribute('active');
      }
      this._value = value;
    }

    setupEvents() {
      this._onSlot = () => this._sync();
      this._slot.addEventListener('slotchange', this._onSlot);
      this._sync();
    }

    teardownEvents() {
      this._slot?.removeEventListener('slotchange', this._onSlot);
    }

    onAttributeChanged(name) {
      if (name !== 'value') { this._syncBar(); return; }
      const before = this._value;
      this._sync();
      if (this._value !== before) this.emit('nk-change', { value: this._value });
    }

    get value() { return this._value ?? this.getAttribute('value'); }
    set value(v) { this.setAttribute('value', v); }
    get always() { return this.getBoolAttr('always'); }
    set always(v) { this.setBoolAttr('always', v); }
    get fixed() { return this.getBoolAttr('fixed'); }
    set fixed(v) { this.setBoolAttr('fixed', v); }
    get floating() { return this.getBoolAttr('floating'); }
    set floating(v) { this.setBoolAttr('floating', v); }
  }

  customElements.define('nk-tab-bar', NkTabBar);

  // <nk-tab-bar-item icon="📥" value="inbox" active>Inbox</nk-tab-bar-item>
  // → <button class="nk-tab-bar-item active" type="button">
  //     <span class="icon">📥</span><span class="label">Inbox</span></button>
  //
  // One destination of <nk-tab-bar>. A click emits nk-select (cancelable);
  // inside a bar it then moves the bar's `value`, standalone it sets its own
  // `active`. With `href` it navigates afterwards, with `drawer` it opens the
  // nearest <nk-sidebar> as a drawer instead of becoming active – the usual
  // fifth item ("More"). The icon is an attribute or a `slot="icon"` node.
  class NkTabBarItem extends NkElement {
    static get observedAttributes() { return ['icon', 'label', 'value', 'href', 'active', 'disabled', 'drawer']; }

    render() {
      this._btn = this.createElement('button', ['nk-tab-bar-item'], { type: 'button' });
      this._iconSlot = this.createElement('slot', [], { name: 'icon' });
      this._icon = this.createElement('span', ['icon'], { 'aria-hidden': 'true' });
      this._iconSlot.appendChild(this._icon);
      this._label = this.createElement('span', ['label']);
      this._labelSlot = document.createElement('slot');
      this._labelText = document.createTextNode('');
      this._labelSlot.appendChild(this._labelText);
      this._label.appendChild(this._labelSlot);
      this._btn.append(this._iconSlot, this._label);
      this._wrapper.appendChild(this._btn);
      this._sync();
    }

    _sync() {
      this._icon.textContent = this.getAttribute('icon') ?? '';
      this._labelText.data = this.getAttribute('label') ?? '';
      const active = this.getBoolAttr('active');
      this._btn.classList.toggle('active', active);
      active ? this._btn.setAttribute('aria-current', 'page') : this._btn.removeAttribute('aria-current');
      this._btn.disabled = this.getBoolAttr('disabled');
      const href = this.getAttribute('href');
      href ? this._btn.setAttribute('data-href', href) : this._btn.removeAttribute('data-href');
    }

    setupEvents() {
      this._onClick = () => this.select();
      this._btn.addEventListener('click', this._onClick);
    }

    teardownEvents() {
      this._btn?.removeEventListener('click', this._onClick);
    }

    select() {
      if (this.getBoolAttr('disabled')) return;
      const href = this.getAttribute('href');
      const ok = this.emit('nk-select', { value: this.value, label: this.label, href, item: this, drawer: this.getBoolAttr('drawer') });
      if (!ok) return;
      if (this.getBoolAttr('drawer')) {
        const sidebar = this.closest('nk-app')?.querySelector('nk-sidebar') ?? document.querySelector('nk-sidebar');
        sidebar?.toggle?.();
        return;
      }
      const bar = this.closest('nk-tab-bar');
      if (bar) bar.value = this.value;
      else this.setBoolAttr('active', true);
      if (href) location.assign(href);
    }

    onAttributeChanged() { this._sync(); }
    focus(o) { this._btn?.focus(o); }

    get value() { return this.getAttribute('value') ?? this.label; }
    set value(v) { this.setAttribute('value', v); }
    get label() { return this.getAttribute('label') ?? this.textContent.trim(); }
    set label(v) { this.setAttribute('label', v); }
    get active() { return this.getBoolAttr('active'); }
    set active(v) { this.setBoolAttr('active', v); }
    get disabled() { return this.getBoolAttr('disabled'); }
    set disabled(v) { this.setBoolAttr('disabled', v); }
    get drawer() { return this.getBoolAttr('drawer'); }
    set drawer(v) { this.setBoolAttr('drawer', v); }
  }

  customElements.define('nk-tab-bar-item', NkTabBarItem);

  // <nk-page icon="🚀" cover full small>
  //   <nk-page-title>NotionKit MVP</nk-page-title>
  //   <nk-page-actions>…</nk-page-actions>
  //   <p class="lead">…</p>
  //   …blocks…
  // </nk-page>
  // → <div class="nk-page-scroll"><div class="nk-cover"></div><div class="nk-page">
  //     <div class="nk-page-icon">🚀</div>…</div></div>
  //
  // The host is display:contents, so .nk-page-scroll is the flex child of
  // .nk-main that scrolls. `narrow` drops the scroll wrapper for pages that are
  // the document itself (landing / docs skeleton). The page icon is rendered
  // here because its ::slotted twin is keyed on the parent (.nk-page). `full` and
  // `small` are Notion's page options – full width, 14px text – set on the page.
  class NkPage extends NkElement {
    static get observedAttributes() { return ['icon', 'cover', 'narrow', 'full', 'small']; }

    render() {
      this._page = this.createElement('div', ['nk-page']);
      this._icon = this.createElement('div', ['nk-page-icon'], { role: 'button', title: 'Change icon' });
      const iconSlot = this.createElement('slot', [], { name: 'icon' });
      iconSlot.appendChild(this._icon);
      this._page.appendChild(iconSlot);
      this._page.appendChild(document.createElement('slot'));
      this._cover = this.createElement('div', ['nk-cover']);
      this._coverSlot = this.createElement('slot', [], { name: 'cover' });
      this._coverSlot.appendChild(this._cover);
      this._build();
    }

    _build() {
      this._wrapper.replaceChildren();
      if (this.getBoolAttr('narrow')) {
        this._wrapper.append(this._coverSlot, this._page);
      } else {
        const scroll = this.createElement('div', ['nk-page-scroll']);
        scroll.append(this._coverSlot, this._page);
        this._wrapper.appendChild(scroll);
      }
      this._sync();
    }

    _sync() {
      const icon = this.getAttribute('icon');
      this._icon.textContent = icon || '';
      this._icon.style.display = icon ? '' : 'none';
      this._cover.style.display = this.getBoolAttr('cover') ? '' : 'none';
      // `covered` is the stylesheet's twin of `.nk-cover + .nk-page`: the icon
      // overlaps the cover and the page drops its top padding. Without a cover
      // (attribute or a slotted nk-page-cover) the icon sits in the padding.
      const slotted = this._coverSlot.assignedNodes().some(n => n.nodeType === Node.ELEMENT_NODE || n.data.trim());
      this._page.classList.toggle('covered', this.getBoolAttr('cover') || slotted);
      this._page.classList.toggle('full', this.getBoolAttr('full'));
      this._page.classList.toggle('small', this.getBoolAttr('small'));
    }

    setupEvents() {
      this._onIcon = () => this.emit('nk-action', { action: 'icon', value: this.getAttribute('icon') });
      this._onCover = () => this._sync();
      this._icon.addEventListener('click', this._onIcon);
      this._coverSlot.addEventListener('slotchange', this._onCover);
      this._sync();
    }

    teardownEvents() { this._icon?.removeEventListener('click', this._onIcon); this._coverSlot?.removeEventListener('slotchange', this._onCover); }

    onAttributeChanged(name) {
      if (name === 'narrow') this._build();
      else this._sync();
    }

    get icon() { return this.getAttribute('icon'); }
    set icon(v) { v == null ? this.removeAttribute('icon') : this.setAttribute('icon', v); }
    get cover() { return this.getBoolAttr('cover'); }
    set cover(v) { this.setBoolAttr('cover', v); }
    get full() { return this.getBoolAttr('full'); }
    set full(v) { this.setBoolAttr('full', v); }
    get small() { return this.getBoolAttr('small'); }
    set small(v) { this.setBoolAttr('small', v); }
  }

  customElements.define('nk-page', NkPage);

  // <nk-page-cover slot="cover"></nk-page-cover>            → the token gradient
  // <nk-page-cover slot="cover" src="cover.jpg" position="center 30%"></nk-page-cover>  → a picture
  // → <div class="nk-cover"><img src="cover.jpg" alt=""></div>: the picture fills
  // the band, cropped rather than stretched; `position` moves the crop, as
  // Notion's "Reposition" does. The gradient stays underneath while it loads.
  class NkPageCover extends NkElement {
    static get observedAttributes() { return ['src', 'position']; }

    render() {
      this._cover = this.createElement('div', ['nk-cover']);
      this._img = this.createElement('img', [], { alt: '' });
      this._wrapper.appendChild(this._cover);
      this._sync();
    }

    _sync() {
      const src = this.getAttribute('src');
      if (src) {
        this._img.src = src;
        this._img.style.objectPosition = this.getAttribute('position') || '';
        if (!this._img.isConnected) this._cover.appendChild(this._img);
      } else this._img.remove();
    }

    onAttributeChanged() { this._sync(); }

    get src() { return this.getAttribute('src'); }
    set src(v) { v == null ? this.removeAttribute('src') : this.setAttribute('src', v); }
  }

  customElements.define('nk-page-cover', NkPageCover);

  // <nk-page-title>NotionKit MVP</nk-page-title>          → <h1 class="nk-page-title">
  // <nk-page-title editable placeholder="Untitled">…</nk-page-title>
  // With `editable` the text is copied into the shadow heading (contenteditable
  // cannot edit slotted nodes) and nk-change fires on blur / Enter.
  class NkPageTitle extends NkElement {
    static get observedAttributes() { return ['editable', 'placeholder', 'value']; }
    static get observesLightDom() { return true; }

    render() {
      this._h1 = this.createElement('h1', ['nk-page-title']);
      this._slot = document.createElement('slot');
      this._h1.appendChild(this._slot);
      this._wrapper.appendChild(this._h1);
      this._sync();
    }

    _sync() {
      const editable = this.getBoolAttr('editable');
      if (editable) {
        if (this._slot.isConnected) this._slot.remove();
        this._h1.setAttribute('contenteditable', 'plaintext-only');
        this._h1.setAttribute('spellcheck', 'false');
        this._h1.setAttribute('role', 'textbox');
        this._h1.setAttribute('aria-label', 'Title');
        const text = this.getAttribute('value') ?? this.textContent.trim();
        if (this._h1.textContent !== text && document.activeElement !== this) this._h1.textContent = text;
        this._h1.setAttribute('data-placeholder', this.getAttribute('placeholder') || '');
      } else {
        this._h1.removeAttribute('contenteditable');
        this._h1.removeAttribute('role');
        if (this.hasAttribute('value')) { if (this._slot.isConnected) this._slot.remove(); this._h1.textContent = this.getAttribute('value'); }
        else if (!this._slot.isConnected) { this._h1.textContent = ''; this._h1.appendChild(this._slot); }
      }
    }

    projectLightDom() { if (this._h1 && !this.hasAttribute('value')) this._sync(); }

    setupEvents() {
      this._committed = this.value;
      this._onKey = (e) => { if (e.key === 'Enter') { e.preventDefault(); this._h1.blur(); } };
      this._onBlur = () => this._commit();
      this._h1.addEventListener('keydown', this._onKey);
      this._h1.addEventListener('blur', this._onBlur);
    }

    teardownEvents() {
      this._h1?.removeEventListener('keydown', this._onKey);
      this._h1?.removeEventListener('blur', this._onBlur);
    }

    _commit() {
      const value = this._h1.textContent.trim();
      if (value === this._committed) return;
      this._committed = value;
      this.emit('nk-change', { value });
    }

    onAttributeChanged() { this._sync(); }

    get value() { return this.getBoolAttr('editable') ? (this._h1?.textContent.trim() ?? '') : (this.getAttribute('value') ?? this.textContent.trim()); }
    set value(v) { this.setAttribute('value', v); }
    get editable() { return this.getBoolAttr('editable'); }
    set editable(v) { this.setBoolAttr('editable', v); }
    focus(o) { this._h1?.focus(o); }
  }

  customElements.define('nk-page-title', NkPageTitle);

  // <nk-page-actions><span>👤 Ada</span><span>📅 12 May</span><span>🏷️ <nk-tag color="purple">Design</nk-tag></span></nk-page-actions>
  // → <div class="nk-page-meta">…</div>  – the meta row under the title
  class NkPageActions extends NkElement {
    render() {
      const row = this.createElement('div', ['nk-page-meta']);
      row.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(row);
    }
  }

  customElements.define('nk-page-actions', NkPageActions);

  // <nk-block-host handle><p>Editable content lives here.</p></nk-block-host>
  // → <div class="nk-block-host"><span class="nk-block-handle">⠿</span>…</div>
  // The optical shell for editor content (hover wash, focus ring, drop target).
  // Mount an editor into the light DOM; <nk-editor> (v1.1) does that for TipTap.
  class NkBlockHost extends NkElement {
    static get observedAttributes() { return ['handle', 'drop-target']; }

    render() {
      this._box = this.createElement('div', ['nk-block-host']);
      this._handle = this.createElement('span', ['nk-block-handle'], { 'aria-hidden': 'true' });
      this._handle.textContent = '⠿';
      this._box.appendChild(this._handle);
      this._box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(this._box);
      this._sync();
    }

    _sync() {
      this._handle.style.display = this.getBoolAttr('handle') ? '' : 'none';
      this._box.classList.toggle('nk-drop-target', this.getBoolAttr('drop-target'));
    }

    onAttributeChanged() { this._sync(); }

    get handle() { return this.getBoolAttr('handle'); }
    set handle(v) { this.setBoolAttr('handle', v); }
    get dropTarget() { return this.getBoolAttr('drop-target'); }
    set dropTarget(v) { this.setBoolAttr('drop-target', v); }
  }

  customElements.define('nk-block-host', NkBlockHost);

  // <nk-banner variant="info">ℹ️ <span>Text</span><span slot="action">Open</span></nk-banner>
  // → <div class="nk-banner info">…<span class="b-action">Open</span></div>
  const VARIANTS = ['info', 'success', 'warning', 'danger'];

  class NkBanner extends NkElement {
    static get observedAttributes() { return ['variant']; }

    render() {
      this._box = this.createElement('div', this._classes(), { role: 'status' });
      this._box.appendChild(document.createElement('slot'));
      this._box.appendChild(this.createElement('slot', [], { name: 'action' }));
      this._wrapper.appendChild(this._box);
    }

    _classes() {
      const v = this.getAttribute('variant');
      return ['nk-banner', ...(VARIANTS.includes(v) ? [v] : [])];
    }

    onAttributeChanged() { if (this._box) this._box.className = this._classes().join(' '); }

    get variant() { return this.getAttribute('variant'); }
    set variant(v) { v ? this.setAttribute('variant', v) : this.removeAttribute('variant'); }
  }

  customElements.define('nk-banner', NkBanner);

  // <nk-empty icon="🗂️" title="No entries yet" desc="Create the first one."><nk-btn variant="primary" small>＋ New</nk-btn></nk-empty>
  // → <div class="nk-empty"><div class="e-icon">🗂️</div><div class="e-title">…</div><div class="e-desc">…</div><div class="e-actions">…</div></div>
  // `title` is the heading, never a tooltip – see NkElement.takeTitle(). The
  // default slot sits in .e-actions, so several buttons keep 8px apart.
  class NkEmpty extends NkElement {
    static get observedAttributes() { return ['icon', 'title', 'desc']; }

    render() {
      const box = this.createElement('div', ['nk-empty']);
      this._parts = {};
      for (const [slot, cls] of [['icon', 'e-icon'], ['title', 'e-title'], ['desc', 'e-desc']]) {
        const el = this.createElement('div', [cls]);
        const s = this.createElement('slot', [], { name: slot });
        s.appendChild(el);
        box.appendChild(s);
        this._parts[slot] = el;
      }
      const actions = this.createElement('div', ['e-actions']);
      actions.appendChild(document.createElement('slot'));
      box.appendChild(actions);
      this._wrapper.appendChild(box);
      this.takeTitle();
      this._sync();
    }

    _sync() {
      for (const [slot, el] of Object.entries(this._parts)) {
        const v = slot === 'title' ? this._titleText : this.getAttribute(slot);
        el.textContent = v || '';
        el.style.display = v ? '' : 'none';
      }
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'title' && !this.takeTitle(value)) return;
      this._sync();
    }

    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._sync(); }
  }

  customElements.define('nk-empty', NkEmpty);

  // <nk-skeleton height="18" width="60%"></nk-skeleton>
  // <nk-skeleton lines="3" widths="100%,85%,40%"></nk-skeleton>
  // → <div class="nk-skeleton" style="height:…;width:…"></div> × lines
  class NkSkeleton extends NkElement {
    static get observedAttributes() { return ['lines', 'height', 'width', 'widths']; }

    render() { this._build(); }

    _build() {
      this._wrapper.replaceChildren();
      const lines = Math.max(1, Number(this.getAttribute('lines')) || 1);
      const height = this.getAttribute('height') || '13';
      const widths = (this.getAttribute('widths') || '').split(',').map(s => s.trim()).filter(Boolean);
      const width = this.getAttribute('width');
      for (let i = 0; i < lines; i++) {
        const line = this.createElement('div', ['nk-skeleton'], { 'aria-hidden': 'true' });
        line.style.height = /^\d+(\.\d+)?$/.test(height) ? `${height}px` : height;
        const w = widths[i] ?? width;
        if (w) line.style.width = w;
        this._wrapper.appendChild(line);
      }
    }

    onAttributeChanged() { this._build(); }
  }

  customElements.define('nk-skeleton', NkSkeleton);

  // <nk-synced badge="⟳ 3 places">…</nk-synced>
  // → <div class="nk-synced"><span class="synced-badge">⟳ 3 places</span>…</div>
  class NkSynced extends NkElement {
    static get observedAttributes() { return ['badge']; }

    render() {
      const box = this.createElement('div', ['nk-synced']);
      this._badge = this.createElement('span', ['synced-badge']);
      box.appendChild(this._badge);
      box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(box);
      this._sync();
    }

    _sync() { this._badge.textContent = this.getAttribute('badge') ?? '⟳ synced'; }
    onAttributeChanged() { this._sync(); }

    get badge() { return this.getAttribute('badge'); }
    set badge(v) { this.setAttribute('badge', v); }
  }

  customElements.define('nk-synced', NkSynced);

  // <nk-tabs value="notes">
  //   <nk-tab value="notes">📝 Notes</nk-tab>
  //   <nk-tab value="tasks">✅ Tasks</nk-tab>
  //   <div slot="panel" data-tab="notes" class="nk-tab-panel">…</div>
  //   <div slot="panel" data-tab="tasks" class="nk-tab-panel">…</div>
  // </nk-tabs>
  // → <div class="nk-tabs" role="tablist">…</div> + the panels below it.
  // The tabs keep one tab active and hide every panel whose data-tab does not
  // match `value` (through the hidden attribute – the panels stay yours).
  const KEYS$1 = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };

  class NkTabs extends NkElement {
    static get observedAttributes() { return ['value']; }

    render() {
      this._strip = this.createElement('div', ['nk-tabs'], { role: 'tablist' });
      this._tabSlot = document.createElement('slot');
      this._strip.appendChild(this._tabSlot);
      this._panelSlot = this.createElement('slot', [], { name: 'panel' });
      this._wrapper.append(this._strip, this._panelSlot);
      this._sync();
    }

    get tabs() { return [...this.querySelectorAll(':scope > nk-tab')]; }
    get panels() { return [...this.querySelectorAll(':scope > [slot="panel"]')]; }

    _sync() {
      const tabs = this.tabs;
      if (!tabs.length) return;
      let value = this.getAttribute('value');
      if (value === null || !tabs.some(t => t.getAttribute('value') === value || (t.getAttribute('value') === null && t.textContent.trim() === value))) {
        const preset = tabs.find(t => t.hasAttribute('active')) || tabs[0];
        value = preset.getAttribute('value') ?? preset.textContent.trim();
      }
      for (const tab of tabs) {
        const v = tab.getAttribute('value') ?? tab.textContent.trim();
        if (v === value) tab.setAttribute('active', ''); else tab.removeAttribute('active');
      }
      for (const panel of this.panels) panel.hidden = panel.dataset.tab !== value;
      this._value = value;
    }

    setupEvents() {
      this._onSlot = () => this._sync();
      this._onKey = (e) => {
        const dir = KEYS$1[e.key];
        if (!dir) return;
        const tabs = this.tabs.filter(t => !t.hasAttribute('disabled'));
        const i = tabs.indexOf(e.target.closest?.('nk-tab'));
        if (i < 0 || tabs.length < 2) return;
        e.preventDefault();
        const next = tabs[(i + dir + tabs.length) % tabs.length];
        next.select();
        next.focus();
      };
      this._tabSlot.addEventListener('slotchange', this._onSlot);
      this._panelSlot.addEventListener('slotchange', this._onSlot);
      this.addEventListener('keydown', this._onKey);
      this._sync();
    }

    teardownEvents() {
      this._tabSlot?.removeEventListener('slotchange', this._onSlot);
      this._panelSlot?.removeEventListener('slotchange', this._onSlot);
      this.removeEventListener('keydown', this._onKey);
    }

    onAttributeChanged() {
      const before = this._value;
      this._sync();
      if (this._value !== before) this.emit('nk-change', { value: this._value });
    }

    get value() { return this._value ?? this.getAttribute('value'); }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-tabs', NkTabs);

  // <nk-tab value="notes" active>📝 Notes</nk-tab>  →  <span class="nk-tab active" role="tab">
  class NkTab extends NkElement {
    static get observedAttributes() { return ['active', 'value', 'disabled']; }

    render() {
      this._tab = this.createElement('span', ['nk-tab'], { role: 'tab' });
      this._tab.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(this._tab);
      this._sync();
    }

    _sync() {
      const active = this.getBoolAttr('active');
      this._tab.classList.toggle('active', active);
      this._tab.setAttribute('aria-selected', active ? 'true' : 'false');
      this._tab.tabIndex = active ? 0 : -1;
      this._tab.setAttribute('aria-disabled', this.getBoolAttr('disabled') ? 'true' : 'false');
    }

    setupEvents() {
      this._onClick = () => this.select();
      this._onKey = (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); this.select(); } };
      this._tab.addEventListener('click', this._onClick);
      this._tab.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._tab?.removeEventListener('click', this._onClick);
      this._tab?.removeEventListener('keydown', this._onKey);
    }

    select() {
      if (this.getBoolAttr('disabled')) return;
      const ok = this.emit('nk-select', { value: this.value, label: this.textContent.trim(), item: this });
      if (!ok) return;
      const tabs = this.closest('nk-tabs');
      if (tabs) tabs.value = this.value;
      else this.setBoolAttr('active', true);
    }

    onAttributeChanged() { this._sync(); }
    focus(o) { this._tab?.focus(o); }

    get value() { return this.getAttribute('value') ?? this.textContent.trim(); }
    set value(v) { this.setAttribute('value', v); }
    get active() { return this.getBoolAttr('active'); }
    set active(v) { this.setBoolAttr('active', v); }
  }

  customElements.define('nk-tab', NkTab);

  // <nk-segmented name="range" value="week">
  //   <button value="week">Week</button><button value="month">Month</button>
  // </nk-segmented>
  // → <div class="nk-segmented">…</div> with .active on the chosen button.
  // The buttons stay in the light DOM (their look comes from the ::slotted
  // twins); the element sets type/role, moves .active and submits the value.
  // With `scroll` the chosen option is kept in view – after the first layout,
  // on every change and when the row's width changes – by scrolling the row
  // alone, never the page, by a relative distance, so right-to-left works too.
  const KEYS = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };

  class NkSegmented extends NkFormElement {
    static get observedAttributes() { return ['value', 'name', 'disabled', 'scroll', 'wrap']; }

    render() {
      this._box = this.createElement('div', ['nk-segmented'], { role: 'radiogroup' });
      this._slot = document.createElement('slot');
      this._box.appendChild(this._slot);
      this._wrapper.appendChild(this._box);
      this._syncBox();
      this._sync();
    }

    // `scroll`: one horizontally scrollable row, scrollbar hidden; `wrap`: rows.
    _syncBox() {
      this._box.classList.toggle('scroll', this.getBoolAttr('scroll'));
      this._box.classList.toggle('wrap', this.getBoolAttr('wrap'));
    }

    get buttons() { return this._slot?.assignedElements().filter(el => el.localName === 'button') ?? []; }
    _valueOf(btn) { return btn.getAttribute('value') ?? btn.textContent.trim(); }

    _sync() {
      const buttons = this.buttons;
      if (!buttons.length) return;
      let value = this.getAttribute('value');
      if (value === null || !buttons.some(b => this._valueOf(b) === value)) {
        value = this._valueOf(buttons.find(b => b.classList.contains('active')) || buttons[0]);
      }
      if (this._defaultValue === undefined) this._defaultValue = value;
      const disabled = this.getBoolAttr('disabled') || !!this._formDisabled;
      for (const b of buttons) {
        const on = this._valueOf(b) === value;
        b.type = 'button';
        b.setAttribute('role', 'radio');
        b.setAttribute('aria-checked', on ? 'true' : 'false');
        b.classList.toggle('active', on);
        b.tabIndex = on ? 0 : -1;
        b.disabled = disabled;
      }
      this._value = value;
      this.setFormValue(value);
      this._reveal(this._revealed ? 'smooth' : 'instant');
    }

    /** Scrolls the row the least distance that shows the chosen button whole. */
    _reveal(behavior) {
      if (!this.getBoolAttr('scroll') || !this._box?.clientWidth) return;
      const btn = this.buttons.find(b => b.classList.contains('active'));
      if (!btn) return;
      const row = this._box.getBoundingClientRect(), r = btn.getBoundingClientRect(), pad = 8;
      const delta = r.left < row.left ? r.left - row.left - pad : r.right > row.right ? r.right - row.right + pad : 0;
      const smooth = behavior === 'smooth' && !matchMedia('(prefers-reduced-motion: reduce)').matches;
      if (delta) this._box.scrollBy({ left: delta, behavior: smooth ? 'smooth' : 'instant' });
      this._revealed = true;
    }

    setupEvents() {
      this._onSlot = () => this._sync();
      this._onClick = (e) => {
        const btn = e.target.closest?.('button');
        if (!btn || !this.buttons.includes(btn) || btn.disabled) return;
        this._choose(this._valueOf(btn));
      };
      this._onKey = (e) => {
        const dir = KEYS[e.key];
        if (!dir) return;
        const buttons = this.buttons;
        const i = buttons.indexOf(e.target.closest?.('button'));
        if (i < 0 || buttons.length < 2) return;
        e.preventDefault();
        const next = buttons[(i + dir + buttons.length) % buttons.length];
        this._choose(this._valueOf(next));
        next.focus();
      };
      this._slot.addEventListener('slotchange', this._onSlot);
      this.addEventListener('click', this._onClick);
      this.addEventListener('keydown', this._onKey);
      this._resize = new ResizeObserver(() => this._reveal('instant'));
      this._resize.observe(this._box);
      this._sync();
    }

    teardownEvents() {
      this._slot?.removeEventListener('slotchange', this._onSlot);
      this.removeEventListener('click', this._onClick);
      this.removeEventListener('keydown', this._onKey);
      this._resize?.disconnect();
    }

    _choose(value) {
      if (value === this._value) return;
      this._syncing = true;
      this.setAttribute('value', value);
      this._syncing = false;
      this._sync();
      this.emit('nk-change', { value, name: this.name });
      this.dispatchEvent(new Event('change', { bubbles: true }));
    }

    onAttributeChanged(name) {
      if (name === 'scroll' || name === 'wrap') { this._syncBox(); return; }
      if (!this._syncing) this._sync();
    }
    resetValue() { if (this._defaultValue !== undefined) this.setAttribute('value', this._defaultValue); }
    onFormDisabled(d) { this._formDisabled = d; this._sync(); }

    get value() { return this._value ?? this.getAttribute('value'); }
    set value(v) { this.setAttribute('value', v); }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
    get scroll() { return this.getBoolAttr('scroll'); }
    set scroll(v) { this.setBoolAttr('scroll', v); }
    get wrap() { return this.getBoolAttr('wrap'); }
    set wrap(v) { this.setBoolAttr('wrap', v); }
  }

  customElements.define('nk-segmented', NkSegmented);

  // <nk-stats><nk-stat …></nk-stat>…</nk-stats>  →  <div class="nk-stats">
  class NkStats extends NkElement {
    render() {
      const row = this.createElement('div', ['nk-stats']);
      row.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(row);
    }
  }

  customElements.define('nk-stats', NkStats);

  // <nk-stat label="Active pages" value="128" delta="▲ 12 this week" trend="up"></nk-stat>
  // → <div class="nk-stat"><div class="s-label">…</div><div class="s-value">…</div><div class="s-delta up">…</div></div>
  class NkStat extends NkElement {
    static get observedAttributes() { return ['label', 'value', 'delta', 'trend']; }

    render() {
      const box = this.createElement('div', ['nk-stat']);
      this._parts = {};
      for (const [slot, cls] of [['label', 's-label'], ['value', 's-value'], ['delta', 's-delta']]) {
        const el = this.createElement('div', [cls]);
        const s = this.createElement('slot', [], { name: slot });
        s.appendChild(el);
        box.appendChild(s);
        this._parts[slot] = el;
      }
      this._wrapper.appendChild(box);
      this._sync();
    }

    _sync() {
      for (const [slot, el] of Object.entries(this._parts)) {
        const v = this.getAttribute(slot);
        el.textContent = v ?? '';
        el.style.display = v === null ? 'none' : '';
      }
      const trend = this.getAttribute('trend');
      this._parts.delta.className = 's-delta' + (trend === 'up' || trend === 'down' ? ` ${trend}` : '');
    }

    onAttributeChanged() { this._sync(); }

    get value() { return this.getAttribute('value'); }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-stat', NkStat);

  // <nk-avatar-group more="+2">
  //   <span class="mini-avatar" style="background:#448361">SL</span>…
  // </nk-avatar-group>
  // → <div class="nk-avatar-group">…<span class="mini-avatar more">+2</span></div>
  // The avatars stay in the light DOM (::slotted twins size and overlap them);
  // the "more" bubble is rendered here from the attribute.
  class NkAvatarGroup extends NkElement {
    static get observedAttributes() { return ['more']; }

    render() {
      const box = this.createElement('div', ['nk-avatar-group']);
      box.appendChild(document.createElement('slot'));
      this._more = this.createElement('span', ['mini-avatar', 'more']);
      box.appendChild(this._more);
      this._wrapper.appendChild(box);
      this._sync();
    }

    _sync() {
      const more = this.getAttribute('more');
      this._more.textContent = more || '';
      this._more.style.display = more ? '' : 'none';
    }

    onAttributeChanged() { this._sync(); }

    get more() { return this.getAttribute('more'); }
    set more(v) { v == null ? this.removeAttribute('more') : this.setAttribute('more', v); }
  }

  customElements.define('nk-avatar-group', NkAvatarGroup);

  // Shared by every element that draws an avatar: nk-avatar itself, member
  // rows, comments and the person cells of a database view.
  const AVATAR_COLORS = ['gray', 'brown', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink', 'red'];

  /**
   * Paints an .nk-avatar. One of Notion's nine colour names becomes its class,
   * so the tokens decide the shade in each theme; any other value – a hex, a
   * gradient, a var() – is taken as an inline background, as before 1.6.0.
   * Without a colour the avatar keeps the stylesheet's gradient.
   */
  function paintAvatar(el, color) {
    for (const c of AVATAR_COLORS) el.classList.toggle(c, c === color);
    el.style.background = color && !AVATAR_COLORS.includes(color) ? color : '';
  }

  /** "Sara Lindt" → "SL". */
  const initialsOf = name => String(name || '').split(/\s+/).map(w => w[0]).filter(Boolean).join('').slice(0, 2).toUpperCase();

  // <nk-avatar>MK</nk-avatar>                       → <span class="nk-avatar">MK</span>
  // <nk-avatar size="small" color="green">SL</nk-avatar>
  // <nk-avatar name="Ada Lovelace" size="large"></nk-avatar>   → initials "AL"
  // <nk-avatar src="ada.jpg" name="Ada Lovelace" size="xlarge"></nk-avatar>
  // <nk-avatar square>A</nk-avatar>                  → a workspace icon
  // size: small (20px) · default 24px · large (32px) · xlarge (56px). color: one
  // of Notion's nine names, or any CSS background. Without content the initials
  // come from `name`; with `src` a photo fills the circle and `name` is its alt.
  class NkAvatar extends NkElement {
    static get observedAttributes() { return ['size', 'color', 'square', 'src', 'name']; }

    render() {
      this._box = this.createElement('span', ['nk-avatar']);
      this._slot = document.createElement('slot');
      this._initials = document.createTextNode('');
      this._slot.appendChild(this._initials);
      this._img = this.createElement('img', [], { alt: '' });
      this._box.appendChild(this._slot);
      this._wrapper.appendChild(this._box);
      this._sync();
    }

    _sync() {
      const size = this.getAttribute('size');
      for (const s of ['small', 'large', 'xlarge']) this._box.classList.toggle(s, s === size);
      this._box.classList.toggle('square', this.getBoolAttr('square'));
      paintAvatar(this._box, this.getAttribute('color'));
      const name = this.getAttribute('name');
      this._initials.data = initialsOf(name);
      const src = this.getAttribute('src');
      if (src) {
        this._img.src = src;
        this._img.alt = name || '';
        if (this._slot.isConnected) this._slot.replaceWith(this._img);
      } else if (this._img.isConnected) this._img.replaceWith(this._slot);
      // A picture or initials with a name speak for themselves; a bare glyph is decoration.
      if (name && !src) { this._box.setAttribute('role', 'img'); this._box.setAttribute('aria-label', name); }
      else { this._box.removeAttribute('role'); this._box.removeAttribute('aria-label'); }
    }

    onAttributeChanged() { this._sync(); }

    get size() { return this.getAttribute('size'); }
    set size(v) { v == null ? this.removeAttribute('size') : this.setAttribute('size', v); }
    get color() { return this.getAttribute('color'); }
    set color(v) { v == null ? this.removeAttribute('color') : this.setAttribute('color', v); }
    get src() { return this.getAttribute('src'); }
    set src(v) { v == null ? this.removeAttribute('src') : this.setAttribute('src', v); }
  }

  customElements.define('nk-avatar', NkAvatar);

  // <nk-mention type="person"><span slot="avatar" class="mini-avatar" style="background:#448361">SL</span>Sara Lindt</nk-mention>
  // <nk-mention type="page">📄 Onboarding</nk-mention>   <nk-mention type="date">📅 May 20</nk-mention>
  // → <span class="nk-mention person">…</span>
  const TYPES = ['person', 'page', 'date'];

  class NkMention extends NkElement {
    static get observedAttributes() { return ['type']; }

    render() {
      this._box = this.createElement('span', this._classes());
      this._box.appendChild(this.createElement('slot', [], { name: 'avatar' }));
      this._box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(this._box);
    }

    _classes() {
      const t = this.getAttribute('type');
      return ['nk-mention', ...(TYPES.includes(t) ? [t] : [])];
    }

    onAttributeChanged() { if (this._box) this._box.className = this._classes().join(' '); }

    get type() { return this.getAttribute('type'); }
    set type(v) { v ? this.setAttribute('type', v) : this.removeAttribute('type'); }
  }

  customElements.define('nk-mention', NkMention);

  // <nk-template-btn icon="📅" value="week-plan">Insert week plan</nk-template-btn>
  // → <button class="nk-template-btn">📅 Insert week plan</button>
  class NkTemplateBtn extends NkElement {
    static get observedAttributes() { return ['icon', 'value', 'disabled']; }

    render() {
      this._btn = this.createElement('button', ['nk-template-btn'], { type: 'button' });
      // One flex item: "📅 Insert week plan" is a single text run in the class
      // markup, so icon, space and label must not become separate flex items.
      const run = document.createElement('span');
      this._icon = document.createElement('span');
      this._space = document.createTextNode(' ');
      run.append(this._icon, this._space, document.createElement('slot'));
      this._btn.appendChild(run);
      this._wrapper.appendChild(this._btn);
      this._sync();
    }

    _sync() {
      const icon = this.getAttribute('icon');
      this._icon.textContent = icon || '';
      this._space.data = icon ? ' ' : '';
      this._btn.disabled = this.getBoolAttr('disabled');
    }

    setupEvents() {
      this._onClick = () => this.emit('nk-select', { value: this.value, label: this.textContent.trim() });
      this._btn.addEventListener('click', this._onClick);
    }

    teardownEvents() { this._btn?.removeEventListener('click', this._onClick); }
    onAttributeChanged() { this._sync(); }
    focus(o) { this._btn?.focus(o); }

    get value() { return this.getAttribute('value') ?? this.textContent.trim(); }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-template-btn', NkTemplateBtn);

  // <nk-model-card name="model" value="gpt" title="Mona Pro" desc="Best for long documents" selected></nk-model-card>
  // → <div class="nk-model-card selected"><div class="m-radio"></div><div><div class="m-name">…</div><div class="m-desc">…</div></div></div>
  // Cards with the same `name` in the same tree behave like radios and submit
  // the selected value with the form. `title` is the card's name, never a
  // tooltip – see NkElement.takeTitle().
  class NkModelCard extends NkFormElement {
    static get observedAttributes() { return ['name', 'value', 'title', 'desc', 'selected', 'disabled']; }

    render() {
      this._card = this.createElement('div', ['nk-model-card'], { role: 'radio', tabindex: '0' });
      this._card.appendChild(this.createElement('div', ['m-radio']));
      const text = document.createElement('div');
      this._name = this.createElement('div', ['m-name']);
      const titleSlot = this.createElement('slot', [], { name: 'title' });
      titleSlot.appendChild(this._name);
      this._desc = this.createElement('div', ['m-desc']);
      const descSlot = this.createElement('slot', [], { name: 'desc' });
      descSlot.appendChild(this._desc);
      text.append(titleSlot, descSlot);
      this._card.appendChild(text);
      this._wrapper.appendChild(this._card);
      this._defaultSelected = this.getBoolAttr('selected');
      this.takeTitle();
      this._sync();
      if (this.getBoolAttr('selected')) this._unselectPeers();
    }

    _sync() {
      const on = this.getBoolAttr('selected');
      this._card.classList.toggle('selected', on);
      this._card.setAttribute('aria-checked', on ? 'true' : 'false');
      this._card.setAttribute('aria-disabled', this.getBoolAttr('disabled') ? 'true' : 'false');
      this._name.textContent = this._titleText || '';
      const desc = this.getAttribute('desc');
      this._desc.textContent = desc || '';
      this._desc.style.display = desc ? '' : 'none';
      this.setFormValue(on ? (this.getAttribute('value') || 'on') : null, on ? 'selected' : 'unselected');
    }

    _group() {
      const name = this.getAttribute('name');
      const root = this.getRootNode();
      if (!name || typeof root?.querySelectorAll !== 'function') return [this];
      return [...root.querySelectorAll('nk-model-card')].filter(el => el.getAttribute('name') === name);
    }

    _unselectPeers() {
      for (const el of this._group()) if (el !== this && el.hasAttribute('selected')) el.removeAttribute('selected');
    }

    setupEvents() {
      this._onClick = () => this.select();
      this._onKey = (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); this.select(); } };
      this._card.addEventListener('click', this._onClick);
      this._card.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._card?.removeEventListener('click', this._onClick);
      this._card?.removeEventListener('keydown', this._onKey);
    }

    select() {
      if (this.getBoolAttr('disabled') || this.getBoolAttr('selected')) return;
      this.setBoolAttr('selected', true);
      this.emit('nk-change', { value: this.value, name: this.name, checked: true });
      this.emit('nk-select', { value: this.value, label: this.title });
      this.dispatchEvent(new Event('change', { bubbles: true }));
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'title' && !this.takeTitle(value)) return;
      this._sync();
      if (name === 'selected' && this.getBoolAttr('selected')) this._unselectPeers();
    }

    resetValue() { this.setBoolAttr('selected', this._defaultSelected); }
    focus(o) { this._card?.focus(o); }

    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._sync(); }
    get selected() { return this.getBoolAttr('selected'); }
    set selected(v) { this.setBoolAttr('selected', v); }
    get value() { return this.getAttribute('value') || 'on'; }
    set value(v) { this.setAttribute('value', v); }
    get name() { return this.getAttribute('name'); }
    set name(v) { this.setAttribute('name', v); }
  }

  customElements.define('nk-model-card', NkModelCard);

  // <nk-profile-row avatar="MK"><nk-btn variant="secondary" small>Change photo</nk-btn></nk-profile-row>
  // → <div class="nk-profile-row"><div class="big-avatar">MK</div>…</div>
  class NkProfileRow extends NkElement {
    static get observedAttributes() { return ['avatar']; }

    render() {
      const row = this.createElement('div', ['nk-profile-row']);
      this._avatar = this.createElement('div', ['big-avatar']);
      const s = this.createElement('slot', [], { name: 'avatar' });
      s.appendChild(this._avatar);
      row.appendChild(s);
      row.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(row);
      this._sync();
    }

    _sync() { this._avatar.textContent = this.getAttribute('avatar') || ''; }
    onAttributeChanged() { this._sync(); }
  }

  customElements.define('nk-profile-row', NkProfileRow);

  // <nk-danger-zone title="Danger zone">…</nk-danger-zone>
  // → <div class="nk-danger-zone"><div class="dz-title">Danger zone</div>…</div>
  // `title` is the heading, never a tooltip – see NkElement.takeTitle().
  class NkDangerZone extends NkElement {
    static get observedAttributes() { return ['title']; }

    render() {
      const box = this.createElement('div', ['nk-danger-zone']);
      this._titleEl = this.createElement('div', ['dz-title']);
      box.appendChild(this._titleEl);
      box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(box);
      this.takeTitle();
      this._sync();
    }

    _sync() {
      const t = this._titleText;
      this._titleEl.textContent = t || '';
      this._titleEl.style.display = t ? '' : 'none';
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'title' && !this.takeTitle(value)) return;
      this._sync();
    }

    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._sync(); }
  }

  customElements.define('nk-danger-zone', NkDangerZone);

  // <nk-member-list><nk-member-row …></nk-member-row>…</nk-member-list>
  // → <div class="nk-member-list">…</div>; the last row gets `last` (no border).
  class NkMemberList extends NkElement {
    render() {
      const box = this.createElement('div', ['nk-member-list']);
      this._slot = document.createElement('slot');
      box.appendChild(this._slot);
      this._wrapper.appendChild(box);
      this._mark();
    }

    _mark() {
      const rows = this._slot.assignedElements().filter(el => el.localName === 'nk-member-row');
      rows.forEach((row, i) => { if (i === rows.length - 1) row.setAttribute('last', ''); else row.removeAttribute('last'); });
    }

    setupEvents() {
      this._onSlot = () => this._mark();
      this._slot.addEventListener('slotchange', this._onSlot);
      this._mark();
    }

    teardownEvents() { this._slot?.removeEventListener('slotchange', this._onSlot); }
  }

  customElements.define('nk-member-list', NkMemberList);

  // <nk-member-row name="Sara Lindt" mail="sara@…" avatar="SL" color="green">
  //   <nk-select slot="role" compact>…</nk-select>
  // </nk-member-row>
  // → <div class="nk-member-row"><span class="nk-avatar green">SL</span><div>Sara Lindt<div class="m-mail">…</div></div><select …></div>
  // color: one of Notion's nine names, or any CSS background; without it the
  // avatar gradient. The row sizes the avatar to 28px.
  class NkMemberRow extends NkElement {
    static get observedAttributes() { return ['name', 'mail', 'avatar', 'color', 'last']; }

    render() {
      this._row = this.createElement('div', ['nk-member-row']);
      this._avatar = this.createElement('span', ['nk-avatar']);
      const avatarSlot = this.createElement('slot', [], { name: 'avatar' });
      avatarSlot.appendChild(this._avatar);
      const text = document.createElement('div');
      this._name = document.createTextNode('');
      this._mail = this.createElement('div', ['m-mail']);
      text.append(this._name, this._mail);
      // `.nk-member-row .nk-select { margin-left: auto }` cannot reach a select
      // inside another shadow root, and a display:contents host takes no margin –
      // so the role slot sits in a box that carries that rule.
      const role = document.createElement('div');
      role.style.marginLeft = 'auto';
      role.appendChild(this.createElement('slot', [], { name: 'role' }));
      this._row.append(avatarSlot, text, role, document.createElement('slot'));
      this._wrapper.appendChild(this._row);
      this._sync();
    }

    _sync() {
      this._name.data = this.getAttribute('name') || '';
      const mail = this.getAttribute('mail');
      this._mail.textContent = mail || '';
      this._mail.style.display = mail ? '' : 'none';
      const avatar = this.getAttribute('avatar');
      this._avatar.textContent = avatar || initialsOf(this.getAttribute('name'));
      paintAvatar(this._avatar, this.getAttribute('color'));
      this._row.classList.toggle('last', this.getBoolAttr('last'));
    }

    onAttributeChanged() { this._sync(); }
  }

  customElements.define('nk-member-row', NkMemberRow);

  // <nk-props>
  //   <nk-prop label="Status" icon="◉"><nk-tag color="blue">In progress</nk-tag></nk-prop>
  //   <nk-prop label="Owner" icon="👤"><nk-avatar size="small">MK</nk-avatar>Marcel Karas</nk-prop>
  // </nk-props>
  // → <dl class="nk-props">…</dl> – the properties under a page title. The rows
  // are <nk-prop> hosts with display: contents, so each .nk-prop is a row of
  // this list.
  class NkProps extends NkElement {
    render() {
      const list = this.createElement('dl', ['nk-props']);
      list.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(list);
    }
  }

  customElements.define('nk-props', NkProps);

  // <nk-prop label="Due" icon="📅">2 June 2026</nk-prop>
  // → <div class="nk-prop"><dt class="p-name"><span class="p-icon">📅</span>Due</dt><dd class="p-value">2 June 2026</dd></div>
  // The value is the element's content – tags, an avatar and a name, a date, a
  // <nk-progress wide>. slot="icon" replaces the icon glyph. A click on the
  // name or the value fires nk-action { action: 'name' | 'value', label }, the
  // moment Notion opens the property's editor.
  class NkProp extends NkElement {
    static get observedAttributes() { return ['label', 'icon']; }

    render() {
      this._row = this.createElement('div', ['nk-prop']);
      this._name = this.createElement('dt', ['p-name']);
      this._icon = this.createElement('span', ['p-icon']);
      const iconSlot = this.createElement('slot', [], { name: 'icon' });
      iconSlot.appendChild(this._icon);
      this._label = document.createTextNode('');
      this._name.append(iconSlot, this._label);
      this._value = this.createElement('dd', ['p-value']);
      this._value.appendChild(document.createElement('slot'));
      this._row.append(this._name, this._value);
      this._wrapper.appendChild(this._row);
      this._sync();
    }

    _sync() {
      const icon = this.getAttribute('icon');
      this._icon.textContent = icon || '';
      this._icon.style.display = icon ? '' : 'none';
      this._label.data = this.getAttribute('label') || '';
    }

    setupEvents() {
      this._onClick = (e) => {
        const part = e.composedPath().includes(this._name) ? 'name' : 'value';
        this.emit('nk-action', { action: part, label: this.label });
      };
      this._row.addEventListener('click', this._onClick);
    }

    teardownEvents() { this._row?.removeEventListener('click', this._onClick); }
    onAttributeChanged() { this._sync(); }

    get label() { return this.getAttribute('label') || ''; }
    set label(v) { this.setAttribute('label', v); }
    get icon() { return this.getAttribute('icon'); }
    set icon(v) { v == null ? this.removeAttribute('icon') : this.setAttribute('icon', v); }
  }

  customElements.define('nk-prop', NkProp);

  // <nk-panels><nk-panel …></nk-panel><nk-panel …></nk-panel></nk-panels>
  // → <div class="nk-panels">…</div>: columns of at least 200px that share the
  // row and fall to one on a phone. The <nk-panel> hosts are display: contents,
  // so each .nk-panel is a cell of this grid.
  class NkPanels extends NkElement {
    render() {
      const grid = this.createElement('div', ['nk-panels']);
      grid.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(grid);
    }
  }

  customElements.define('nk-panels', NkPanels);

  // <nk-panel title="Weekly review"><p>Three pages changed.</p></nk-panel>
  // <nk-panel href="/roadmap" cover="covers/aurora.svg" icon="🚀" title="Roadmap"><p>2 min ago</p></nk-panel>
  // → <a class="nk-panel" href="/roadmap"><div class="nk-cover"><img src="…" alt=""></div>
  //      <div class="nk-page-icon">🚀</div><h3>Roadmap</h3>…</a>
  // `cover` without a value draws the token gradient, with one a picture; `icon`
  // overlaps the cover as on a page – a page tile, as on Notion's Home. With
  // `href` the panel is a link. `title` is the heading, never a tooltip.
  class NkPanel extends NkElement {
    static get observedAttributes() { return ['title', 'icon', 'cover', 'href', 'target']; }

    render() {
      this._cover = this.createElement('div', ['nk-cover']);
      this._img = this.createElement('img', [], { alt: '' });
      this._icon = this.createElement('div', ['nk-page-icon']);
      this._heading = document.createElement('h3');
      this._slot = document.createElement('slot');
      this.takeTitle();
      this._build();
    }

    _build() {
      const href = this.getAttribute('href');
      const box = this.createElement(href ? 'a' : 'div', ['nk-panel']);
      box.append(this._cover, this._icon, this._heading, this._slot);
      this._wrapper.replaceChildren(box);
      this._box = box;
      this._sync();
    }

    _sync() {
      const href = this.getAttribute('href');
      if (href) { this._box.setAttribute('href', href); this._box.target = this.getAttribute('target') || ''; }
      const cover = this.getAttribute('cover');
      this._cover.style.display = cover === null ? 'none' : '';
      if (cover) { this._img.src = cover; if (!this._img.isConnected) this._cover.appendChild(this._img); }
      else this._img.remove();
      const icon = this.getAttribute('icon');
      this._icon.textContent = icon || '';
      this._icon.style.display = icon ? '' : 'none';
      this._heading.textContent = this._titleText || '';
      this._heading.style.display = this._titleText ? '' : 'none';
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'title' && !this.takeTitle(value)) return;
      if (name === 'href' && (value === null) !== (this._box.localName === 'div')) { this._build(); return; }
      this._sync();
    }

    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._sync(); }
    get href() { return this.getAttribute('href'); }
    set href(v) { v == null ? this.removeAttribute('href') : this.setAttribute('href', v); }
  }

  customElements.define('nk-panel', NkPanel);

  // <nk-sheet id="more" title="More">
  //   <nk-tree manual>
  //     <nk-section-label>Favourites</nk-section-label>
  //     <nk-tree-item icon="🚀">NotionKit MVP</nk-tree-item>
  //   </nk-tree>
  // </nk-sheet>
  // → <div class="nk-sheet-backdrop open"><div class="nk-sheet" role="dialog">
  //     <div class="sh-grabber"></div><div class="sh-title">More</div>…</div></div>
  //
  // A bottom sheet (NotionKit 1.7.0) with nk-modal's contract: show(), close()
  // and toggle(), Escape and the backdrop close it, focus moves in and back
  // out, the page behind is scroll-locked and inert. It lies above the tab
  // bar; rows inside are 40px high. `title` is the heading shown under the
  // grabber and the dialog's name, never a tooltip – see NkElement.takeTitle().
  // Choosing a row does not close the sheet – the app decides (nk-select
  // bubbles out). Put the element directly under <body>: a transformed
  // ancestor would trap the fixed backdrop.
  class NkSheet extends NkElement {
    static get observedAttributes() { return ['open', 'title']; }

    render() {
      this.takeTitle();
      this._backdrop = this.createElement('div', ['nk-sheet-backdrop']);
      this._backdrop.inert = true;
      this._box = this.createElement('div', ['nk-sheet'], { role: 'dialog', 'aria-modal': 'true', tabindex: '-1' });
      this._heading = this.createElement('div', ['sh-title'], { id: 'title' });
      this._box.append(this.createElement('div', ['sh-grabber']), this._heading, document.createElement('slot'));
      this._backdrop.appendChild(this._box);
      this._wrapper.appendChild(this._backdrop);
      this._syncTitle();
      this._syncOpen();
    }

    _syncTitle() {
      const text = this._titleText || '';
      this._heading.textContent = text;
      this._heading.hidden = !text;
      if (text) this._box.setAttribute('aria-labelledby', 'title');
      else this._box.removeAttribute('aria-labelledby');
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      this._backdrop.classList.toggle('open', open);
      this._backdrop.inert = !open;
      if (open === this._wasOpen) return;
      this._wasOpen = open;
      if (open) {
        this._returnFocus = deepActiveElement();
        lockScroll();
        this._undoInert = inertOutside(this);
        requestAnimationFrame(() => (firstFocusable(this) || this._box).focus({ preventScroll: true }));
      } else {
        unlockScroll();
        this._undoInert?.(); this._undoInert = null;
        const back = this._returnFocus;
        if (back && typeof back.focus === 'function' && back.isConnected) back.focus({ preventScroll: true });
        this._returnFocus = null;
      }
    }

    setupEvents() {
      this._onBackdrop = (e) => { if (e.target === this._backdrop) this.close(); };
      this._onKey = (e) => { if (e.key === 'Escape' && this.getBoolAttr('open')) { e.stopPropagation(); this.close(); } };
      this._backdrop.addEventListener('click', this._onBackdrop);
      document.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._backdrop?.removeEventListener('click', this._onBackdrop);
      document.removeEventListener('keydown', this._onKey);
      if (this._wasOpen) { unlockScroll(); this._undoInert?.(); this._undoInert = null; this._wasOpen = false; }
    }

    onAttributeChanged(name, oldValue, value) {
      if (name === 'title') { if (this.takeTitle(value)) this._syncTitle(); return; }
      this._syncOpen();
      this.emit('nk-toggle', { open: this.getBoolAttr('open') });
    }

    show() { this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._syncTitle(); }
  }

  customElements.define('nk-sheet', NkSheet);

  // <nk-steps label="Connect your own model" current="2"
  //           steps="Choose a provider, Enter the API key, Test the connection"></nk-steps>
  // steps.steps = [{ label: 'Choose a provider', desc: 'Anthropic' }, 'Enter the API key', …];
  // → <ol class="nk-steps" aria-label="…">
  //     <li class="nk-step done"><span class="st-mark">✓</span><span>Choose a provider<span class="st-desc">Anthropic</span></span></li>
  //     <li class="nk-step current" aria-current="step"><span class="st-mark">2</span><span>Enter the API key</span></li>
  //     <li class="nk-step"><span class="st-mark">3</span><span>Test the connection</span></li></ol>
  // A setup in a few steps (NotionKit 1.7.0). `current` counts from 1; the
  // steps before it are done, one past the last marks all of them done. The
  // `steps` attribute is a comma-separated list; the property also takes
  // { label, desc } objects for a line under the label.
  class NkSteps extends NkElement {
    static get observedAttributes() { return ['steps', 'current', 'label']; }

    render() {
      this._list = this.createElement('ol', ['nk-steps']);
      this._wrapper.appendChild(this._list);
      this._sync();
    }

    _items() {
      if (this._steps) return this._steps;
      return (this.getAttribute('steps') || '').split(',').map(s => s.trim()).filter(Boolean);
    }

    _sync() {
      const label = this.getAttribute('label');
      if (label) this._list.setAttribute('aria-label', label); else this._list.removeAttribute('aria-label');
      const current = this.current;
      this._list.replaceChildren(...this._items().map((step, i) => {
        const n = i + 1, { label: text, desc } = typeof step === 'object' && step ? step : { label: String(step) };
        const li = this.createElement('li', ['nk-step']);
        li.classList.toggle('done', n < current);
        li.classList.toggle('current', n === current);
        if (n === current) li.setAttribute('aria-current', 'step');
        const mark = this.createElement('span', ['st-mark'], { 'aria-hidden': 'true' });
        mark.textContent = n < current ? '✓' : String(n);
        const body = document.createElement('span');
        body.textContent = text ?? '';
        if (desc) { const d = this.createElement('span', ['st-desc']); d.textContent = desc; body.appendChild(d); }
        li.append(mark, body);
        return li;
      }));
    }

    onAttributeChanged() { this._sync(); }

    get steps() { return this._items(); }
    set steps(v) { this._steps = Array.isArray(v) ? v : null; if (this._list) this._sync(); }
    get current() { const n = parseInt(this.getAttribute('current'), 10); return Number.isFinite(n) ? n : 1; }
    set current(v) { this.setAttribute('current', String(v)); }
    /** Moves on to the next step; returns the new current step. */
    next() { this.current = Math.min(this.current + 1, this._items().length + 1); return this.current; }
  }

  customElements.define('nk-steps', NkSteps);

  // <nk-bookmark href="https://notionkit.jungherz.com" title="NotionKit" desc="…"
  //              favicon="favicon.svg" cover="og.jpg"></nk-bookmark>
  // → <a class="nk-bookmark" href="…" target="_blank" rel="noopener">
  //     <span class="bm-text"><span class="bm-title">…</span><span class="bm-desc">…</span>
  //       <span class="bm-url"><img class="bm-favicon"><span>…</span></span></span>
  //     <span class="bm-cover"><img></span></a>
  // Notion's link block (NotionKit 1.8.0), filled from what you know of the
  // page: og:title, og:description, og:image. `title` is the heading, never a
  // tooltip – see NkElement.takeTitle(); without it the host name stands in.
  // The address shown is `href`, or `url` where it should read differently. A
  // part without a value is left out, not hidden – the stylesheet gives the
  // description its own display. Opens in a new tab unless `target` says so.
  class NkBookmark extends NkElement {
    static get observedAttributes() { return ['href', 'title', 'desc', 'favicon', 'cover', 'target', 'url']; }

    render() {
      this.takeTitle();
      this._a = this.createElement('a', ['nk-bookmark']);
      this._wrapper.appendChild(this._a);
      this._sync();
    }

    _sync() {
      if (!this._a) return;
      const href = this.getAttribute('href') || '';
      this._a.setAttribute('href', href);
      const target = this.getAttribute('target') || '_blank';
      this._a.setAttribute('target', target);
      if (target === '_blank') this._a.setAttribute('rel', 'noopener'); else this._a.removeAttribute('rel');
      let host = href;
      try { host = new URL(href, location.href).host; } catch { /* keep the text */ }

      const text = this.createElement('span', ['bm-text']);
      const title = this.createElement('span', ['bm-title']);
      title.textContent = this._titleText || host;
      text.appendChild(title);
      const desc = this.getAttribute('desc');
      if (desc) { const d = this.createElement('span', ['bm-desc']); d.textContent = desc; text.appendChild(d); }
      const url = this.createElement('span', ['bm-url']);
      const favicon = this.getAttribute('favicon');
      if (favicon) url.appendChild(this.createElement('img', ['bm-favicon'], { src: favicon, alt: '' }));
      const address = document.createElement('span');
      address.textContent = this.getAttribute('url') || href;
      url.appendChild(address);
      text.appendChild(url);
      const parts = [text];
      const cover = this.getAttribute('cover');
      if (cover) { const c = this.createElement('span', ['bm-cover']); c.appendChild(this.createElement('img', [], { src: cover, alt: '' })); parts.push(c); }
      this._a.replaceChildren(...parts);
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'title' && !this.takeTitle(value)) return;
      this._sync();
    }

    focus(o) { this._a?.focus(o); }
    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); this._sync(); }
    get href() { return this.getAttribute('href') || ''; }
    set href(v) { this.setAttribute('href', v); }
  }

  customElements.define('nk-bookmark', NkBookmark);

  // <nk-copy-field value="https://monahilft.notionkit.app"></nk-copy-field>
  // <nk-copy-field value="ntn_4f2…" secret mono></nk-copy-field>
  // → <div class="nk-copy-field mono"><span class="cf-value">…</span>
  //     <button class="cf-btn">Show</button><button class="cf-btn">Copy</button></div>
  // A value to take along – a link, an address, a key (NotionKit 1.8.0). Copy
  // writes it to the clipboard and says "Copied" in green for a moment; where
  // the clipboard is not allowed, the value is shown and selected instead, for
  // ⌘C. `secret` masks it behind a Show/Hide toggle; Copy still copies the
  // real value. `value` set as a property is not reflected, so a key never
  // lands in the markup. Fires nk-action { action: 'copy', value, ok }. The
  // texts are attributes: copy-label, copied-label, show-label, hide-label.
  class NkCopyField extends NkElement {
    static get observedAttributes() { return ['value', 'mono', 'wrap', 'wide', 'secret', 'copy-label', 'copied-label', 'show-label', 'hide-label']; }

    render() {
      this._val ??= this.getAttribute('value') ?? '';
      this._box = this.createElement('div', ['nk-copy-field']);
      this._text = this.createElement('span', ['cf-value']);
      this._show = this.createElement('button', ['cf-btn'], { type: 'button' });
      this._copy = this.createElement('button', ['cf-btn'], { type: 'button' });
      this._box.append(this._text, this._copy);
      this._wrapper.appendChild(this._box);
      this._sync();
    }

    _sync() {
      if (!this._box) return;
      for (const m of ['mono', 'wrap', 'wide']) this._box.classList.toggle(m, this.getBoolAttr(m));
      const secret = this.getBoolAttr('secret');
      this._text.textContent = secret && !this._revealed ? '•'.repeat(Math.min(this._val.length, 24)) : this._val;
      // Not `hidden`: .cf-btn sets no display of its own that could lose to it,
      // but a button that is not there cannot be tabbed to either.
      if (secret && !this._show.isConnected) this._copy.before(this._show);
      if (!secret) this._show.remove();
      this._show.textContent = this._revealed ? (this.getAttribute('hide-label') || 'Hide') : (this.getAttribute('show-label') || 'Show');
      this._show.setAttribute('aria-pressed', String(!!this._revealed));
      this._copy.textContent = this._copied ? (this.getAttribute('copied-label') || 'Copied') : (this.getAttribute('copy-label') || 'Copy');
      this._copy.classList.toggle('copied', !!this._copied);
    }

    setupEvents() {
      this._onCopy = () => this.copy();
      this._onShow = () => { this._revealed = !this._revealed; this._sync(); };
      this._copy.addEventListener('click', this._onCopy);
      this._show.addEventListener('click', this._onShow);
    }

    teardownEvents() {
      this._copy?.removeEventListener('click', this._onCopy);
      this._show?.removeEventListener('click', this._onShow);
      clearTimeout(this._timer);
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'value') this._val = value ?? '';
      this._sync();
    }

    /** Copies the value; resolves to whether the clipboard took it. */
    async copy() {
      const value = this._val;
      let ok = true;
      try { await navigator.clipboard.writeText(value); } catch { ok = false; }
      if (ok) {
        this._copied = true;
        clearTimeout(this._timer);
        this._timer = setTimeout(() => { this._copied = false; this._sync(); }, 1500);
      } else {
        this._revealed = true;
      }
      this._sync();
      if (!ok) getSelection()?.selectAllChildren(this._text);
      this.emit('nk-action', { action: 'copy', value, ok });
      return ok;
    }

    get value() { return this._val ?? this.getAttribute('value') ?? ''; }
    set value(v) { this._val = v == null ? '' : String(v); this._sync(); }
    get secret() { return this.getBoolAttr('secret'); }
    set secret(v) { this.setBoolAttr('secret', v); }
  }

  customElements.define('nk-copy-field', NkCopyField);

  // <nk-image-picker initials="MK" label="Profile picture"></nk-image-picker>
  // <nk-image-picker square initials="M" max="256" type="image/png"></nk-image-picker>
  // → <div class="nk-profile-row"><div class="big-avatar">MK | <img></div>
  //     <div class="pr-actions"><button class="nk-btn secondary small">Upload image</button>
  //       <button class="nk-btn secondary small pr-remove">Remove</button><input type="file" hidden></div></div>
  //
  // A picture for a person or a workspace (NotionKit 1.8.0): the profile row
  // with its actions, round – or `square`, a workspace icon. Ported from
  // GlassKit Elements' glk-image-picker: the file goes through
  // createImageBitmap with imageOrientation 'from-image' – the EXIF rotation
  // of a phone photo without a library – onto a canvas no larger than `max`
  // (default 512), out as `type` (default image/jpeg, on white; image/png
  // keeps transparency) at `quality` (0.82). The file input is hidden and
  // opened from a real button, and cleared after every pick, so the same file
  // can be chosen again. Not form-associated: listen to nk-change and upload
  // the data URL. `src` set as a property is not reflected, so megabytes of
  // data URL never land in the DOM.
  // Events: nk-change { dataUrl, width, height, size } after a pick, and on
  //         remove with an empty dataUrl; nk-error { message, name } when the
  //         file cannot be decoded (HEIC outside Safari, a broken file).
  // Texts:  choose-label, change-label, remove-label; label names the group.

  function dataUrlBytes(url) {
    const b64 = url.slice(url.indexOf(',') + 1);
    const padding = b64.endsWith('==') ? 2 : b64.endsWith('=') ? 1 : 0;
    return Math.floor((b64.length * 3) / 4) - padding;
  }

  class NkImagePicker extends NkElement {
    static get observedAttributes() { return ['src', 'initials', 'square', 'label', 'max', 'type', 'quality', 'accept', 'choose-label', 'change-label', 'remove-label']; }

    render() {
      this._src ??= this.getAttribute('src') || '';
      this._row = this.createElement('div', ['nk-profile-row'], { role: 'group' });
      this._avatar = this.createElement('div', ['big-avatar']);
      const actions = this.createElement('div', ['pr-actions']);
      this._choose = this.createElement('button', ['nk-btn', 'secondary', 'small'], { type: 'button' });
      this._remove = this.createElement('button', ['nk-btn', 'secondary', 'small', 'pr-remove'], { type: 'button' });
      this._input = this.createElement('input', [], { type: 'file', tabindex: '-1' });
      this._input.hidden = true;
      actions.append(this._choose, this._remove, this._input);
      this._row.append(this._avatar, actions);
      this._wrapper.appendChild(this._row);
      this._sync();
    }

    get _max() { return Math.max(1, Math.floor(Number(this.getAttribute('max'))) || 512); }
    get _type() { return this.getAttribute('type') || 'image/jpeg'; }
    get _quality() { const q = Number(this.getAttribute('quality')); return this.hasAttribute('quality') && q >= 0 && q <= 1 ? q : 0.82; }

    _sync() {
      if (!this._row) return;
      this._avatar.classList.toggle('square', this.getBoolAttr('square'));
      if (!this._src) {
        this._img = null;
        this._avatar.textContent = this.getAttribute('initials') || '';
      } else if (!this._img || this._img.getAttribute('src') !== this._src) {
        this._img = this.createElement('img', [], { src: this._src, alt: '' });
        this._avatar.replaceChildren(this._img);
      }
      const label = this.getAttribute('label');
      if (label) this._row.setAttribute('aria-label', label); else this._row.removeAttribute('aria-label');
      this._choose.textContent = this._src ? (this.getAttribute('change-label') || 'Change image') : (this.getAttribute('choose-label') || 'Upload image');
      this._remove.textContent = this.getAttribute('remove-label') || 'Remove';
      this._remove.hidden = !this._src;
      this._input.accept = this.getAttribute('accept') || 'image/*';
    }

    async _pick(file) {
      if (!file) return;
      try {
        const bitmap = await createImageBitmap(file, { imageOrientation: 'from-image' });
        const scale = Math.min(1, this._max / Math.max(bitmap.width, bitmap.height));
        const canvas = document.createElement('canvas');
        canvas.width = Math.max(1, Math.round(bitmap.width * scale));
        canvas.height = Math.max(1, Math.round(bitmap.height * scale));
        const ctx = canvas.getContext('2d');
        if (this._type === 'image/jpeg') {
          // JPEG has no alpha; without this a transparent PNG comes out on black.
          ctx.fillStyle = '#fff';
          ctx.fillRect(0, 0, canvas.width, canvas.height);
        }
        ctx.drawImage(bitmap, 0, 0, canvas.width, canvas.height);
        bitmap.close();
        const dataUrl = canvas.toDataURL(this._type, this._quality);
        this.src = dataUrl;
        this.emit('nk-change', { dataUrl, width: canvas.width, height: canvas.height, size: dataUrlBytes(dataUrl) });
      } catch (error) {
        this.emit('nk-error', { message: error?.message || String(error), name: file.name });
      }
    }

    setupEvents() {
      this._onChoose = () => this._input.click();
      this._onChange = (e) => { const file = e.target.files?.[0]; e.target.value = ''; this._pick(file); };
      this._onRemove = () => {
        this.src = '';
        this._choose.focus();   // the remove button just went away under the focus
        this.emit('nk-change', { dataUrl: '', width: 0, height: 0, size: 0 });
      };
      this._choose.addEventListener('click', this._onChoose);
      this._input.addEventListener('change', this._onChange);
      this._remove.addEventListener('click', this._onRemove);
    }

    teardownEvents() {
      this._choose?.removeEventListener('click', this._onChoose);
      this._input?.removeEventListener('change', this._onChange);
      this._remove?.removeEventListener('click', this._onRemove);
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'src') this._src = value || '';
      this._sync();
    }

    /** Opens the file dialog, as the button does. */
    choose() { this._input?.click(); }
    get src() { return this._src ?? this.getAttribute('src') ?? ''; }
    set src(v) { this._src = v || ''; this._sync(); }
    get square() { return this.getBoolAttr('square'); }
    set square(v) { this.setBoolAttr('square', v); }
  }

  customElements.define('nk-image-picker', NkImagePicker);

  // Calendar days for nk-calendar and nk-calendar-view: local days as
  // 'YYYY-MM-DD', no library. Weeks are ISO weeks (Monday-based, the week with
  // the year's first Thursday); the first weekday comes from the locale.
  const pad = n => String(n).padStart(2, '0');

  const isoOf = d => `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
  const addDays = (d, n) => new Date(d.getFullYear(), d.getMonth(), d.getDate() + n);

  /** 'YYYY-MM-DD[THH:MM]', 'D.M.YYYY', a Date or { start } → a local Date at midnight, or null. */
  function parseDay(v) {
    if (v instanceof Date) return Number.isNaN(v.getTime()) ? null : new Date(v.getFullYear(), v.getMonth(), v.getDate());
    if (v && typeof v === 'object') return v.start ? parseDay(v.start) : null;
    const s = String(v ?? '').trim();
    let m = s.match(/^(\d{4})-(\d{2})-(\d{2})/);
    if (m) return new Date(+m[1], +m[2] - 1, +m[3]);
    m = s.match(/^(\d{1,2})\.(\d{1,2})\.(\d{4})$/);
    return m ? new Date(+m[3], +m[2] - 1, +m[1]) : null;
  }

  function isoWeek(d) {
    const t = new Date(Date.UTC(d.getFullYear(), d.getMonth(), d.getDate()));
    t.setUTCDate(t.getUTCDate() + 4 - (t.getUTCDay() || 7));
    return Math.ceil(((t - Date.UTC(t.getUTCFullYear(), 0, 1)) / 864e5 + 1) / 7);
  }

  /** The page's language unless the element names one. */
  const resolveLocale = attr => attr || document.documentElement.lang || navigator.language || 'en';

  /** 0 = Sunday … 6 = Saturday, from Intl.Locale's week info; Monday where the browser cannot say. */
  function firstWeekday(locale) {
    try {
      const l = new Intl.Locale(locale), info = l.getWeekInfo?.() ?? l.weekInfo;
      if (info?.firstDay) return info.firstDay % 7;
    } catch { /* an unknown tag */ }
    return 1;
  }

  /** Short weekday names from `weekStart` on; `length` cuts them (2 → "Mo"). */
  function weekdayNames(locale, weekStart, length = 0) {
    const f = new Intl.DateTimeFormat(locale, { weekday: 'short' });
    // 7 June 2026 is a Sunday, so 7 + n is weekday n.
    return Array.from({ length: 7 }, (_, i) => {
      const name = f.format(new Date(2026, 5, 7 + ((weekStart + i) % 7))).replace('.', '');
      return length ? name.slice(0, length) : name;
    });
  }

  /** A month from its week start: `first`, `day(i)` for the i-th cell, the `rows` it needs. */
  function monthGrid(month, weekStart) {
    const [y, m] = month.split('-').map(Number), first = new Date(y, m - 1, 1), offset = (first.getDay() - weekStart + 7) % 7;
    return { m, first, day: i => new Date(y, m - 1, 1 - offset + i), rows: Math.ceil((offset + new Date(y, m, 0).getDate()) / 7) };
  }

  function stepMonth(month, n) {
    const [y, m] = month.split('-').map(Number);
    return isoOf(new Date(y, m - 1 + n, 1)).slice(0, 7);
  }

  /** "KW" for German, "W" elsewhere – the head of the week column. */
  const weekLabel = locale => (/^de\b/i.test(locale) ? 'KW' : 'W');

  const CHEVRONS = {
    prev: '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M15 6l-6 6 6 6"/></svg>',
    next: '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M9 6l6 6-6 6"/></svg>',
  };

  // Placement for elements that float over the page – <nk-menu floating>,
  // <nk-calendar floating>: the host is a fixed box put under its anchor.

  /** The border box of an element, or of the first descendant with one (display: contents hosts). */
  function boxOf(el) {
    if (el.getClientRects().length) return el.getBoundingClientRect();
    for (const child of [...(el.shadowRoot?.children ?? []), ...el.children]) {
      const r = boxOf(child);
      if (r.width || r.height) return r;
    }
    return el.getBoundingClientRect();
  }

  /**
   * Puts `host` 6px below `target` – an element or a rect ({ left, top, right,
   * bottom }, a DOMRect) – centred on it: above with side="top", or where the
   * window ends below; kept 8px inside the window. For <nk-tooltip>.
   */
  function placeNear(host, target, side = 'bottom', gap = 6) {
    const r = target instanceof Element ? boxOf(target) : target;
    const { width: w, height: h } = host.getBoundingClientRect();
    const below = r.bottom + gap, above = r.top - gap - h;
    const top = side === 'top' ? (above < 8 ? below : above) : (below + h > innerHeight - 8 ? above : below);
    host.style.top = `${Math.round(top)}px`;
    host.style.left = `${Math.round(Math.min(Math.max(8, (r.left + r.right) / 2 - w / 2), innerWidth - 8 - w))}px`;
    host.style.right = '';
  }

  /** Puts `host` 6px under `anchor`, right edges aligned – left edges with align="start". */
  function placeUnder(host, anchor, align) {
    const r = boxOf(anchor);
    host.style.top = `${r.bottom + 6}px`;
    if (align === 'start') { host.style.left = `${r.left}px`; host.style.right = ''; }
    else { host.style.right = `${Math.max(8, innerWidth - r.right)}px`; host.style.left = ''; }
  }

  // <nk-calendar name="due" value="2026-06-02" weeks weekend="6,0"></nk-calendar>
  // <nk-calendar range value="2026-06-08/2026-06-12"></nk-calendar>
  // <nk-calendar floating sheet id="picker"></nk-calendar>   picker.show(button)
  // → [<div class="nk-pop floating sheet open">]<div class="nk-calendar weeks">
  //     <div class="cal-head"><div class="cal-title">June 2026</div><button class="cal-nav">Today</button>‹ ›</div>
  //     <div class="cal-grid"><span class="cal-wd">W</span>…<span class="cal-week">23</span><button class="cal-day selected">2</button>…</div>
  //     [<div class="cal-foot"><input class="nk-input" type="time"></div>]</div>
  //
  // Notion's date picker (NotionKit 1.9.0), after GlassKit Elements'
  // glk-calendar, with what a planning tool needs on top: a range, ISO
  // calendar weeks, days not worked, marks and a time.
  // value: YYYY-MM-DD; with `range` the ISO interval "start/end"; with `time`
  //        YYYY-MM-DDTHH:MM. Also as the properties value, start, end.
  // month (YYYY-MM shown; default the value's, else today's), min, max, today,
  // locale (default the page's lang), week-start (0 = Sunday … 6 = Saturday;
  // default from the locale, Monday where the browser cannot say), weeks,
  // weekend (weekdays not worked, "6,0" for Saturday and Sunday), range, time,
  // clearable, label and the texts today-label, prev-label, next-label,
  // week-label, time-label, clear-label. `time` belongs to single days; a
  // range has none.
  // days (property, or JSON attribute): { 'YYYY-MM-DD': { off, label, marks } }
  // – `off` greys a holiday, `label` names the day (tooltip and screen
  // reader), `marks` puts up to three dots in the nine colours on it.
  // Keys: arrows by a day or a week, Home/End to the ends of the week,
  // PageUp/PageDown by a month (Shift: a year); Enter or Space picks. Arrows
  // only move, so the picker stays quiet while someone looks around. Days
  // outside min/max are aria-disabled – in the arrow path and announced, never
  // picked. A range takes two picks, in either order. Form-associated: the
  // value goes with the form, reset restores it.
  // Events: nk-change { value, start, end, time } when a pick – for a range the
  // second one –, a new time or Clear changes the value; nk-month { month } when the month
  // changes; nk-toggle { open } with `floating`. `required` makes an empty
  // value invalid.
  // `floating` makes it a popover over the page – show(anchor) puts it under
  // the anchor, right edges aligned (align="start": left edges) – and `sheet`
  // a bottom sheet with 44px cells on a phone, as <nk-menu floating sheet>.
  // A pick closes it – the second of a range; with `time` Escape or a tap
  // outside does. Put a floating calendar directly under <body>.
  const STEPS = { ArrowLeft: -1, ArrowRight: 1, ArrowUp: -7, ArrowDown: 7 };
  const floatSheet$1 = new CSSStyleSheet();
  floatSheet$1.replaceSync(`:host([floating]) { display: block; position: fixed; z-index: 60; pointer-events: none; }`);

  class NkCalendar extends NkFormElement {
    static get hostStyles() { return floatSheet$1; }
    static get observedAttributes() {
      return ['value', 'month', 'min', 'max', 'today', 'locale', 'week-start', 'weeks', 'weekend', 'range', 'time', 'clearable', 'days',
        'label', 'today-label', 'prev-label', 'next-label', 'week-label', 'time-label', 'clear-label', 'floating', 'sheet', 'open', 'align', 'required'];
    }

    render() {
      // A value or month set as a property before the first connect wins over the attributes.
      this._readValue(this._pendingValue ?? this.getAttribute('value'));
      const month = this._pendingMonth || this._attrMonth();
      if (month) this._month = month;
      this._initialValue = this.getAttribute('value') ?? '';
      this._build();
    }

    // ── value ──
    _readValue(v) {
      const s = String(v ?? '');
      this._start = this._end = this._day = null;
      this._time = '';
      if (this.getBoolAttr('range')) {
        const [a, b] = s.split('/');
        this._start = parseDay(a) ? isoOf(parseDay(a)) : null;
        this._end = parseDay(b) ? isoOf(parseDay(b)) : null;
      } else if (parseDay(s)) {
        this._day = isoOf(parseDay(s));
        this._time = s.match(/T(\d{2}:\d{2})/)?.[1] ?? '';
      }
      // A new value brings its month; without one the month stays.
      const shown = this._day || this._start;
      this._month = shown ? shown.slice(0, 7) : (this._month || this._attrMonth() || this._todayIso().slice(0, 7));
      this._focus = null;
    }

    _attrMonth() { const m = this.getAttribute('month'); return /^\d{4}-\d{2}$/.test(m || '') ? m : null; }
    _todayIso() { return this.getAttribute('today') || isoOf(new Date()); }

    get value() {
      if (this.getBoolAttr('range')) return this._start ? `${this._start}${this._end ? `/${this._end}` : ''}` : '';
      return this._day ? `${this._day}${this.getBoolAttr('time') && this._time ? `T${this._time}` : ''}` : '';
    }
    set value(v) {
      this._readValue(v);
      if (this._grid) this._draw(); else this._pendingValue = String(v ?? '');
    }
    get start() { return this.getBoolAttr('range') ? this._start : this._day; }
    get end() { return this.getBoolAttr('range') ? this._end : this._day; }
    get month() { return this._month; }
    set month(v) {
      if (!/^\d{4}-\d{2}$/.test(v)) return;
      this._month = v; this._focus = null;
      if (this._grid) this._draw(); else this._pendingMonth = v;
    }
    get days() { return this._days ?? {}; }
    set days(v) { this._days = v && typeof v === 'object' ? v : {}; if (this._grid) this._draw(); }

    // ── markup ──
    _build() {
      const floating = this.getBoolAttr('floating');
      this._cal = this.createElement('div', ['nk-calendar'], { role: 'group' });
      const head = this.createElement('div', ['cal-head']);
      this._title = this.createElement('div', ['cal-title'], { 'aria-live': 'polite' });
      this._todayBtn = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._prev = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._prev.innerHTML = CHEVRONS.prev;
      this._next = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._next.innerHTML = CHEVRONS.next;
      head.append(this._title, this._todayBtn, this._prev, this._next);
      this._grid = this.createElement('div', ['cal-grid']);
      this._foot = this.createElement('div', ['cal-foot']);
      this._timeInput = this.createElement('input', ['nk-input'], { type: 'time' });
      this._clear = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._cal.append(head, this._grid);
      this._pop = floating ? this.createElement('div', ['nk-pop', 'floating']) : null;
      if (this._pop) this._pop.appendChild(this._cal);
      this._wrapper.replaceChildren(this._pop ?? this._cal);
      this._draw();
    }

    get _hasTime() { return this.getBoolAttr('time') && !this.getBoolAttr('range'); }
    get _locale() { return resolveLocale(this.getAttribute('locale')); }
    get _weekStart() {
      const n = Number(this.getAttribute('week-start'));
      return this.hasAttribute('week-start') && Number.isInteger(n) && n >= 0 && n <= 6 ? n : firstWeekday(this._locale);
    }

    _syncPop() {
      this._pop?.classList.toggle('sheet', this.getBoolAttr('sheet'));
      this._pop?.classList.toggle('open', this.getBoolAttr('open'));
    }

    _draw() {
      const locale = this._locale, ws = this._weekStart, weeks = this.getBoolAttr('weeks'), range = this.getBoolAttr('range');
      const g = monthGrid(this._month, ws), today = this._todayIso();
      const min = this.getAttribute('min') || '', max = this.getAttribute('max') || '';
      const weekend = new Set((this.getAttribute('weekend') || '').split(',').map(s => s.trim()).filter(Boolean).map(Number));
      let days = this._days;
      if (!days && this.hasAttribute('days')) { try { days = JSON.parse(this.getAttribute('days')); } catch { days = {}; } }
      days ??= {};
      const long = new Intl.DateTimeFormat(locale, { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' });
      this._cal.classList.toggle('weeks', weeks);
      this._syncPop();
      const label = this.getAttribute('label');
      if (label) this._cal.setAttribute('aria-label', label); else this._cal.removeAttribute('aria-label');
      this._title.textContent = new Intl.DateTimeFormat(locale, { month: 'long', year: 'numeric' }).format(g.first);
      this._todayBtn.textContent = this.getAttribute('today-label') || 'Today';
      this._prev.setAttribute('aria-label', this.getAttribute('prev-label') || 'Previous month');
      this._next.setAttribute('aria-label', this.getAttribute('next-label') || 'Next month');

      // The one tab stop: the focused day, else the chosen one, else the 1st.
      const cells = Array.from({ length: 42 }, (_, i) => isoOf(g.day(i)));
      const chosen = range ? this._start : this._day;
      if (!this._focus || !cells.includes(this._focus)) this._focus = cells.includes(chosen) ? chosen : `${this._month}-01`;

      const nodes = [];
      if (weeks) nodes.push(this._head(this.getAttribute('week-label') || weekLabel(locale)));
      for (const w of weekdayNames(locale, ws, 2)) nodes.push(this._head(w));
      for (let i = 0; i < 42; i++) {
        const d = g.day(i), iso = cells[i], info = days[iso] || {};
        if (weeks && i % 7 === 0) { const wk = this.createElement('span', ['cal-week'], { 'aria-hidden': 'true' }); wk.textContent = isoWeek(d); nodes.push(wk); }
        const inRange = range && this._start && this._end && iso > this._start && iso < this._end;
        const cls = ['cal-day', d.getMonth() !== g.m - 1 && 'out', (weekend.has(d.getDay()) || info.off) && 'off', iso === today && 'today',
          !range && iso === this._day && 'selected', range && iso === this._start && 'start', range && iso === (this._end || this._start) && this._start && 'end', inRange && 'in-range'].filter(Boolean);
        const day = this.createElement('button', cls, { type: 'button', 'data-date': iso, tabindex: iso === this._focus ? '0' : '-1',
          'aria-pressed': String(cls.includes('selected') || cls.includes('start') || cls.includes('end')),
          'aria-label': `${long.format(d)}${info.label ? `, ${info.label}` : ''}` });
        if (info.label) day.title = info.label;
        if ((min && iso < min) || (max && iso > max)) day.setAttribute('aria-disabled', 'true');
        day.textContent = d.getDate();
        const marks = (info.marks || []).slice(0, 3);
        if (marks.length) {
          const m = this.createElement('span', ['cal-marks']);
          for (const tone of marks) m.appendChild(this.createElement('i', [tone]));
          day.appendChild(m);
        }
        nodes.push(day);
      }
      // The days are drawn anew; focus inside the grid moves to the new tab stop.
      const focused = this._grid.contains(this._shadow.activeElement);
      this._grid.replaceChildren(...nodes);
      if (focused) this.focusDay();

      const time = this._hasTime, clearable = this.getBoolAttr('clearable');
      this._timeInput.value = this._time;
      this._timeInput.setAttribute('aria-label', this.getAttribute('time-label') || 'Time');
      this._clear.textContent = this.getAttribute('clear-label') || 'Clear';
      this._foot.replaceChildren(...[time && this._timeInput, clearable && this._clear].filter(Boolean));
      if (time || clearable) this._cal.appendChild(this._foot); else this._foot.remove();
      this._syncForm();
    }

    _syncForm() {
      this.setFormValue(this.value);
      if (this.getBoolAttr('required') && !this.value) this.setValidity({ valueMissing: true }, 'Please choose a date.', this._grid?.querySelector('[tabindex="0"]') ?? undefined);
      else this.setValidity({});
    }

    _head(text) {
      const s = this.createElement('span', ['cal-wd'], { 'aria-hidden': 'true' });
      s.textContent = text;
      return s;
    }

    // ── interaction ──
    _goMonth(month, focus) {
      if (month !== this._month) { this._month = month; this.emit('nk-month', { month }); }
      this._focus = focus ?? null;
      this._draw();
    }

    _pick(iso) {
      const day = this._grid.querySelector(`[data-date="${iso}"]`);
      if (day?.getAttribute('aria-disabled') === 'true') return;
      const before = this.value;
      let done = true;
      if (this.getBoolAttr('range')) {
        if (!this._start || this._end) { this._start = iso; this._end = null; done = false; }
        else if (iso < this._start) { this._end = this._start; this._start = iso; }
        else this._end = iso;
      } else this._day = iso;
      this._focus = iso;
      if (iso.slice(0, 7) !== this._month) this._goMonth(iso.slice(0, 7), iso); else this._draw();
      if (!done) return;
      // Only a new value is a change; the same day again just closes a popover.
      if (this.value !== before) this.emit('nk-change', { value: this.value, start: this.start, end: this.end, time: this._time || null });
      if (this.getBoolAttr('floating') && !this._hasTime) this.close();
    }

    setupEvents() {
      this._onGrid = (e) => { const day = e.target.closest('.cal-day'); if (day) this._pick(day.dataset.date); };
      this._onToday = () => { const t = this._todayIso(); this._goMonth(t.slice(0, 7), t); };
      this._onPrev = () => this._goMonth(stepMonth(this._month, -1));
      this._onNext = () => this._goMonth(stepMonth(this._month, 1));
      this._onKey = (e) => {
        // Enter and Space are the day button's own click.
        const day = e.target.closest?.('.cal-day');
        if (!day) return;
        const date = parseDay(day.dataset.date), ws = this._weekStart, wd = (date.getDay() - ws + 7) % 7;
        let target = null;
        if (STEPS[e.key]) target = addDays(date, STEPS[e.key]);
        else if (e.key === 'Home') target = addDays(date, -wd);
        else if (e.key === 'End') target = addDays(date, 6 - wd);
        else if (e.key === 'PageUp' || e.key === 'PageDown') {
          const months = (e.key === 'PageUp' ? -1 : 1) * (e.shiftKey ? 12 : 1);
          const last = new Date(date.getFullYear(), date.getMonth() + months + 1, 0).getDate();
          target = new Date(date.getFullYear(), date.getMonth() + months, Math.min(date.getDate(), last));
        }
        if (!target) return;
        e.preventDefault();
        const iso = isoOf(target);
        if (iso.slice(0, 7) !== this._month) this._goMonth(iso.slice(0, 7), iso);
        else { this._focus = iso; this._draw(); }
      };
      this._onTime = () => {
        this._time = this._timeInput.value;
        this._syncForm();
        if (this._day) this.emit('nk-change', { value: this.value, start: this.start, end: this.end, time: this._time || null });
      };
      this._onClear = () => {
        if (!this.value) return;
        this._day = this._start = this._end = null; this._time = '';
        this._draw();
        this.emit('nk-change', { value: '', start: null, end: null, time: null });
      };
      // Floating: a tap outside closes it and reaches nothing else; Escape closes (captured, before a modal around it).
      this._onOutside = (e) => {
        const path = e.composedPath();
        if (path.includes(this) || (this._anchor && path.includes(this._anchor))) return;
        e.preventDefault(); e.stopPropagation();
        this.close();
      };
      this._onEscape = (e) => { if (e.key === 'Escape' && this.getBoolAttr('floating') && this.getBoolAttr('open')) { e.stopPropagation(); this.close(); } };
      this._grid.addEventListener('click', this._onGrid);
      this._grid.addEventListener('keydown', this._onKey);
      this._todayBtn.addEventListener('click', this._onToday);
      this._prev.addEventListener('click', this._onPrev);
      this._next.addEventListener('click', this._onNext);
      this._timeInput.addEventListener('change', this._onTime);
      this._clear.addEventListener('click', this._onClear);
      document.addEventListener('keydown', this._onEscape, true);
      if (this.getBoolAttr('open')) document.addEventListener('click', this._onOutside, true);
    }

    teardownEvents() {
      this._grid?.removeEventListener('click', this._onGrid);
      this._grid?.removeEventListener('keydown', this._onKey);
      this._todayBtn?.removeEventListener('click', this._onToday);
      this._prev?.removeEventListener('click', this._onPrev);
      this._next?.removeEventListener('click', this._onNext);
      this._timeInput?.removeEventListener('change', this._onTime);
      this._clear?.removeEventListener('click', this._onClear);
      document.removeEventListener('keydown', this._onEscape, true);
      document.removeEventListener('click', this._onOutside, true);
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'value') { this._readValue(value); this._draw(); return; }
      if (name === 'month') { this.month = value; return; }
      if (name === 'open' || name === 'sheet') { this._syncPop(); if (name === 'open') this._syncOpen(); return; }
      if (name === 'align') return;
      if (name === 'floating') { this.teardownEvents(); this._build(); if (this.isConnected) this.setupEvents(); return; }
      if (name === 'range') this._readValue(this.getAttribute('value'));
      if (name === 'days') this._days = null;
      this._draw();
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      if (this._anchor) (this._anchor.shadowRoot?.querySelector('button, a[href]') ?? this._anchor).setAttribute('aria-expanded', String(open));
      if (open) {
        this._returnFocus = deepActiveElement();
        if (this._returnFocus?.matches(':focus-visible')) requestAnimationFrame(() => this.focusDay());
        document.addEventListener('click', this._onOutside, true);
      } else {
        document.removeEventListener('click', this._onOutside, true);
        const back = this._returnFocus;
        this._returnFocus = null;
        const active = deepActiveElement();
        if (back?.isConnected && active && this._shadow.contains(active)) back.focus({ preventScroll: true });
      }
      this.emit('nk-toggle', { open });
    }

    // ── form ──
    resetValue() { this._readValue(this._initialValue); this._draw(); }
    restoreValue(state) { this._readValue(state); this._draw(); }
    onFormDisabled(disabled) { this._cal?.toggleAttribute('inert', disabled); }

    // ── API ──
    /** Opens under `anchor` (with `floating`), on the month of the value. */
    show(anchor) {
      if (anchor) { this._anchor = anchor; placeUnder(this, anchor, this.getAttribute('align')); }
      const shown = this.start;
      if (shown && shown.slice(0, 7) !== this._month) { this._month = shown.slice(0, 7); this._focus = null; this._draw(); }
      this.setBoolAttr('open', true);
    }
    close() { this.setBoolAttr('open', false); }
    toggle(anchor) { this.getBoolAttr('open') ? this.close() : this.show(anchor); }
    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    /** Moves focus to the day with the tab stop. */
    focusDay() { this._grid?.querySelector('[tabindex="0"]')?.focus(); }
  }

  customElements.define('nk-calendar', NkCalendar);

  // <nk-peek id="peek" label="Database Table-View">
  //   <nk-btn slot="actions" variant="topbar" aria-label="Open as page"><svg …></svg></nk-btn>
  //   <nk-page-title>🗃️ Database Table-View</nk-page-title>
  //   <nk-props>…</nk-props>
  //   <div class="nk-prose">…</div>
  //   <nk-comments>…</nk-comments>
  // </nk-peek>
  // → <div class="nk-peek-backdrop open"><aside class="nk-peek" role="dialog">
  //     <div class="pk-bar"><button class="nk-topbar-btn">»</button>…actions…</div>
  //     <div class="pk-body">…</div></aside></div>
  //
  // Notion's side peek (NotionKit 1.8.0): a row beside the table, which stays
  // usable – no scrim, nothing inert. show() slides it in and moves focus to
  // it; », Escape and a click elsewhere on the page close it, and Escape and »
  // hand focus back. A click that calls show() again – on another row – only
  // swaps the content, and clicks inside other overlays (a menu opened from
  // the peek) leave it open. Below 860px it is a bottom sheet over a dimmed
  // page; there it is modal: the page is inert and scroll-locked, the dimmed
  // page takes the tap that closes it. Put it directly under <body>.
  //
  // `resizable` (NotionKit 1.10.0): the left edge takes the pointer – and the
  // arrow keys, 16px a step – to make the peek wider or narrower, as in
  // Notion: from `min` (380) to `max` (all but 320px of the window). The width
  // is the token --nk-peek-width, set on :root; `width` (px) sets it too, and
  // nk-resize { width } fires when a drag or a key ends – the moment to keep
  // it. resize-label names the edge (default "Resize").
  // `inset`: while it is open on the desktop, the page's <nk-app> gets
  // `peek-inset` – its main column makes room instead of lying under the peek.
  const CLOSE_ICON = '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="m6 17 5-5-5-5M13 17l5-5-5-5"/></svg>';
  const OVERLAYS = new Set(['nk-menu', 'nk-pop', 'nk-modal', 'nk-sheet', 'nk-dialog', 'nk-cmdk', 'nk-toast']);
  const OVERLAY_CLASSES = ['nk-pop', 'nk-modal-backdrop', 'nk-sheet-backdrop', 'nk-dialog-backdrop', 'nk-cmdk-backdrop'];
  const phone = () => matchMedia('(max-width: 860px)').matches;

  class NkPeek extends NkElement {
    static get observedAttributes() { return ['open', 'label', 'close-label', 'resizable', 'width', 'min', 'max', 'resize-label', 'inset']; }

    render() {
      this._backdrop = this.createElement('div', ['nk-peek-backdrop']);
      this._box = this.createElement('aside', ['nk-peek'], { role: 'dialog', tabindex: '-1' });
      this._handle = this.createElement('div', ['pk-resize'], { role: 'separator', 'aria-orientation': 'vertical', tabindex: '0' });
      this._box.appendChild(this._handle);
      const bar = this.createElement('div', ['pk-bar']);
      this._close = this.createElement('button', ['nk-topbar-btn'], { type: 'button' });
      this._close.innerHTML = CLOSE_ICON;
      bar.append(this._close, this.createElement('slot', [], { name: 'actions' }));
      const body = this.createElement('div', ['pk-body']);
      body.appendChild(document.createElement('slot'));
      this._box.append(bar, body);
      this._backdrop.appendChild(this._box);
      this._wrapper.appendChild(this._backdrop);
      this._syncLabels();
      this._syncResize();
      if (this.hasAttribute('width')) this._setWidth(Number(this.getAttribute('width')));
      this._syncOpen();
    }

    // ── width ──
    get _min() { return Number(this.getAttribute('min')) || 380; }
    get _max() { return Number(this.getAttribute('max')) || Math.max(this._min, innerWidth - 320); }
    /** The peek's width in px – the token --nk-peek-width. */
    get width() { return parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--nk-peek-width')) || 560; }
    set width(v) { this._setWidth(Number(v)); }

    _setWidth(w) {
      if (!Number.isFinite(w)) return;
      w = Math.round(Math.min(Math.max(w, this._min), Math.max(this._min, this._max)));
      document.documentElement.style.setProperty('--nk-peek-width', `${w}px`);
      this._handle.setAttribute('aria-valuenow', w);
    }

    _syncResize() {
      this._handle.hidden = !this.getBoolAttr('resizable');
      this._handle.setAttribute('aria-label', this.getAttribute('resize-label') || 'Resize');
      this._handle.setAttribute('aria-valuemin', this._min);
      this._handle.setAttribute('aria-valuenow', Math.round(this.width));
    }

    /** The app whose main column makes room: the peek's own, else the page's. */
    _app() { return this.closest('nk-app') ?? document.querySelector('nk-app'); }
    _syncInset() {
      const on = this.getBoolAttr('inset') && this.getBoolAttr('open') && !phone();
      if (on) this._app()?.setAttribute('peek-inset', '');
      else if (this._insetApp) this._app()?.removeAttribute('peek-inset');
      this._insetApp = on;
    }

    _syncLabels() {
      const label = this.getAttribute('label');
      if (label) this._box.setAttribute('aria-label', label); else this._box.removeAttribute('aria-label');
      this._close.setAttribute('aria-label', this.getAttribute('close-label') || 'Close');
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      this._backdrop.classList.toggle('open', open);
      this._syncInset();
      if (open === this._wasOpen) return;
      this._wasOpen = open;
      if (open) {
        this._returnFocus = deepActiveElement();
        if (phone()) {
          this._modal = true;
          this._box.setAttribute('aria-modal', 'true');
          lockScroll();
          this._undoInert = inertOutside(this);
        }
        requestAnimationFrame(() => this._box.focus({ preventScroll: true }));
      } else {
        if (this._modal) { this._modal = false; this._box.removeAttribute('aria-modal'); unlockScroll(); this._undoInert?.(); this._undoInert = null; }
        const active = deepActiveElement();
        const inside = active && (this.contains(active) || this._shadow.contains(active) || active === document.body);
        const back = this._returnFocus;
        this._returnFocus = null;
        if (inside && back?.isConnected && typeof back.focus === 'function') back.focus({ preventScroll: true });
      }
    }

    setupEvents() {
      this._onClose = () => this.close();
      this._onBackdrop = (e) => { if (e.target === this._backdrop) this.close(); };
      this._onKey = (e) => { if (e.key === 'Escape' && this.getBoolAttr('open')) this.close(); };
      // Bubble phase, after the page's own handlers: a click that showed the
      // peek again (another row) must not close it.
      this._onOutside = (e) => {
        if (!this.getBoolAttr('open') || this._shownAt >= e.timeStamp) return;
        const path = e.composedPath();
        if (path.includes(this)) return;
        if (path.some(n => n.nodeType === 1 && (OVERLAYS.has(n.localName) || OVERLAY_CLASSES.some(c => n.classList.contains(c))))) return;
        this.close();
      };
      // The left edge: a drag with pointer capture, or the arrow keys.
      const resized = () => this.emit('nk-resize', { width: Math.round(this.width) });
      this._onResizeStart = (e) => {
        if (e.button !== 0) return;
        e.preventDefault();
        this._handle.setPointerCapture(e.pointerId);
        this._handle.classList.add('active');
      };
      this._onResizeMove = (e) => { if (this._handle.hasPointerCapture(e.pointerId)) this._setWidth(innerWidth - e.clientX); };
      this._onResizeEnd = () => { this._handle.classList.remove('active'); resized(); };
      this._onResizeKey = (e) => {
        const step = { ArrowLeft: 16, ArrowRight: -16 }[e.key];
        if (!step) return;
        e.preventDefault();
        this._setWidth(this.width + step);
        resized();
      };
      this._onViewport = () => this._syncInset();
      this._close.addEventListener('click', this._onClose);
      this._backdrop.addEventListener('click', this._onBackdrop);
      this._handle.addEventListener('pointerdown', this._onResizeStart);
      this._handle.addEventListener('pointermove', this._onResizeMove);
      this._handle.addEventListener('lostpointercapture', this._onResizeEnd);
      this._handle.addEventListener('keydown', this._onResizeKey);
      document.addEventListener('keydown', this._onKey);
      document.addEventListener('click', this._onOutside);
      addEventListener('resize', this._onViewport);
    }

    teardownEvents() {
      this._close?.removeEventListener('click', this._onClose);
      this._backdrop?.removeEventListener('click', this._onBackdrop);
      this._handle?.removeEventListener('pointerdown', this._onResizeStart);
      this._handle?.removeEventListener('pointermove', this._onResizeMove);
      this._handle?.removeEventListener('lostpointercapture', this._onResizeEnd);
      this._handle?.removeEventListener('keydown', this._onResizeKey);
      document.removeEventListener('keydown', this._onKey);
      document.removeEventListener('click', this._onOutside);
      removeEventListener('resize', this._onViewport);
      if (this._insetApp) { this._app()?.removeAttribute('peek-inset'); this._insetApp = false; }
      if (this._modal) { this._modal = false; unlockScroll(); this._undoInert?.(); this._undoInert = null; }
      this._wasOpen = false;
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'width') { this._setWidth(Number(value)); return; }
      if (name === 'inset') { this._syncInset(); return; }
      if (name !== 'open') { this._syncLabels(); this._syncResize(); return; }
      this._syncOpen();
      this.emit('nk-toggle', { open: this.getBoolAttr('open') });
    }

    show() { this._shownAt = performance.now(); this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.getBoolAttr('open') ? this.close() : this.show(); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { v ? this.show() : this.close(); }
  }

  customElements.define('nk-peek', NkPeek);

  // <nk-dialog id="trash" title="Move “Roadmap” to trash?" alert>
  //   The page and its sub-pages can be restored from Trash for 30 days.
  //   <nk-btn slot="actions" variant="secondary" value="">Cancel</nk-btn>
  //   <nk-btn slot="actions" variant="danger-solid" value="trash">Move to trash</nk-btn>
  // </nk-dialog>
  // → <div class="nk-dialog-backdrop open"><div class="nk-dialog" role="alertdialog">
  //     <div class="dl-title">…</div><div class="dl-body">…</div><div class="dl-actions">…</div></div></div>
  //
  // A question or a short form (NotionKit 1.10.0) with the contract of
  // <nk-modal> and <nk-sheet>: show(), close(), toggle(), nk-toggle; Escape –
  // captured, so it closes before a peek, a menu or a modal behind it – and
  // the backdrop close it; focus moves in, to an [autofocus] element or the
  // first field or button, and back out; the page behind is inert and
  // scroll-locked. It lies above the modal and the sheet, so a question can
  // come from either. 440px, `wide` 560px; below 860px a bottom sheet with the
  // buttons stacked, the confirming one on top.
  // A button in slot="actions" with a `value` closes it with that value; so
  // does the submit of a <form method="dialog"> inside, with its submitter's
  // value. Before it closes, nk-close { value } fires and can be cancelled –
  // the place to check an input dialog; Escape and the backdrop close with ''.
  // `returnValue` keeps the last value. `alert` makes it an alertdialog, named
  // and described by its title and text. `title` is the heading, never a
  // tooltip – see NkElement.takeTitle(). Put it directly under <body>.
  class NkDialog extends NkElement {
    static get observedAttributes() { return ['open', 'title', 'wide', 'alert']; }

    render() {
      this.takeTitle();
      this.returnValue = '';
      this._backdrop = this.createElement('div', ['nk-dialog-backdrop']);
      this._backdrop.inert = true;
      this._box = this.createElement('div', ['nk-dialog'], { 'aria-modal': 'true', tabindex: '-1' });
      this._heading = this.createElement('div', ['dl-title'], { id: 'title' });
      const body = this.createElement('div', ['dl-body'], { id: 'body' });
      body.appendChild(document.createElement('slot'));
      this._actions = this.createElement('div', ['dl-actions']);
      this._actionsSlot = this.createElement('slot', [], { name: 'actions' });
      this._actions.appendChild(this._actionsSlot);
      this._box.append(this._heading, body, this._actions);
      this._backdrop.appendChild(this._box);
      this._wrapper.appendChild(this._backdrop);
      this._syncTitle();
      this._syncMode();
      this._syncOpen();
    }

    _syncTitle() {
      const text = this._titleText || '';
      this._heading.textContent = text;
      this._heading.hidden = !text;
      if (text) this._box.setAttribute('aria-labelledby', 'title');
      else this._box.removeAttribute('aria-labelledby');
    }

    _syncMode() {
      const alert = this.getBoolAttr('alert');
      this._box.setAttribute('role', alert ? 'alertdialog' : 'dialog');
      if (alert) this._box.setAttribute('aria-describedby', 'body'); else this._box.removeAttribute('aria-describedby');
      this._box.classList.toggle('wide', this.getBoolAttr('wide'));
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      this._backdrop.classList.toggle('open', open);
      this._backdrop.inert = !open;
      if (open === this._wasOpen) return;
      this._wasOpen = open;
      if (open) {
        this._returnFocus = this._from ?? deepActiveElement();
        this._from = null;
        // An overlay behind – a modal – made this host inert; the dialog lifts that while it is open.
        this._wasInert = this.inert;
        this.inert = false;
        lockScroll();
        this._undoInert = inertOutside(this);
        requestAnimationFrame(() => (this.querySelector('[autofocus]') || firstFocusable(this) || this._box).focus({ preventScroll: true }));
      } else {
        unlockScroll();
        this._undoInert?.(); this._undoInert = null;
        this.inert = this._wasInert;
        const back = this._returnFocus;
        this._returnFocus = null;
        if (back?.isConnected && typeof back.focus === 'function') back.focus({ preventScroll: true });
      }
    }

    setupEvents() {
      this._onBackdrop = (e) => { if (e.target === this._backdrop) this.close(''); };
      this._onKey = (e) => { if (e.key === 'Escape' && this.getBoolAttr('open')) { e.stopPropagation(); this.close(''); } };
      // A button with a value closes it; a submit button is the form's.
      this._onClick = (e) => {
        const button = e.composedPath().find(n => n instanceof Element && n !== this && this.contains(n) && n.hasAttribute('value') && (n.localName === 'nk-btn' || n.localName === 'button'));
        if (!button || button.getAttribute('type') === 'submit') return;
        this.close(button.getAttribute('value'));
      };
      this._onSubmit = (e) => {
        if ((e.target.getAttribute('method') || '').toLowerCase() !== 'dialog') return;
        e.preventDefault();
        this.close(e.submitter?.value || e.submitter?.getAttribute?.('value') || 'submit');
      };
      this._onSlot = () => { this._actions.hidden = !this._actionsSlot.assignedElements().length; };
      this._backdrop.addEventListener('click', this._onBackdrop);
      this.addEventListener('click', this._onClick);
      this.addEventListener('submit', this._onSubmit);
      this._actionsSlot.addEventListener('slotchange', this._onSlot);
      document.addEventListener('keydown', this._onKey, true);
      this._onSlot();
    }

    teardownEvents() {
      this._backdrop?.removeEventListener('click', this._onBackdrop);
      this.removeEventListener('click', this._onClick);
      this.removeEventListener('submit', this._onSubmit);
      this._actionsSlot?.removeEventListener('slotchange', this._onSlot);
      document.removeEventListener('keydown', this._onKey, true);
      if (this._wasOpen) { unlockScroll(); this._undoInert?.(); this._undoInert = null; this._wasOpen = false; }
    }

    onAttributeChanged(name, oldValue, value) {
      if (name === 'title') { if (this.takeTitle(value)) this._syncTitle(); return; }
      if (name !== 'open') { this._syncMode(); return; }
      this._syncOpen();
      this.emit('nk-toggle', { open: this.getBoolAttr('open') });
    }

    /** Opens it; focus goes back to `from` when it closes (default: what had focus – pass the ⋯ of a menu that closes). */
    show(from) { this.returnValue = ''; this._from = from ?? null; this.setBoolAttr('open', true); }
    /** Closes with `value`, unless an nk-close listener cancels it. Returns whether it closed. */
    close(value = '') {
      if (!this.getBoolAttr('open')) return false;
      if (!this.emit('nk-close', { value })) return false;
      this.returnValue = value;
      this.setBoolAttr('open', false);
      return true;
    }
    toggle() { this.getBoolAttr('open') ? this.close('') : this.show(); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { v ? this.show() : this.close(''); }
    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._syncTitle(); }
  }

  customElements.define('nk-dialog', NkDialog);

  // <nk-tooltip></nk-tooltip>                    one for the page: every [data-tooltip]
  //   <button data-tooltip="Close sidebar" data-tooltip-key="⌘\">«</button>
  // <nk-tooltip for="saveBtn" shortcut="⌘S">Save the page</nk-tooltip>
  // tip.show(barOrRect, 'Shell · 12.10.–30.11.'); tip.hide();
  // → <div class="nk-tooltip open" role="tooltip">Close sidebar<span class="tt-key">⌘\</span></div>
  //
  // Notion's hover hint (NotionKit 1.10.0). Without `for` it serves every
  // element with data-tooltip – and data-tooltip-key for a shortcut – on the
  // page, inside shadow roots too: the tooltips of a whole toolbar in one
  // element. With `for` it belongs to the element with that id and shows its
  // own content and `shortcut`. It appears after `delay` ms (400) under the
  // pointer and at once on keyboard focus, never on touch; leaving, a press,
  // blur, scrolling and Escape hide it. It sits 6px below its target and
  // centred, above it where the window ends – placement="top" prefers above –
  // and 8px inside the window (placeNear in util/floating.js). show(target,
  // text, shortcut) takes an element or a rect, for what has no element of
  // its own: the bars of a Gantt chart. While it shows, the target's
  // aria-describedby names it (in the same document or shadow root).
  const hostSheet = new CSSStyleSheet();
  hostSheet.replaceSync(`:host { display: block; position: fixed; top: 0; left: 0; z-index: 130; pointer-events: none; }`);
  let count = 0;

  class NkTooltip extends NkElement {
    static get hostStyles() { return hostSheet; }
    static get observedAttributes() { return ['for', 'shortcut']; }

    render() {
      this._bubble = this.createElement('div', ['nk-tooltip'], { role: 'tooltip' });
      this._slot = document.createElement('slot');
      this._text = document.createTextNode('');
      this._key = this.createElement('span', ['tt-key']);
      this._bubble.append(this._slot, this._text, this._key);
      this._wrapper.appendChild(this._bubble);
      this._fill(null);
    }

    get _delay() { const d = Number(this.getAttribute('delay')); return this.hasAttribute('delay') && d >= 0 ? d : 400; }

    /** Own content (text null) or a given text and shortcut. */
    _fill(text, key = this.getAttribute('shortcut')) {
      this._slot.style.display = text == null ? '' : 'none';
      this._text.data = text ?? '';
      this._key.textContent = key || '';
      this._key.hidden = !key;
    }

    /** Shows it at `target` – an element or a rect – with `text` (else its own content) and `shortcut`. */
    show(target, text, shortcut) {
      clearTimeout(this._timer);
      if (!target) return;
      this._fill(text ?? null, shortcut);
      this._describe(target);
      placeNear(this, target, this.getAttribute('placement') === 'top' ? 'top' : 'bottom');
      this._bubble.classList.add('open');
    }

    hide() {
      clearTimeout(this._timer);
      this._bubble?.classList.remove('open');
      this._describe(null);
      this._target = null;
    }

    get open() { return !!this._bubble?.classList.contains('open'); }

    _describe(target) {
      if (this._described) {
        const ids = (this._described.getAttribute('aria-describedby') || '').split(/\s+/).filter(id => id && id !== this.id);
        ids.length ? this._described.setAttribute('aria-describedby', ids.join(' ')) : this._described.removeAttribute('aria-describedby');
        this._described = null;
      }
      if (!(target instanceof Element) || target.getRootNode() !== this.getRootNode()) return;
      this.id ||= `nk-tooltip-${++count}`;
      const ids = (target.getAttribute('aria-describedby') || '').split(/\s+/).filter(Boolean);
      if (!ids.includes(this.id)) target.setAttribute('aria-describedby', [...ids, this.id].join(' '));
      this._described = target;
    }

    /** The element this tooltip serves for an event: its `for` target, or the nearest [data-tooltip]. */
    _targetOf(e) {
      const path = e.composedPath();
      const own = this.getAttribute('for');
      if (own) { const el = this.getRootNode().getElementById?.(own); return el && path.includes(el) ? el : null; }
      return path.find(n => n instanceof Element && n.hasAttribute('data-tooltip')) ?? null;
    }

    _showFor(el) {
      const own = this.hasAttribute('for');
      this.show(el, own ? null : el.getAttribute('data-tooltip'), own ? this.getAttribute('shortcut') : el.getAttribute('data-tooltip-key'));
    }

    setupEvents() {
      this._onOver = (e) => {
        const el = e.pointerType === 'touch' ? null : this._targetOf(e);
        if (el === this._target) return;
        this.hide();
        if (!el) return;
        this._target = el;
        this._timer = setTimeout(() => this._showFor(el), this._delay);
      };
      this._onFocus = (e) => {
        const el = this._targetOf(e);
        if (!el || !deepActiveElement()?.matches(':focus-visible')) return;
        this.hide();
        this._target = el;
        this._showFor(el);
      };
      this._onHide = () => this.hide();
      this._onKey = (e) => { if (e.key === 'Escape' && this.open) this.hide(); };
      document.addEventListener('pointerover', this._onOver);
      document.addEventListener('focusin', this._onFocus);
      document.addEventListener('focusout', this._onHide);
      document.addEventListener('pointerdown', this._onHide, true);
      document.addEventListener('scroll', this._onHide, true);
      document.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      document.removeEventListener('pointerover', this._onOver);
      document.removeEventListener('focusin', this._onFocus);
      document.removeEventListener('focusout', this._onHide);
      document.removeEventListener('pointerdown', this._onHide, true);
      document.removeEventListener('scroll', this._onHide, true);
      document.removeEventListener('keydown', this._onKey);
      this.hide();
    }

    onAttributeChanged(name) { if (name === 'shortcut' && !this._text.data) this._fill(null); if (name === 'for') this.hide(); }
  }

  customElements.define('nk-tooltip', NkTooltip);

  // <nk-modal id="settings">
  //   <nk-settings-user slot="user" name="…" mail="…"></nk-settings-user>
  //   <nk-settings-pane name="profile" group="Account" icon="👤" label="My profile" title="My profile" active>…</nk-settings-pane>
  //   <nk-settings-pane name="members" group="Workspace" icon="👥" label="Members" title="Members">…</nk-settings-pane>
  // </nk-modal>
  // → <div class="nk-modal-backdrop open"><div class="nk-modal" role="dialog">
  //     <nav class="nk-settings-nav">…user… section labels + tree rows…</nav>
  //     <div class="nk-settings-content">…panes…</div></div></div>
  //
  // The modal renders the nav rows itself from the panes' label/icon/group, so
  // `.nk-settings-nav .nk-tree-item` and the 860px icon-rail rules match with
  // no JS breakpoint. Escape and the backdrop close it; focus moves in and
  // back out; the page behind is scroll-locked and inert. Put the element
  // directly under <body>: a transformed ancestor would trap the fixed backdrop.
  class NkModal extends NkElement {
    static get observedAttributes() { return ['open', 'pane']; }

    render() {
      this._backdrop = this.createElement('div', ['nk-modal-backdrop']);
      this._backdrop.inert = true;
      this._box = this.createElement('div', ['nk-modal'], { role: 'dialog', 'aria-modal': 'true', tabindex: '-1' });
      this._nav = this.createElement('nav', ['nk-settings-nav'], { 'aria-label': 'Settings' });
      this._nav.appendChild(this.createElement('slot', [], { name: 'user' }));
      this._rows = document.createElement('div');
      this._nav.appendChild(this._rows);
      this._nav.appendChild(this.createElement('slot', [], { name: 'nav' }));
      this._content = this.createElement('div', ['nk-settings-content']);
      this._slot = document.createElement('slot');
      this._content.appendChild(this._slot);
      this._box.append(this._nav, this._content);
      this._backdrop.appendChild(this._box);
      this._wrapper.appendChild(this._backdrop);
      this._syncOpen();
      this.requestNav();
    }

    get panes() { return [...this.querySelectorAll(':scope > nk-settings-pane')]; }

    /** Rebuilds the nav from the panes (debounced – panes upgrade after the modal). */
    requestNav() {
      if (this._navQueued) return;
      this._navQueued = true;
      queueMicrotask(() => { this._navQueued = false; this._buildNav(); });
    }

    _buildNav() {
      if (!this._rows) return;
      const panes = this.panes;
      const labelled = panes.filter(p => p.getAttribute('label'));
      this._rows.replaceChildren();
      this._nav.style.display = (labelled.length || this.querySelector(':scope > [slot="nav"], :scope > [slot="user"]')) ? '' : 'none';
      let lastGroup = null;
      for (const pane of labelled) {
        const group = pane.getAttribute('group');
        if (group && group !== lastGroup) {
          const label = this.createElement('div', ['nk-section-label']);
          label.textContent = group;
          this._rows.appendChild(label);
          lastGroup = group;
        }
        const row = this.createElement('div', ['nk-tree-item'], { role: 'tab', tabindex: '0', 'data-pane': pane.name });
        const icon = this.createElement('span', ['icon']);
        icon.textContent = pane.getAttribute('icon') || '';
        const text = this.createElement('span', ['label']);
        text.textContent = pane.getAttribute('label');
        row.append(icon, text);
        this._rows.appendChild(row);
      }
      this._syncActive();
    }

    /** Marks the active pane and its nav row without rebuilding the rows (keeps focus). */
    _syncActive() {
      const panes = this.panes;
      let current = this.getAttribute('pane');
      if (!panes.some(p => p.name === current)) current = (panes.find(p => p.hasAttribute('active')) || panes[0])?.name ?? null;
      for (const row of this._rows.querySelectorAll('[data-pane]')) {
        const on = row.dataset.pane === current;
        row.classList.toggle('active', on);
        row.setAttribute('aria-selected', on ? 'true' : 'false');
      }
      for (const pane of panes) pane.active = pane.name === current;
      this._current = current;
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      this._backdrop.classList.toggle('open', open);
      this._backdrop.inert = !open;
      if (open === this._wasOpen) return;
      this._wasOpen = open;
      if (open) {
        this._returnFocus = document.activeElement;
        lockScroll();
        this._undoInert = inertOutside(this);
        requestAnimationFrame(() => {
          const target = this._rows.querySelector('.nk-tree-item.active') || firstFocusable(this) || this._box;
          target.focus({ preventScroll: true });
        });
      } else {
        unlockScroll();
        this._undoInert?.(); this._undoInert = null;
        const back = this._returnFocus;
        if (back && typeof back.focus === 'function' && back.isConnected) back.focus({ preventScroll: true });
        this._returnFocus = null;
      }
    }

    setupEvents() {
      this._onBackdrop = (e) => { if (e.target === this._backdrop) this.close(); };
      this._onKey = (e) => { if (e.key === 'Escape' && this.getBoolAttr('open')) { e.stopPropagation(); this.close(); } };
      this._onNavClick = (e) => { const row = e.target.closest('[data-pane]'); if (row) { row.focus(); this.pane = row.dataset.pane; } };
      this._onNavKey = (e) => {
        const row = e.target.closest?.('[data-pane]');
        if (!row) return;
        if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); this.pane = row.dataset.pane; return; }
        const dir = { ArrowDown: 1, ArrowUp: -1 }[e.key];
        if (!dir) return;
        const rows = [...this._rows.querySelectorAll('[data-pane]')];
        const next = rows[(rows.indexOf(row) + dir + rows.length) % rows.length];
        e.preventDefault(); next.focus(); this.pane = next.dataset.pane;
      };
      this._onSlot = () => this.requestNav();
      this._backdrop.addEventListener('click', this._onBackdrop);
      document.addEventListener('keydown', this._onKey);
      this._rows.addEventListener('click', this._onNavClick);
      this._rows.addEventListener('keydown', this._onNavKey);
      this._slot.addEventListener('slotchange', this._onSlot);
    }

    teardownEvents() {
      this._backdrop?.removeEventListener('click', this._onBackdrop);
      document.removeEventListener('keydown', this._onKey);
      this._rows?.removeEventListener('click', this._onNavClick);
      this._rows?.removeEventListener('keydown', this._onNavKey);
      this._slot?.removeEventListener('slotchange', this._onSlot);
      if (this._wasOpen) { unlockScroll(); this._undoInert?.(); this._undoInert = null; this._wasOpen = false; }
    }

    onAttributeChanged(name) {
      if (name === 'open') {
        this._syncOpen();
        this.emit('nk-toggle', { open: this.getBoolAttr('open') });
      } else if (name === 'pane') {
        const before = this._current;
        this._syncActive();
        if (this._current !== before) this.emit('nk-select', { value: this._current, label: this.panes.find(p => p.name === this._current)?.getAttribute('label') ?? this._current });
      }
    }

    show(pane) { if (pane) this.setAttribute('pane', pane); this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get pane() { return this._current ?? this.getAttribute('pane'); }
    set pane(v) { this.setAttribute('pane', v); }
  }

  customElements.define('nk-modal', NkModal);

  // <nk-settings-pane name="profile" label="My profile" icon="👤" group="Account" title="My profile" active>…</nk-settings-pane>
  // `title` is the heading, never a tooltip – see NkElement.takeTitle().
  // → <section class="nk-settings-pane active"><h2>My profile</h2>…</section>
  // `label`, `icon` and `group` feed the nav that <nk-modal> renders; `title`
  // renders the pane heading (child-combinator rule, so it lives here). Slotted
  // <h2>/<h3> are styled by their twins.
  class NkSettingsPane extends NkElement {
    static get observedAttributes() { return ['name', 'label', 'icon', 'group', 'title', 'active']; }

    render() {
      this._section = this.createElement('section', ['nk-settings-pane'], { role: 'tabpanel' });
      this._h2 = document.createElement('h2');
      this._section.appendChild(this._h2);
      this._section.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(this._section);
      this.takeTitle();
      this._sync();
    }

    _sync() {
      const title = this._titleText;
      this._h2.textContent = title || '';
      this._h2.style.display = title ? '' : 'none';
      this._section.classList.toggle('active', this.getBoolAttr('active'));
      this._section.setAttribute('aria-label', this.getAttribute('label') || title || this.getAttribute('name') || '');
    }

    onAttributeChanged(name, _old, value) {
      if (name === 'title' && !this.takeTitle(value)) return;
      this._sync();
      if (['label', 'icon', 'group', 'name'].includes(name)) this.closest('nk-modal')?.requestNav?.();
    }

    get title() { return this._titleText ?? ''; }
    set title(v) { this._titleText = v == null ? '' : String(v); if (this._initialized) this._sync(); }
    get name() { return this.getAttribute('name') ?? this.getAttribute('label') ?? ''; }
    get active() { return this.getBoolAttr('active'); }
    set active(v) { this.setBoolAttr('active', v); }
  }

  customElements.define('nk-settings-pane', NkSettingsPane);

  // <nk-settings-user slot="user" name="Marcel Karas" mail="marcel@…" avatar="MK"></nk-settings-user>
  // → <div class="nk-settings-user"><div class="avatar">MK</div><div class="u-text"><div class="name">…</div><div class="mail">…</div></div></div>
  class NkSettingsUser extends NkElement {
    static get observedAttributes() { return ['name', 'mail', 'avatar']; }

    render() {
      const box = this.createElement('div', ['nk-settings-user']);
      this._avatar = this.createElement('div', ['avatar']);
      const s = this.createElement('slot', [], { name: 'avatar' });
      s.appendChild(this._avatar);
      const text = this.createElement('div', ['u-text']);
      this._name = this.createElement('div', ['name']);
      this._mail = this.createElement('div', ['mail']);
      text.append(this._name, this._mail);
      box.append(s, text);
      this._wrapper.appendChild(box);
      this._sync();
    }

    _sync() {
      const name = this.getAttribute('name') || '';
      this._name.textContent = name;
      const mail = this.getAttribute('mail');
      this._mail.textContent = mail || '';
      this._mail.style.display = mail ? '' : 'none';
      this._avatar.textContent = this.getAttribute('avatar') || name.split(/\s+/).map(w => w[0]).join('').slice(0, 2).toUpperCase();
    }

    onAttributeChanged() { this._sync(); }
  }

  customElements.define('nk-settings-user', NkSettingsUser);

  // Subsequence scoring for the command palette: every query character must
  // appear in order; consecutive hits, word starts and an early first hit score
  // higher. Returns 0 when the query does not match.
  function fuzzyScore(query, text) {
    const q = query.trim().toLowerCase();
    if (!q) return 1;
    const t = text.toLowerCase();
    let ti = 0, score = 0, streak = 0;
    for (let qi = 0; qi < q.length; qi++) {
      const idx = t.indexOf(q[qi], ti);
      if (idx < 0) return 0;
      const wordStart = idx === 0 || /[\s\-_/.]/.test(t[idx - 1]);
      streak = idx === ti ? streak + 1 : 0;
      score += 1 + streak * 2 + (wordStart ? 3 : 0) - Math.min(3, (idx - ti) * 0.25);
      ti = idx + 1;
    }
    if (t.startsWith(q)) score += 10;
    return Math.max(0.01, score / (1 + t.length / 40));
  }

  // <nk-cmdk id="palette" placeholder="Search or type a command …"></nk-cmdk>
  // palette.commands = [
  //   { group: 'Pages',   items: [{ id: 'mvp', icon: '🚀', label: 'NotionKit MVP', keywords: 'project' }] },
  //   { group: 'Actions', items: [{ id: 'theme', icon: '🌙', label: 'Toggle theme', shortcut: '⌘⇧L', action: () => … }] },
  // ];
  // → <div class="nk-cmdk-backdrop open"><div class="nk-cmdk"><div class="nk-cmdk-input-row">🔍<input><kbd>esc</kbd></div>
  //     <div class="nk-cmdk-list">…</div><div class="nk-cmdk-footer">…</div></div></div>
  // mod+K toggles (attribute `hotkey`), ↑↓ move, Enter picks (nk-command +
  // item.action), Escape / backdrop close. Fuzzy subsequence search over
  // label + keywords. Put the element directly under <body>.
  class NkCmdk extends NkElement {
    static get observedAttributes() { return ['open', 'hotkey', 'placeholder']; }

    render() {
      this._backdrop = this.createElement('div', ['nk-cmdk-backdrop']);
      this._backdrop.inert = true;
      this._box = this.createElement('div', ['nk-cmdk'], { role: 'dialog', 'aria-modal': 'true', 'aria-label': 'Command palette' });
      const row = this.createElement('div', ['nk-cmdk-input-row']);
      const glass = document.createElement('span');
      glass.style.fontSize = '15px';
      glass.textContent = '🔍';
      this._input = this.createElement('input', [], { type: 'text', autocomplete: 'off', role: 'combobox', 'aria-expanded': 'true', 'aria-autocomplete': 'list' });
      const esc = this.createElement('kbd', ['nk-kbd']);
      esc.textContent = 'esc';
      row.append(glass, this._input, esc);
      this._list = this.createElement('div', ['nk-cmdk-list'], { role: 'listbox', id: 'list' });
      this._input.setAttribute('aria-controls', 'list');
      this._footer = this.createElement('div', ['nk-cmdk-footer']);
      const footerSlot = this.createElement('slot', [], { name: 'footer' });
      footerSlot.innerHTML = '<span><kbd class="nk-kbd">↑</kbd><kbd class="nk-kbd">↓</kbd> navigate</span><span><kbd class="nk-kbd">↵</kbd> open</span><span><kbd class="nk-kbd">⌘</kbd><kbd class="nk-kbd">K</kbd> toggle</span>';
      this._footer.appendChild(footerSlot);
      this._box.append(row, this._list, this._footer);
      this._backdrop.appendChild(this._box);
      this._wrapper.appendChild(this._backdrop);
      this._commands = [];
      this._index = 0;
      this._syncAttrs();
      this._renderList();
      this._syncOpen();
    }

    _syncAttrs() { this._input.placeholder = this.getAttribute('placeholder') || 'Search or type a command …'; }

    /** Filtered, flat list in group order, best matches first within a group. */
    results() {
      const q = this._input?.value ?? '';
      const out = [];
      for (const group of this._commands) {
        const scored = (group.items || []).map(item => ({ item, group: group.group, score: fuzzyScore(q, `${item.label} ${item.keywords || ''}`) })).filter(r => r.score > 0);
        scored.sort((a, b) => b.score - a.score);
        out.push(...scored);
      }
      return out;
    }

    _renderList() {
      if (!this._list) return;
      const results = this.results();
      this._index = Math.min(this._index, Math.max(0, results.length - 1));
      this._list.replaceChildren();
      if (!results.length) {
        const empty = this.createElement('div', ['nk-cmdk-empty']);
        empty.textContent = `No results for “${this._input.value}”`;
        this._list.appendChild(empty);
        this._input.removeAttribute('aria-activedescendant');
        return;
      }
      let lastGroup = null;
      results.forEach((r, i) => {
        if (r.group && r.group !== lastGroup) {
          const g = this.createElement('div', ['nk-cmdk-group']);
          g.textContent = r.group;
          this._list.appendChild(g);
          lastGroup = r.group;
        }
        const el = this.createElement('div', ['nk-cmdk-item'], { role: 'option', id: `opt-${i}`, 'data-index': i });
        el.classList.toggle('selected', i === this._index);
        el.setAttribute('aria-selected', i === this._index ? 'true' : 'false');
        const icon = this.createElement('span', ['m-icon']);
        icon.textContent = r.item.icon || '';
        const label = document.createElement('span');
        label.textContent = r.item.label;
        el.append(icon, label);
        if (r.item.shortcut) { const sc = this.createElement('span', ['m-shortcut']); sc.textContent = r.item.shortcut; el.appendChild(sc); }
        this._list.appendChild(el);
      });
      this._input.setAttribute('aria-activedescendant', `opt-${this._index}`);
    }

    _move(dir) {
      const n = this.results().length;
      if (!n) return;
      this._index = (this._index + dir + n) % n;
      for (const el of this._list.querySelectorAll('.nk-cmdk-item')) {
        const on = Number(el.dataset.index) === this._index;
        el.classList.toggle('selected', on);
        el.setAttribute('aria-selected', on ? 'true' : 'false');
        if (on) el.scrollIntoView({ block: 'nearest' });
      }
      this._input.setAttribute('aria-activedescendant', `opt-${this._index}`);
    }

    pick(index = this._index) {
      const r = this.results()[index];
      if (!r) return;
      const query = this._input.value;
      this.close();
      const ok = this.emit('nk-command', { id: r.item.id ?? r.item.label, item: r.item, query });
      if (ok && typeof r.item.action === 'function') r.item.action(r.item);
    }

    _matchesHotkey(e) {
      const spec = (this.getAttribute('hotkey') || 'mod+k').toLowerCase().split('+');
      const key = spec.pop();
      const mod = spec.includes('mod') ? (e.metaKey || e.ctrlKey) : true;
      const shift = spec.includes('shift') ? e.shiftKey : !e.shiftKey;
      const alt = spec.includes('alt') ? e.altKey : !e.altKey;
      return mod && shift && alt && e.key.toLowerCase() === key;
    }

    _syncOpen() {
      const open = this.getBoolAttr('open');
      this._backdrop.classList.toggle('open', open);
      this._backdrop.inert = !open;
      if (open === this._wasOpen) return;
      this._wasOpen = open;
      if (open) {
        this._returnFocus = document.activeElement;
        this._input.value = '';
        this._index = 0;
        this._renderList();
        lockScroll();
        this._undoInert = inertOutside(this);
        requestAnimationFrame(() => this._input.focus({ preventScroll: true }));
      } else {
        unlockScroll();
        this._undoInert?.(); this._undoInert = null;
        const back = this._returnFocus;
        if (back && typeof back.focus === 'function' && back.isConnected) back.focus({ preventScroll: true });
        this._returnFocus = null;
      }
    }

    setupEvents() {
      this._onDocKey = (e) => {
        if (this._matchesHotkey(e)) { e.preventDefault(); this.toggle(); return; }
        if (e.key === 'Escape' && this.getBoolAttr('open')) { e.stopPropagation(); this.close(); }
      };
      this._onInput = () => { this._index = 0; this._renderList(); };
      this._onInputKey = (e) => {
        if (e.key === 'ArrowDown') { e.preventDefault(); this._move(1); }
        else if (e.key === 'ArrowUp') { e.preventDefault(); this._move(-1); }
        else if (e.key === 'Enter') { e.preventDefault(); this.pick(); }
      };
      this._onListClick = (e) => { const el = e.target.closest('.nk-cmdk-item'); if (el) this.pick(Number(el.dataset.index)); };
      this._onListMove = (e) => { const el = e.target.closest('.nk-cmdk-item'); if (el && Number(el.dataset.index) !== this._index) { this._index = Number(el.dataset.index); this._move(0); } };
      this._onListDown = (e) => e.preventDefault();   // keep the input focused
      this._onBackdrop = (e) => { if (e.target === this._backdrop) this.close(); };
      document.addEventListener('keydown', this._onDocKey);
      this._input.addEventListener('input', this._onInput);
      this._input.addEventListener('keydown', this._onInputKey);
      this._list.addEventListener('click', this._onListClick);
      this._list.addEventListener('mousemove', this._onListMove);
      this._list.addEventListener('mousedown', this._onListDown);
      this._backdrop.addEventListener('click', this._onBackdrop);
    }

    teardownEvents() {
      document.removeEventListener('keydown', this._onDocKey);
      this._input?.removeEventListener('input', this._onInput);
      this._input?.removeEventListener('keydown', this._onInputKey);
      this._list?.removeEventListener('click', this._onListClick);
      this._list?.removeEventListener('mousemove', this._onListMove);
      this._list?.removeEventListener('mousedown', this._onListDown);
      this._backdrop?.removeEventListener('click', this._onBackdrop);
      if (this._wasOpen) { unlockScroll(); this._undoInert?.(); this._undoInert = null; this._wasOpen = false; }
    }

    onAttributeChanged(name) {
      if (name === 'open') { this._syncOpen(); this.emit('nk-toggle', { open: this.getBoolAttr('open') }); }
      else this._syncAttrs();
    }

    show() { this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }

    get commands() { return this._commands; }
    set commands(list) { this._commands = Array.isArray(list) ? list : []; this._index = 0; this._renderList(); }
    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get query() { return this._input?.value ?? ''; }
    set query(v) { if (this._input) { this._input.value = v; this._index = 0; this._renderList(); } }
  }

  customElements.define('nk-cmdk', NkCmdk);

  // <nk-menu>
  //   <nk-menu-item type="label">Page</nk-menu-item>
  //   <nk-menu-item icon="✏️" shortcut="⌘E" value="rename">Rename</nk-menu-item>
  //   <nk-menu-item type="separator"></nk-menu-item>
  //   <nk-menu-item icon="🗑️" danger value="delete">Delete</nk-menu-item>
  // </nk-menu>
  // → <div class="nk-pop nk-menu" role="menu">…</div>
  // ↑↓ move between items, Enter selects, nk-select bubbles from the item.
  //
  // <nk-menu floating sheet> is a menu over the page (NotionKit 1.7.0): it
  // fades in like the palette when `open` is set and is a bottom sheet on a
  // phone. The host becomes the fixed box you position – show(anchor) puts it
  // under the anchor, right edges aligned (align="start": left edges). A tap
  // outside closes it and reaches nothing else; Escape and a chosen item
  // close it too, a switch row does not. Opened from the keyboard, focus moves
  // to the first item; closing returns it to where it was.
  const floatSheet = new CSSStyleSheet();
  floatSheet.replaceSync(`:host([floating]) { display: block; position: fixed; z-index: 60; pointer-events: none; }`);

  class NkMenu extends NkElement {
    static get hostStyles() { return floatSheet; }
    static get observedAttributes() { return ['floating', 'sheet', 'open', 'align']; }

    render() {
      this._box = this.createElement('div', ['nk-pop', 'nk-menu'], { role: 'menu' });
      this._box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(this._box);
      this._syncMode();
    }

    _syncMode() {
      this._box.classList.toggle('floating', this.getBoolAttr('floating'));
      this._box.classList.toggle('sheet', this.getBoolAttr('sheet'));
      this._box.classList.toggle('open', this.getBoolAttr('open'));
    }

    onAttributeChanged(name) {
      this._syncMode();
      if (name !== 'open') return;
      const open = this.getBoolAttr('open');
      // aria-expanded belongs on the control itself, inside a host such as <nk-btn>.
      if (this._anchor) (this._anchor.shadowRoot?.querySelector('button, a[href]') ?? this._anchor).setAttribute('aria-expanded', String(open));
      if (open) {
        this._returnFocus = deepActiveElement();
        if (this._returnFocus?.matches(':focus-visible')) requestAnimationFrame(() => this.focusFirst());
        document.addEventListener('click', this._onOutside, true);
      } else {
        document.removeEventListener('click', this._onOutside, true);
        const back = this._returnFocus;
        this._returnFocus = null;
        if (back?.isConnected && this.contains(document.activeElement)) back.focus({ preventScroll: true });
      }
      this.emit('nk-toggle', { open });
    }

    /** Opens under `anchor` (any element, display: contents hosts included). */
    show(anchor) {
      if (anchor) {
        this._anchor = anchor;
        placeUnder(this, anchor, this.getAttribute('align'));
      }
      this.setBoolAttr('open', true);
    }
    close() { this.setBoolAttr('open', false); }
    toggle(anchor) { this.getBoolAttr('open') ? this.close() : this.show(anchor); }
    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }

    get items() { return [...this.querySelectorAll(':scope > nk-menu-item')].filter(i => i.selectable); }

    setupEvents() {
      this._onKey = (e) => {
        const dir = { ArrowDown: 1, ArrowUp: -1 }[e.key];
        const current = e.target.closest?.('nk-menu-item');
        const items = this.items;
        if (!items.length) return;
        if (dir) {
          e.preventDefault();
          const i = items.indexOf(current);
          items[(i + dir + items.length) % items.length].focus();
        } else if ((e.key === 'Enter' || e.key === ' ') && current) {
          e.preventDefault(); current.select();
        }
      };
      this.addEventListener('keydown', this._onKey);
      // Floating: outside taps close and go nowhere else; Escape and a chosen item close.
      this._onOutside = (e) => {
        const path = e.composedPath();
        if (path.includes(this) || (this._anchor && path.includes(this._anchor))) return;
        e.preventDefault(); e.stopPropagation();
        this.close();
      };
      // Captured, so a menu inside a modal or sheet closes before they do.
      this._onEscape = (e) => { if (e.key === 'Escape' && this.getBoolAttr('floating') && this.getBoolAttr('open')) { e.stopPropagation(); this.close(); } };
      this._onSelect = () => { if (this.getBoolAttr('floating')) this.close(); };
      document.addEventListener('keydown', this._onEscape, true);
      this.addEventListener('nk-select', this._onSelect);
      if (this.getBoolAttr('open')) document.addEventListener('click', this._onOutside, true);
    }

    teardownEvents() {
      this.removeEventListener('keydown', this._onKey);
      document.removeEventListener('keydown', this._onEscape, true);
      this.removeEventListener('nk-select', this._onSelect);
      document.removeEventListener('click', this._onOutside, true);
    }

    /** Focuses the first item (called by a popover when it opens). */
    focusFirst() { this.items[0]?.focus(); }
  }

  customElements.define('nk-menu', NkMenu);

  // <nk-menu-item icon="✏️" shortcut="⌘E" value="rename">Rename</nk-menu-item>
  // <nk-menu-item type="separator"></nk-menu-item>   <nk-menu-item type="label">Danger</nk-menu-item>
  // <nk-menu-item icon="🗑️" danger value="delete">Delete</nk-menu-item>
  // <nk-menu-item type="switch" icon="🔡" value="small" checked>Small text</nk-menu-item>
  // <nk-menu-item type="check" icon="◉" value="open" checked>Status: Open</nk-menu-item>
  // → <div class="nk-menu-item danger"><span class="m-icon">🗑️</span>Delete<span class="m-shortcut">…</span></div>
  // A switch item carries a .nk-switch on the right, as "Small text" does in
  // Notion's page menu; a check item a ✓ where the shortcut stands, as in a
  // filter menu. A click on either flips `checked` and fires nk-change
  // { value, checked } instead of nk-select, so the menu around it stays open.
  class NkMenuItem extends NkElement {
    static get observedAttributes() { return ['icon', 'shortcut', 'danger', 'value', 'type', 'disabled', 'checked']; }

    render() { this._build(); }

    _build() {
      const type = this.getAttribute('type');
      let el;
      if (type === 'separator') {
        el = this.createElement('div', ['nk-menu-sep'], { role: 'separator' });
      } else if (type === 'label') {
        el = this.createElement('div', ['nk-menu-label']);
        el.appendChild(document.createElement('slot'));
      } else {
        const isSwitch = type === 'switch';
        this._check = type === 'check';
        el = this.createElement('div', ['nk-menu-item'], { role: isSwitch || this._check ? 'menuitemcheckbox' : 'menuitem', tabindex: '-1' });
        this._icon = this.createElement('span', ['m-icon']);
        const iconSlot = this.createElement('slot', [], { name: 'icon' });
        iconSlot.appendChild(this._icon);
        this._shortcut = this.createElement('span', ['m-shortcut']);
        el.append(iconSlot, document.createElement('slot'), this._shortcut);
        this._switch = isSwitch ? this.createElement('span', ['nk-switch'], { 'aria-hidden': 'true' }) : null;
        if (this._switch) el.appendChild(this._switch);
      }
      if (this._el) { this._el.removeEventListener('click', this._onClick); this._el.replaceWith(el); } else this._wrapper.appendChild(el);
      this._el = el;
      if (this._onClick) el.addEventListener('click', this._onClick);
      this._sync();
    }

    _sync() {
      if (!this._el.classList.contains('nk-menu-item')) return;
      const icon = this.getAttribute('icon');
      this._icon.textContent = icon || '';
      this._icon.style.display = icon ? '' : 'none';
      const sc = this.getAttribute('shortcut');
      this._shortcut.textContent = sc || '';
      this._shortcut.style.display = sc ? '' : 'none';
      this._el.classList.toggle('danger', this.getBoolAttr('danger'));
      this._el.setAttribute('aria-disabled', this.getBoolAttr('disabled') ? 'true' : 'false');
      if (this._switch || this._check) {
        const on = this.getBoolAttr('checked') ? 'true' : 'false';
        this._el.setAttribute('aria-checked', on);
        if (this._switch) this._switch.setAttribute('aria-checked', on);
        if (this._check) { this._shortcut.textContent = on === 'true' ? '✓' : ''; this._shortcut.style.display = ''; }
      }
    }

    setupEvents() {
      this._onClick = () => this.select();
      this._el.addEventListener('click', this._onClick);
    }

    teardownEvents() { this._el?.removeEventListener('click', this._onClick); }

    select() {
      if (!this._el.classList.contains('nk-menu-item') || this.getBoolAttr('disabled')) return;
      if (this._switch || this._check) {
        this.checked = !this.checked;
        this.emit('nk-change', { value: this.value, checked: this.checked, item: this });
        return;
      }
      this.emit('nk-select', { value: this.value, label: this.textContent.trim(), item: this });
    }

    onAttributeChanged(name) { name === 'type' ? this._build() : this._sync(); }
    focus(o) { this._el?.focus(o); }
    get selectable() { return this._el?.classList.contains('nk-menu-item') && !this.getBoolAttr('disabled'); }

    get checked() { return this.getBoolAttr('checked'); }
    set checked(v) { this.setBoolAttr('checked', v); }
    get value() { return this.getAttribute('value') ?? this.textContent.trim(); }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-menu-item', NkMenuItem);

  // <nk-pop placement="bottom-start">
  //   <nk-btn slot="trigger" variant="secondary">Options</nk-btn>
  //   <nk-menu>…</nk-menu>
  // </nk-pop>
  // The trigger stays in the light DOM; a click on it toggles `open`, outside
  // clicks and Escape close. Content is wrapped in `.nk-pop` unless it brings
  // its own surface (nk-menu, nk-emoji-picker) or `bare` is set. The only
  // styling here is positioning – the surface look is the stylesheet's.
  const popSheet = new CSSStyleSheet();
  popSheet.replaceSync(`
  .nk-pop-anchor { position: relative; display: inline-block; }
  .nk-pop-float { display: none; position: absolute; z-index: 50; top: 100%; left: 0; margin-top: 4px; }
  :host([open]) .nk-pop-float { display: block; }
  :host([placement="bottom-end"]) .nk-pop-float { left: auto; right: 0; }
  :host([placement="top-start"]) .nk-pop-float { top: auto; bottom: 100%; margin: 0 0 4px; }
  :host([placement="top-end"]) .nk-pop-float { top: auto; bottom: 100%; left: auto; right: 0; margin: 0 0 4px; }
`);
  const OWN_SURFACE = new Set(['nk-menu', 'nk-emoji-picker']);

  class NkPop extends NkElement {
    static get hostStyles() { return popSheet; }
    static get observedAttributes() { return ['open', 'placement', 'bare']; }

    render() {
      const anchor = this.createElement('div', ['nk-pop-anchor']);
      this._triggerSlot = this.createElement('slot', [], { name: 'trigger' });
      this._float = this.createElement('div', ['nk-pop-float']);
      this._surface = this.createElement('div', ['nk-pop']);
      this._slot = document.createElement('slot');
      this._surface.appendChild(this._slot);
      this._float.appendChild(this._surface);
      anchor.append(this._triggerSlot, this._float);
      this._wrapper.appendChild(anchor);
      this._syncSurface();
    }

    _syncSurface() {
      const bare = this.getBoolAttr('bare') || this._slot.assignedElements().some(el => OWN_SURFACE.has(el.localName));
      this._surface.className = bare ? '' : 'nk-pop';
    }

    setupEvents() {
      this._onTrigger = (e) => {
        if (!e.composedPath().some(n => n instanceof Element && n.getAttribute?.('slot') === 'trigger')) return;
        e.stopPropagation();
        this.toggle();
      };
      this._onDocClick = (e) => { if (this.getBoolAttr('open') && !e.composedPath().includes(this)) this.close(); };
      this._onKey = (e) => { if (e.key === 'Escape' && this.getBoolAttr('open')) this.close(); };
      this._onSelect = (e) => { if (e.target !== this) this.close(); };
      this._onSlot = () => this._syncSurface();
      this.addEventListener('click', this._onTrigger);
      document.addEventListener('click', this._onDocClick);
      document.addEventListener('keydown', this._onKey);
      this._float.addEventListener('nk-select', this._onSelect);
      this._slot.addEventListener('slotchange', this._onSlot);
    }

    teardownEvents() {
      this.removeEventListener('click', this._onTrigger);
      document.removeEventListener('click', this._onDocClick);
      document.removeEventListener('keydown', this._onKey);
      this._float?.removeEventListener('nk-select', this._onSelect);
      this._slot?.removeEventListener('slotchange', this._onSlot);
    }

    onAttributeChanged(name) {
      if (name === 'bare') this._syncSurface();
      if (name === 'open') {
        const open = this.getBoolAttr('open');
        for (const el of this._triggerSlot.assignedElements()) el.setAttribute('aria-expanded', open ? 'true' : 'false');
        this.emit('nk-toggle', { open });
      }
    }

    show() { this.setBoolAttr('open', true); }
    close() { this.setBoolAttr('open', false); }
    toggle() { this.setBoolAttr('open', !this.getBoolAttr('open')); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get placement() { return this.getAttribute('placement') || 'bottom-start'; }
    set placement(v) { this.setAttribute('placement', v); }
  }

  customElements.define('nk-pop', NkPop);

  // Built-in emoji set for <nk-emoji-picker>: eight categories, names for
  // search. Override with `picker.emojis = [{ char, name, cat }]`.
  const C = (cat, icon, list) => list.map(([char, name]) => ({ char, name, cat, icon }));
  const EMOJI_CATEGORIES = [
    { id: 'smileys', icon: '😀' }, { id: 'people', icon: '👋' }, { id: 'nature', icon: '🌿' }, { id: 'food', icon: '☕' },
    { id: 'activity', icon: '🎯' }, { id: 'travel', icon: '🚀' }, { id: 'objects', icon: '💡' }, { id: 'symbols', icon: '✅' },
  ];
  const EMOJI = [
    ...C('smileys', '😀', [['😀', 'grinning'], ['😊', 'smile blush'], ['😂', 'joy laugh'], ['🙂', 'slight smile'], ['😉', 'wink'], ['😍', 'heart eyes love'], ['🤔', 'thinking'], ['😎', 'cool sunglasses'], ['🥳', 'party'], ['😴', 'sleep'], ['🤯', 'mind blown'], ['😅', 'sweat smile'], ['🙃', 'upside down'], ['😇', 'angel halo'], ['🤗', 'hug'], ['😢', 'cry sad']]),
    ...C('people', '👋', [['👋', 'wave hello'], ['👍', 'thumbs up'], ['👎', 'thumbs down'], ['🙏', 'pray thanks'], ['👏', 'clap'], ['🤝', 'handshake deal'], ['💪', 'strong'], ['👀', 'eyes look'], ['🧠', 'brain knowledge'], ['❤️', 'heart love'], ['👤', 'person user'], ['👥', 'people team members'], ['🧑‍💻', 'developer'], ['🙋', 'raise hand'], ['✍️', 'writing'], ['🫶', 'heart hands']]),
    ...C('nature', '🌿', [['🌿', 'leaf herb'], ['🌱', 'seedling'], ['🌳', 'tree'], ['🌸', 'blossom flower'], ['🌞', 'sun'], ['🌙', 'moon night'], ['⭐', 'star'], ['🔥', 'fire hot'], ['🌈', 'rainbow'], ['🌊', 'wave water'], ['🍀', 'clover luck'], ['🐝', 'bee'], ['🦋', 'butterfly'], ['🐢', 'turtle'], ['🌍', 'earth globe'], ['❄️', 'snow']]),
    ...C('food', '☕', [['☕', 'coffee'], ['🍵', 'tea'], ['🍎', 'apple'], ['🍕', 'pizza'], ['🍰', 'cake'], ['🍪', 'cookie'], ['🥐', 'croissant'], ['🍔', 'burger'], ['🍜', 'noodles'], ['🍺', 'beer'], ['🍷', 'wine'], ['🥗', 'salad'], ['🍫', 'chocolate'], ['🍓', 'strawberry'], ['🥑', 'avocado'], ['🍩', 'donut']]),
    ...C('activity', '🎯', [['🎯', 'target goal'], ['🏆', 'trophy win'], ['🎉', 'tada celebration'], ['🎨', 'art palette design'], ['🎙️', 'microphone podcast voice'], ['🎬', 'clapper movie'], ['🎮', 'game'], ['⚽', 'football'], ['🏃', 'run'], ['🧘', 'yoga calm'], ['🎵', 'music'], ['🎲', 'dice'], ['🏁', 'finish flag'], ['🎓', 'graduation learn'], ['🧩', 'puzzle piece'], ['🔁', 'repeat retro']]),
    ...C('travel', '🚀', [['🚀', 'rocket launch'], ['✈️', 'plane travel'], ['🚗', 'car'], ['🚲', 'bike'], ['🏠', 'house home'], ['🏢', 'office building'], ['🗺️', 'map'], ['🧭', 'compass'], ['⛰️', 'mountain'], ['🏖️', 'beach'], ['🚉', 'station'], ['🛰️', 'satellite'], ['🌆', 'city'], ['🏕️', 'camping'], ['⛵', 'sailboat'], ['🛫', 'departure']]),
    ...C('objects', '💡', [['💡', 'idea bulb'], ['📊', 'chart bar'], ['📈', 'chart up'], ['📅', 'calendar date'], ['📄', 'page document'], ['📁', 'folder'], ['🗃️', 'card box database'], ['📌', 'pin'], ['🔍', 'search'], ['🛠️', 'tools'], ['⚙️', 'gear settings'], ['🔔', 'bell notification'], ['💎', 'gem diamond'], ['📝', 'memo notes'], ['📎', 'paperclip files'], ['🔒', 'lock secure'], ['💬', 'speech comment'], ['📤', 'export outbox'], ['📥', 'inbox'], ['🗑️', 'trash bin'], ['🖥️', 'desktop'], ['📱', 'phone'], ['💻', 'laptop'], ['🧾', 'receipt']]),
    ...C('symbols', '✅', [['✅', 'check done'], ['✨', 'sparkles ai magic'], ['❌', 'cross no'], ['⚠️', 'warning'], ['ℹ️', 'info'], ['❓', 'question'], ['➕', 'plus add'], ['➡️', 'arrow right'], ['🔴', 'red circle'], ['🟢', 'green circle'], ['🟡', 'yellow circle'], ['🔵', 'blue circle'], ['⏳', 'hourglass wait'], ['♻️', 'recycle'], ['🆕', 'new'], ['💯', 'hundred']]),
  ];

  // <nk-emoji-picker></nk-emoji-picker>
  // → <div class="nk-pop"><input class="nk-emoji-search"><div class="nk-emoji-grid">…</div><div class="nk-emoji-cats">…</div></div>
  // Built-in set with names for search; `picker.emojis = [{ char, name, cat }]`
  // replaces it. A click fires nk-select { emoji }.
  class NkEmojiPicker extends NkElement {
    static get observedAttributes() { return ['placeholder', 'value']; }

    render() {
      this._box = this.createElement('div', ['nk-pop']);
      this._search = this.createElement('input', ['nk-emoji-search'], { type: 'search', autocomplete: 'off', 'aria-label': 'Search emoji' });
      this._grid = this.createElement('div', ['nk-emoji-grid'], { role: 'listbox' });
      this._cats = this.createElement('div', ['nk-emoji-cats'], { role: 'tablist' });
      this._box.append(this._search, this._grid, this._cats);
      this._wrapper.appendChild(this._box);
      this._emojis = EMOJI;
      this._cat = EMOJI_CATEGORIES[0].id;
      this._renderCats();
      this._renderGrid();
      this._syncAttrs();
    }

    _syncAttrs() { this._search.placeholder = this.getAttribute('placeholder') || 'Search…'; }

    _renderCats() {
      this._cats.replaceChildren();
      const cats = this._categories();
      for (const cat of cats) {
        const el = this.createElement('span', [], { role: 'tab', 'data-cat': cat.id, title: cat.id });
        el.textContent = cat.icon;
        el.classList.toggle('active', cat.id === this._cat);
        this._cats.appendChild(el);
      }
      this._cats.style.display = cats.length > 1 ? '' : 'none';
    }

    _categories() {
      if (this._catList) return this._catList;
      const seen = new Map();
      for (const e of this._emojis) if (e.cat && !seen.has(e.cat)) seen.set(e.cat, { id: e.cat, icon: e.icon || EMOJI_CATEGORIES.find(c => c.id === e.cat)?.icon || e.char });
      return [...seen.values()];
    }

    _renderGrid() {
      const q = this._search.value.trim().toLowerCase();
      const list = q ? this._emojis.filter(e => (e.name || '').toLowerCase().includes(q) || e.char === q) : this._emojis.filter(e => !e.cat || e.cat === this._cat);
      this._grid.replaceChildren();
      for (const e of list.slice(0, 64)) {
        const s = this.createElement('span', [], { role: 'option', title: e.name || '', 'data-emoji': e.char });
        s.textContent = e.char;
        this._grid.appendChild(s);
      }
      for (const el of this._cats.children) el.classList.toggle('active', !q && el.dataset.cat === this._cat);
    }

    setupEvents() {
      this._onInput = () => this._renderGrid();
      this._onGrid = (e) => { const s = e.target.closest('[data-emoji]'); if (s) this.select(s.dataset.emoji); };
      this._onCats = (e) => { const c = e.target.closest('[data-cat]'); if (!c) return; this._cat = c.dataset.cat; this._search.value = ''; this._renderGrid(); };
      this._onKey = (e) => { if (e.key === 'Enter') { const first = this._grid.querySelector('[data-emoji]'); if (first) this.select(first.dataset.emoji); } };
      this._search.addEventListener('input', this._onInput);
      this._search.addEventListener('keydown', this._onKey);
      this._grid.addEventListener('click', this._onGrid);
      this._cats.addEventListener('click', this._onCats);
    }

    teardownEvents() {
      this._search?.removeEventListener('input', this._onInput);
      this._search?.removeEventListener('keydown', this._onKey);
      this._grid?.removeEventListener('click', this._onGrid);
      this._cats?.removeEventListener('click', this._onCats);
    }

    select(emoji) {
      this.setAttribute('value', emoji);
      this.emit('nk-select', { emoji, value: emoji });
    }

    onAttributeChanged() { this._syncAttrs(); }
    focus(o) { this._search?.focus(o); }

    /** Category strip override: [{ id, icon }]. Emojis without `cat` show in every category. */
    get categories() { return this._categories(); }
    set categories(list) { this._catList = Array.isArray(list) && list.length ? list : null; this._cat = this._categories()[0]?.id; this._renderCats(); this._renderGrid(); }
    get emojis() { return this._emojis; }
    set emojis(list) { this._emojis = Array.isArray(list) ? list : EMOJI; this._cat = this._categories()[0]?.id; this._renderCats(); this._renderGrid(); }
    get value() { return this.getAttribute('value'); }
    set value(v) { this.setAttribute('value', v); }
  }

  customElements.define('nk-emoji-picker', NkEmojiPicker);

  // <nk-toast id="toast"></nk-toast>   …   toast.show('Saved');
  // → <div class="nk-toast show">✓ Saved</div>
  // One node, bottom-centred, auto-hides after `duration` ms (default 2200).
  // Above a tab bar at the bottom of the screen it rises 12px over the bar:
  // notionkit.css does that for class markup with :has(), which cannot see
  // into shadow roots, so the element measures the bar when it opens.
  class NkToast extends NkElement {
    static get observedAttributes() { return ['open', 'duration', 'icon']; }

    render() {
      this._box = this.createElement('div', ['nk-toast'], { role: 'status', 'aria-live': 'polite' });
      this._icon = document.createElement('span');
      this._msg = document.createElement('span');
      this._box.append(this._icon, this._msg, document.createElement('slot'));
      this._wrapper.appendChild(this._box);
      this._sync();
    }

    _sync() {
      if (this.getBoolAttr('open')) this._lift();
      this._box.classList.toggle('show', this.getBoolAttr('open'));
      const icon = this.getAttribute('icon') ?? '✓';
      this._icon.textContent = icon;
      this._icon.style.display = icon ? '' : 'none';
    }

    // A bar counts when it is rendered and ends near the bottom of the viewport –
    // the app's own bar, fixed, sticky or floating. A tab bar shown as a preview
    // somewhere in a page does not lift the toast.
    _lift() {
      const bars = [...document.querySelectorAll('.nk-tab-bar')];
      for (const host of document.querySelectorAll('nk-tab-bar')) {
        const bar = host.shadowRoot?.querySelector('.nk-tab-bar');
        if (bar) bars.push(bar);
      }
      const rect = bars.map(b => b.getBoundingClientRect())
        .find(r => r.height > 0 && r.top < innerHeight && r.bottom > innerHeight - 60);
      this._box.style.bottom = rect ? `${Math.round(innerHeight - rect.top + 12)}px` : '';
    }

    /** Shows `message` (or the slotted content when omitted) and hides it again after `duration`. */
    show(message, { duration } = {}) {
      if (message !== undefined) this._msg.textContent = message;
      clearTimeout(this._timer);
      this.setBoolAttr('open', true);
      const ms = duration ?? Number(this.getAttribute('duration')) ?? 2200;
      if (ms > 0) this._timer = setTimeout(() => this.close(), ms || 2200);
    }

    close() { clearTimeout(this._timer); this.setBoolAttr('open', false); }

    onAttributeChanged(name) {
      this._sync();
      if (name === 'open') this.emit('nk-toggle', { open: this.getBoolAttr('open') });
    }

    disconnectedCallback() { super.disconnectedCallback(); clearTimeout(this._timer); }

    get open() { return this.getBoolAttr('open'); }
    set open(v) { this.setBoolAttr('open', v); }
    get message() { return this._msg?.textContent ?? ''; }
    set message(v) { if (this._msg) this._msg.textContent = v; }
  }

  customElements.define('nk-toast', NkToast);

  // <nk-database id="db" view="table" add-view>
  //   <nk-table-view name="table" label="▦ Table" count></nk-table-view>
  //   <nk-board-view name="board" label="▤ Board" group-by="status"></nk-board-view>
  // </nk-database>
  // db.columns = [{ key: 'name', label: 'Name', type: 'text', icon: '📄', title: true }, { key: 'status', type: 'select', options: [...] }, …];
  // db.rows = [{ id: 1, icon: '🧭', name: '…', status: 'done', … }];
  // <nk-btn slot="tools" variant="tool">Filter</nk-btn> … sit right of the tabs,
  // <nk-filter-bar slot="filters"> under them (NotionKit 1.7.0's toolbar).
  // → <div class="nk-database"><div class="nk-db-toolbar"><div class="nk-db-tabs">…</div>
  //     <div class="tools">…</div></div>…filters…active view…</div>
  // The tabs come from the child views; data is pushed into every view.
  class NkDatabase extends NkElement {
    static get observedAttributes() { return ['view', 'add-view']; }

    render() {
      this._box = this.createElement('div', ['nk-database']);
      this._toolbar = this.createElement('div', ['nk-db-toolbar']);
      this._tabs = this.createElement('div', ['nk-db-tabs'], { role: 'tablist' });
      const tools = this.createElement('div', ['tools']);
      this._toolsSlot = this.createElement('slot', [], { name: 'tools' });
      tools.appendChild(this._toolsSlot);
      this._toolbar.append(this._tabs, tools);
      this._slot = document.createElement('slot');
      this._box.append(this._toolbar, this.createElement('slot', [], { name: 'filters' }), this._slot);
      this._wrapper.appendChild(this._box);
      this._columns = []; this._rows = [];
      this._sync();
    }

    get views() { return this._slot?.assignedElements().filter(el => /^nk-[a-z]+-view$/.test(el.localName)) ?? []; }

    _sync() {
      if (!this._tabs) return;
      const views = this.views;
      let current = this.getAttribute('view');
      if (!views.some(v => v.name === current)) current = views[0]?.name ?? null;
      this._tabs.replaceChildren();
      for (const v of views) {
        const tab = this.createElement('span', ['nk-db-tab'], { role: 'tab', 'data-view': v.name, tabindex: '0' });
        tab.classList.toggle('active', v.name === current);
        tab.setAttribute('aria-selected', v.name === current ? 'true' : 'false');
        tab.append(document.createTextNode(v.getAttribute('label') || v.name));
        const badge = v.hasAttribute('count') ? String(this._rows.length) : v.getAttribute('badge');
        if (badge) { const b = this.createElement('span', ['badge']); b.textContent = badge; tab.appendChild(b); }
        this._tabs.appendChild(tab);
        v.hidden = v.name !== current;
        // A method call, not a property write: a view that has not been upgraded
        // yet would otherwise get an own `data` property that shadows its setter.
        v.setData?.(this._columns, this._rows);
      }
      if (this.getBoolAttr('add-view')) {
        const add = this.createElement('span', ['nk-db-tab', 'add'], { role: 'button', 'data-add': '', tabindex: '0' });
        add.textContent = '＋';
        this._tabs.appendChild(add);
      }
      this._toolbar.hidden = !views.length && !this._toolsSlot.assignedElements().length;
      this._current = current;
    }

    /** Views upgrade after the database; each one asks for a resync once rendered. */
    requestSync() {
      if (this._syncQueued) return;
      this._syncQueued = true;
      queueMicrotask(() => { this._syncQueued = false; this._sync(); });
    }

    setupEvents() {
      this._onClick = (e) => {
        const tab = e.target.closest('[data-view]');
        if (tab) { this.view = tab.dataset.view; return; }
        if (e.target.closest('[data-add]')) this.emit('nk-action', { action: 'add-view' });
      };
      this._onKey = (e) => { if ((e.key === 'Enter' || e.key === ' ') && e.target.closest('[data-view],[data-add]')) { e.preventDefault(); e.target.click(); } };
      this._onSlot = () => this._sync();
      this._tabs.addEventListener('click', this._onClick);
      this._tabs.addEventListener('keydown', this._onKey);
      this._slot.addEventListener('slotchange', this._onSlot);
      this._toolsSlot.addEventListener('slotchange', this._onSlot);
      this._sync();
    }

    teardownEvents() {
      this._tabs?.removeEventListener('click', this._onClick);
      this._tabs?.removeEventListener('keydown', this._onKey);
      this._slot?.removeEventListener('slotchange', this._onSlot);
      this._toolsSlot?.removeEventListener('slotchange', this._onSlot);
    }

    onAttributeChanged(name) {
      const before = this._current;
      this._sync();
      if (name === 'view' && this._current !== before) this.emit('nk-view-change', { view: this._current });
    }

    get columns() { return this._columns; }
    set columns(v) { this._columns = Array.isArray(v) ? v : []; this._sync(); }
    get rows() { return this._rows; }
    set rows(v) { this._rows = Array.isArray(v) ? v : []; this._sync(); }
    get view() { return this._current ?? this.getAttribute('view'); }
    set view(v) { this.setAttribute('view', v); }
    /** Re-renders every view after in-place row mutations. */
    refresh() { this._sync(); }
  }

  customElements.define('nk-database', NkDatabase);

  // Polymorphic property renderer for database views. Returns plain DOM built
  // from NotionKit classes – deliberately not a custom element: every cell rule
  // starts with `.nk-table`, so a cell inside its own shadow root would never
  // be styled. Exported for consumers who render tables themselves.
  //
  // column: { key, label, type, icon, options: [{ value, label, color }], title }
  // types: text | select | multi-select | date | person | checkbox | url | number | progress

  const el = (tag, cls, text) => { const n = document.createElement(tag); if (cls) n.className = cls; if (text != null) n.textContent = text; return n; };
  const COLORS = ['gray', 'brown', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink', 'red'];

  function optionFor(column, value) {
    const opts = column.options || [];
    return opts.find(o => o.value === value) || opts.find(o => o.label === value) || null;
  }

  function tagFor(column, value) {
    const opt = optionFor(column, value);
    const color = opt?.color && COLORS.includes(opt.color) ? opt.color : 'gray';   // Notion's default option colour
    return el('span', `nk-tag ${color}`, opt?.label ?? String(value));
  }

  function renderPropertyCell(column, value, row = {}) {
    const type = column.type || 'text';
    if (value === undefined || value === null || value === '') {
      if (type === 'checkbox') return checkbox(column, false, row);
      if (type === 'progress') return progress(0);
      return el('span', type === 'person' ? 'person-cell' : null, type === 'text' && column.title ? '' : '—');
    }
    switch (type) {
      case 'select': return tagFor(column, value);
      case 'multi-select': {
        const wrap = el('span');
        wrap.style.display = 'inline-flex'; wrap.style.gap = '4px'; wrap.style.flexWrap = 'wrap';
        for (const v of Array.isArray(value) ? value : String(value).split(',').map(s => s.trim()).filter(Boolean)) wrap.appendChild(tagFor(column, v));
        return wrap;
      }
      case 'date': return el('span', 'date-cell', String(value));
      case 'person': {
        const cell = el('span', 'person-cell');
        const p = typeof value === 'string' ? { name: value } : value;
        // color: one of Notion's nine names, or any CSS background; none – the avatar gradient.
        const avatar = el('span', 'nk-avatar small', p.initials || p.avatar || initialsOf(p.name));
        paintAvatar(avatar, p.color);
        cell.append(avatar, document.createTextNode(' ' + (p.name || '')));
        return cell;
      }
      case 'checkbox': return checkbox(column, !!value, row);
      case 'url': { const a = el('a', null, String(value).replace(/^https?:\/\//, '')); a.href = String(value); a.target = '_blank'; a.rel = 'noopener'; return a; }
      // column.locale / column.format: Intl.NumberFormat locale and options, e.g. { minimumFractionDigits: 1 }.
      case 'number': return el('span', null, typeof value === 'number' ? value.toLocaleString(column.locale, column.format) : String(value));
      case 'progress': return progress(Number(value) || 0);
      default: {
        if (column.title) {
          const t = el('span', 'row-title');
          if (row.icon) t.append(document.createTextNode(row.icon + ' '));
          t.append(document.createTextNode(String(value)));
          return t;
        }
        return el('span', null, String(value));
      }
    }
  }

  function checkbox(column, checked, row) {
    const label = el('label', 'nk-check');
    const input = el('input');
    input.type = 'checkbox'; input.checked = checked;
    input.dataset.key = column.key;
    if (row.id != null) input.dataset.rowId = row.id;
    label.appendChild(input);
    return label;
  }

  function progress(value) {
    const frag = document.createDocumentFragment();
    const bar = el('span', 'nk-progress');
    const fill = el('i'); fill.style.width = `${Math.max(0, Math.min(100, value))}%`;
    bar.appendChild(fill);
    frag.append(bar, el('span', 'nk-progress-label', `${Math.round(value)}%`));
    return frag;
  }

  /** Sort comparator for a column type. */
  function compareBy(column, dir = 1) {
    const type = column.type || 'text';
    return (a, b) => {
      let x = a[column.key], y = b[column.key];
      if (type === 'person') { x = x?.name ?? x ?? ''; y = y?.name ?? y ?? ''; }
      if (type === 'number' || type === 'progress' || type === 'checkbox') return ((Number(x) || 0) - (Number(y) || 0)) * dir;
      return String(x ?? '').localeCompare(String(y ?? ''), undefined, { numeric: true }) * dir;
    };
  }

  // <nk-table-view name="table" label="▦ Table" count new-row sortable wrap></nk-table-view>
  // `wrap` lets cell text break (`.nk-table.wrap`), like Notion's "wrap column".
  // Standalone: view.columns = […]; view.rows = […];  Inside <nk-database> the
  // database pushes `data`. → <div class="nk-table-wrap"><table class="nk-table">…</table><div class="nk-new-row">＋ New page</div></div>
  // Cells are plain markup from renderPropertyCell(); rows fire nk-select
  // { row, id, key, cell } – `key` and `cell` say which cell was hit, to open
  // an editor there, like the date picker under a due date – checkboxes
  // nk-change, headers nk-action { action: 'sort' } (and sort locally
  // with `sortable`), the add row nk-action { action: 'new-row' }.
  class NkTableView extends NkElement {
    static get observedAttributes() { return ['name', 'label', 'badge', 'count', 'new-row', 'new-row-label', 'sortable', 'sort-key', 'sort-dir', 'wrap']; }

    render() {
      this._wrap = this.createElement('div', ['nk-table-wrap']);
      this._table = this.createElement('table', ['nk-table']);
      this._thead = document.createElement('thead');
      this._tbody = document.createElement('tbody');
      this._table.append(this._thead, this._tbody);
      this._newRow = this.createElement('div', ['nk-new-row'], { role: 'button', tabindex: '0' });
      this._wrap.append(this._table, this._newRow);
      this._wrapper.appendChild(this._wrap);
      this._columns ??= []; this._rows ??= [];
      this._render();
      this.closest('nk-database')?.requestSync?.();
    }

    _sortedRows() {
      const key = this.getAttribute('sort-key');
      const col = this._columns.find(c => c.key === key);
      if (!col) return this._rows;
      return [...this._rows].sort(compareBy(col, this.getAttribute('sort-dir') === 'desc' ? -1 : 1));
    }

    _render() {
      if (!this._thead) return;
      this._table.classList.toggle('wrap', this.getBoolAttr('wrap'));
      const tr = document.createElement('tr');
      for (const col of this._columns) {
        const th = this.createElement('th', [], { 'data-key': col.key, scope: 'col' });
        if (col.icon) { const i = this.createElement('span', ['th-icon']); i.textContent = col.icon; th.appendChild(i); }
        th.appendChild(document.createTextNode(col.label ?? col.key));
        if (col.key === this.getAttribute('sort-key')) th.appendChild(document.createTextNode(this.getAttribute('sort-dir') === 'desc' ? ' ↓' : ' ↑'));
        if (col.width) th.style.width = col.width;
        tr.appendChild(th);
      }
      this._thead.replaceChildren(tr);
      this._tbody.replaceChildren();
      for (const row of this._sortedRows()) {
        const r = this.createElement('tr', [], { 'data-id': row.id ?? '' });
        for (const col of this._columns) {
          const td = document.createElement('td');
          // Numbers right-aligned in figures of equal width, as Notion sets them.
          if (col.type === 'number') td.className = 'num';
          td.appendChild(renderPropertyCell(col, row[col.key], row));
          r.appendChild(td);
        }
        this._tbody.appendChild(r);
      }
      this._newRow.textContent = this.getAttribute('new-row-label') || '＋ New page';
      this._newRow.style.display = this.getBoolAttr('new-row') ? '' : 'none';
    }

    setupEvents() {
      this._onClick = (e) => {
        if (this._newRow.contains(e.target)) { this.emit('nk-action', { action: 'new-row' }); return; }
        const th = e.target.closest('th[data-key]');
        if (th) {
          const key = th.dataset.key;
          const dir = this.getAttribute('sort-key') === key && this.getAttribute('sort-dir') !== 'desc' ? 'desc' : 'asc';
          const ok = this.emit('nk-action', { action: 'sort', key, value: dir });
          if (ok && this.getBoolAttr('sortable')) { this.setAttribute('sort-key', key); this.setAttribute('sort-dir', dir); }
          return;
        }
        if (e.target.closest('input[type=checkbox], a')) return;
        const tr = e.target.closest('tr[data-id]');
        if (!tr) return;
        // Which cell: `key` names its column, `cell` is there to anchor an editor to.
        const cell = e.target.closest('td');
        const row = this._rowById(tr.dataset.id);
        this.emit('nk-select', { row, id: row?.id, value: row?.id, key: cell ? this._columns[cell.cellIndex]?.key : undefined, cell });
      };
      this._onChange = (e) => {
        const input = e.target;
        if (input.type !== 'checkbox') return;
        const row = this._rowById(input.dataset.rowId);
        if (row) row[input.dataset.key] = input.checked;
        this.emit('nk-change', { row, key: input.dataset.key, value: input.checked });
      };
      this._onKey = (e) => { if ((e.key === 'Enter' || e.key === ' ') && e.target === this._newRow) { e.preventDefault(); this.emit('nk-action', { action: 'new-row' }); } };
      this._wrap.addEventListener('click', this._onClick);
      this._wrap.addEventListener('change', this._onChange);
      this._wrap.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._wrap?.removeEventListener('click', this._onClick);
      this._wrap?.removeEventListener('change', this._onChange);
      this._wrap?.removeEventListener('keydown', this._onKey);
    }

    _rowById(id) { return this._rows.find(r => String(r.id) === String(id)); }
    onAttributeChanged() { this._render(); }

    get name() { return this.getAttribute('name') || 'table'; }
    get columns() { return this._columns; }
    set columns(v) { this._columns = Array.isArray(v) ? v : []; this._render(); }
    get rows() { return this._rows; }
    set rows(v) { this._rows = Array.isArray(v) ? v : []; this._render(); }
    set data({ columns, rows }) { this.setData(columns, rows); }
    setData(columns, rows) { this._columns = columns || []; this._rows = rows || []; this._render(); }
    refresh() { this._render(); }
  }

  customElements.define('nk-table-view', NkTableView);

  // <nk-board-view name="board" label="▤ Board" group-by="status" new-row></nk-board-view>
  // Groups rows by a select column (default: the first select column); one
  // column per option, in option order. Cards show the title column and the
  // `meta-keys` (default: date and progress columns). Drag a card onto another
  // column to change its value – the row is updated and nk-change fires.
  // → <div class="nk-board active"><div class="nk-board-col">…</div>…</div>
  class NkBoardView extends NkElement {
    static get observedAttributes() { return ['name', 'label', 'badge', 'count', 'group-by', 'title-key', 'meta-keys', 'new-row']; }

    render() {
      this._board = this.createElement('div', ['nk-board', 'active']);
      this._wrapper.appendChild(this._board);
      this._columns ??= []; this._rows ??= [];
      this._render();
      this.closest('nk-database')?.requestSync?.();
    }

    _groupColumn() {
      const key = this.getAttribute('group-by');
      return this._columns.find(c => c.key === key) || this._columns.find(c => c.type === 'select') || null;
    }
    _titleColumn() {
      const key = this.getAttribute('title-key');
      return this._columns.find(c => c.key === key) || this._columns.find(c => c.title) || this._columns[0] || null;
    }
    _metaColumns() {
      const keys = (this.getAttribute('meta-keys') || '').split(',').map(s => s.trim()).filter(Boolean);
      if (keys.length) return keys.map(k => this._columns.find(c => c.key === k)).filter(Boolean);
      return this._columns.filter(c => c.type === 'date' || c.type === 'progress');
    }

    _render() {
      if (!this._board) return;
      this._board.replaceChildren();
      const group = this._groupColumn(), title = this._titleColumn();
      if (!group) return;
      const options = group.options?.length ? group.options : [...new Set(this._rows.map(r => r[group.key]))].map(v => ({ value: v, label: String(v) }));
      for (const opt of options) {
        const col = this.createElement('div', ['nk-board-col'], { 'data-value': opt.value });
        const header = this.createElement('div', ['nk-board-col-header']);
        const cards = this._rows.filter(r => r[group.key] === opt.value || r[group.key] === opt.label);
        const count = this.createElement('span', ['count']);
        count.textContent = String(cards.length);
        header.append(tagFor(group, opt.value), count);
        col.appendChild(header);
        for (const row of cards) {
          const card = this.createElement('div', ['nk-card'], { draggable: 'true', 'data-id': row.id ?? '', tabindex: '0' });
          const t = this.createElement('div', ['card-title']);
          t.textContent = `${row.icon ? row.icon + ' ' : ''}${title ? row[title.key] ?? '' : ''}`;
          const meta = this.createElement('div', ['card-meta']);
          for (const c of this._metaColumns()) {
            const v = row[c.key];
            if (v === undefined || v === null || v === '') continue;
            const s = document.createElement('span');
            if (c.type === 'progress') s.textContent = `▰ ${v}%`;
            else if (c.type === 'date') s.textContent = `📅 ${v}`;
            else s.appendChild(renderPropertyCell(c, v, row));
            meta.appendChild(s);
          }
          card.append(t, meta);
          col.appendChild(card);
        }
        if (this.getBoolAttr('new-row')) {
          const add = this.createElement('div', ['nk-new-row'], { role: 'button', tabindex: '0', 'data-add': opt.value });
          add.style.padding = '6px 10px';
          add.textContent = '＋';
          col.appendChild(add);
        }
        this._board.appendChild(col);
      }
    }

    setupEvents() {
      this._onClick = (e) => {
        const add = e.target.closest('[data-add]');
        if (add) { this.emit('nk-action', { action: 'new-row', value: add.dataset.add }); return; }
        const card = e.target.closest('.nk-card');
        if (card) { const row = this._rowById(card.dataset.id); this.emit('nk-select', { row, id: row?.id, value: row?.id }); }
      };
      this._onDragStart = (e) => { const card = e.target.closest?.('.nk-card'); if (!card) return; this._dragId = card.dataset.id; e.dataTransfer.effectAllowed = 'move'; try { e.dataTransfer.setData('text/plain', card.dataset.id); } catch {} };
      this._onDragOver = (e) => { if (this._dragId != null && e.target.closest('.nk-board-col')) { e.preventDefault(); e.dataTransfer.dropEffect = 'move'; } };
      this._onDrop = (e) => {
        const col = e.target.closest('.nk-board-col');
        if (!col || this._dragId == null) return;
        e.preventDefault();
        this.move(this._dragId, col.dataset.value);
        this._dragId = null;
      };
      this._board.addEventListener('click', this._onClick);
      this._board.addEventListener('dragstart', this._onDragStart);
      this._board.addEventListener('dragover', this._onDragOver);
      this._board.addEventListener('drop', this._onDrop);
    }

    teardownEvents() {
      this._board?.removeEventListener('click', this._onClick);
      this._board?.removeEventListener('dragstart', this._onDragStart);
      this._board?.removeEventListener('dragover', this._onDragOver);
      this._board?.removeEventListener('drop', this._onDrop);
    }

    /** Moves a row into another column (sets the group value) and reports it. */
    move(id, value) {
      const row = this._rowById(id), group = this._groupColumn();
      if (!row || !group || row[group.key] === value) return;
      row[group.key] = value;
      this._render();
      this.emit('nk-change', { row, key: group.key, value });
    }

    _rowById(id) { return this._rows.find(r => String(r.id) === String(id)); }
    onAttributeChanged() { this._render(); }

    get name() { return this.getAttribute('name') || 'board'; }
    get columns() { return this._columns; }
    set columns(v) { this._columns = Array.isArray(v) ? v : []; this._render(); }
    get rows() { return this._rows; }
    set rows(v) { this._rows = Array.isArray(v) ? v : []; this._render(); }
    set data({ columns, rows }) { this.setData(columns, rows); }
    setData(columns, rows) { this._columns = columns || []; this._rows = rows || []; this._render(); }
    refresh() { this._render(); }
  }

  customElements.define('nk-board-view', NkBoardView);

  // <nk-list-view name="list" label="☰ List" meta-keys="due,status" new-row></nk-list-view>
  // The third database view: one line per row – icon and title, the `meta-keys`
  // on the right (default: the select and date columns, in column order). Dates
  // and text stand as text, selects as tags, a person as avatar and name.
  // Standalone: view.columns = […]; view.rows = […]. Inside <nk-database> the
  // database pushes the data. Rows fire nk-select, the add row nk-action
  // { action: 'new-row' }.
  // → <div class="nk-list"><div class="nk-list-item"><span class="l-icon">…</span><span class="l-title">…</span><span class="l-meta">…</span></div>…</div>
  class NkListView extends NkElement {
    static get observedAttributes() { return ['name', 'label', 'badge', 'count', 'title-key', 'meta-keys', 'new-row', 'new-row-label']; }

    render() {
      this._box = document.createElement('div');
      this._list = this.createElement('div', ['nk-list'], { role: 'list' });
      this._newRow = this.createElement('div', ['nk-new-row'], { role: 'button', tabindex: '0' });
      this._box.append(this._list, this._newRow);
      this._wrapper.appendChild(this._box);
      this._columns ??= []; this._rows ??= [];
      this._render();
      this.closest('nk-database')?.requestSync?.();
    }

    _titleColumn() {
      const key = this.getAttribute('title-key');
      return this._columns.find(c => c.key === key) || this._columns.find(c => c.title) || this._columns[0] || null;
    }
    _metaColumns() {
      const keys = (this.getAttribute('meta-keys') || '').split(',').map(s => s.trim()).filter(Boolean);
      if (keys.length) return keys.map(k => this._columns.find(c => c.key === k)).filter(Boolean);
      return this._columns.filter(c => c.type === 'select' || c.type === 'date');
    }

    _render() {
      if (!this._list) return;
      const title = this._titleColumn(), meta = this._metaColumns();
      this._list.replaceChildren();
      for (const row of this._rows) {
        const item = this.createElement('div', ['nk-list-item'], { role: 'listitem', tabindex: '0', 'data-id': row.id ?? '' });
        const icon = this.createElement('span', ['l-icon']);
        icon.textContent = row.icon || '';
        if (!row.icon) icon.style.display = 'none';
        const t = this.createElement('span', ['l-title']);
        t.textContent = title ? String(row[title.key] ?? '') : '';
        const m = this.createElement('span', ['l-meta']);
        for (const c of meta) {
          const v = row[c.key];
          if (v === undefined || v === null || v === '') continue;
          m.appendChild(c.type === 'date' || c.type === 'text' || !c.type ? document.createTextNode(String(v)) : renderPropertyCell(c, v, row));
        }
        item.append(icon, t, m);
        this._list.appendChild(item);
      }
      this._newRow.textContent = this.getAttribute('new-row-label') || '＋ New page';
      this._newRow.style.display = this.getBoolAttr('new-row') ? '' : 'none';
    }

    setupEvents() {
      this._onClick = (e) => {
        if (this._newRow.contains(e.target)) { this.emit('nk-action', { action: 'new-row' }); return; }
        const item = e.target.closest('.nk-list-item');
        if (item) { const row = this._rowById(item.dataset.id); this.emit('nk-select', { row, id: row?.id, value: row?.id }); }
      };
      this._onKey = (e) => {
        if (e.key !== 'Enter' && e.key !== ' ') return;
        if (e.target === this._newRow || e.target.classList?.contains('nk-list-item')) { e.preventDefault(); e.target.click(); }
      };
      this._box.addEventListener('click', this._onClick);
      this._box.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._box?.removeEventListener('click', this._onClick);
      this._box?.removeEventListener('keydown', this._onKey);
    }

    _rowById(id) { return this._rows.find(r => String(r.id) === String(id)); }
    onAttributeChanged() { this._render(); }

    get name() { return this.getAttribute('name') || 'list'; }
    get columns() { return this._columns; }
    set columns(v) { this._columns = Array.isArray(v) ? v : []; this._render(); }
    get rows() { return this._rows; }
    set rows(v) { this._rows = Array.isArray(v) ? v : []; this._render(); }
    set data({ columns, rows }) { this.setData(columns, rows); }
    setData(columns, rows) { this._columns = columns || []; this._rows = rows || []; this._render(); }
    refresh() { this._render(); }
  }

  customElements.define('nk-list-view', NkListView);

  // <nk-calendar-view name="calendar" label="📅 Calendar" date-key="due" weeks></nk-calendar-view>
  // The fourth database view (NotionKit 1.9.0): a month, the rows as cards on
  // their dates. `date-key` names the date column (default: the first one),
  // `title-key` the title (default: the title column); dates are YYYY-MM-DD or
  // D.M.YYYY, a range { start, end } stands on its start. month (YYYY-MM;
  // default today's), weeks (the ISO week in front of each row), week-start
  // (0 = Sunday … 6 = Saturday; default from the locale), weekend (weekdays
  // washed like the days of other months, "6,0"), today, locale and the texts
  // today-label, prev-label, next-label, week-label.
  // Standalone: view.columns = […]; view.rows = […]. Inside <nk-database> the
  // database pushes the data. A card fires nk-select { row, id, value } like a
  // row of the table, Today and ‹ › nk-month { month }.
  // → <div class="nk-calendar-view weeks"><div class="cv-head"><div class="cv-title">May 2026</div><button class="cal-nav">Today</button>‹ ›</div>
  //     <div class="cv-grid"><div class="cv-wd">W</div>…<div class="cv-week">21</div><div class="cv-day today"><span class="cv-num">20</span><button class="cv-item">🗃️ …</button></div>…</div></div>
  class NkCalendarView extends NkElement {
    static get observedAttributes() {
      return ['name', 'label', 'badge', 'count', 'date-key', 'title-key', 'month', 'weeks', 'week-start', 'weekend', 'today', 'locale',
        'today-label', 'prev-label', 'next-label', 'week-label'];
    }

    render() {
      this._box = this.createElement('div', ['nk-calendar-view']);
      const head = this.createElement('div', ['cv-head']);
      this._title = this.createElement('div', ['cv-title']);
      this._todayBtn = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._prev = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._prev.innerHTML = CHEVRONS.prev;
      this._next = this.createElement('button', ['cal-nav'], { type: 'button' });
      this._next.innerHTML = CHEVRONS.next;
      head.append(this._title, this._todayBtn, this._prev, this._next);
      this._grid = this.createElement('div', ['cv-grid']);
      this._box.append(head, this._grid);
      this._wrapper.appendChild(this._box);
      this._columns ??= []; this._rows ??= [];
      this._month ??= this._attrMonth() ?? this._todayIso().slice(0, 7);
      this._render();
      this.closest('nk-database')?.requestSync?.();
    }

    _attrMonth() { const m = this.getAttribute('month'); return /^\d{4}-\d{2}$/.test(m || '') ? m : null; }
    _todayIso() { return this.getAttribute('today') || isoOf(new Date()); }
    _dateColumn() {
      const key = this.getAttribute('date-key');
      return this._columns.find(c => c.key === key) || this._columns.find(c => c.type === 'date') || null;
    }
    _titleColumn() {
      const key = this.getAttribute('title-key');
      return this._columns.find(c => c.key === key) || this._columns.find(c => c.title) || this._columns[0] || null;
    }

    _render() {
      if (!this._grid) return;
      const locale = resolveLocale(this.getAttribute('locale')), weeks = this.getBoolAttr('weeks');
      const n = Number(this.getAttribute('week-start'));
      const ws = this.hasAttribute('week-start') && Number.isInteger(n) && n >= 0 && n <= 6 ? n : firstWeekday(locale);
      const g = monthGrid(this._month, ws), today = this._todayIso();
      const weekend = new Set((this.getAttribute('weekend') || '').split(',').map(s => s.trim()).filter(Boolean).map(Number));
      const dateCol = this._dateColumn(), titleCol = this._titleColumn(), byDay = {};
      if (dateCol) for (const row of this._rows) {
        const d = parseDay(row[dateCol.key]);
        if (d) (byDay[isoOf(d)] ||= []).push(row);
      }
      this._box.classList.toggle('weeks', weeks);
      this._title.textContent = new Intl.DateTimeFormat(locale, { month: 'long', year: 'numeric' }).format(g.first);
      this._todayBtn.textContent = this.getAttribute('today-label') || 'Today';
      this._prev.setAttribute('aria-label', this.getAttribute('prev-label') || 'Previous month');
      this._next.setAttribute('aria-label', this.getAttribute('next-label') || 'Next month');

      const cell = (cls, text) => { const el = this.createElement('div', [cls]); el.textContent = text; return el; };
      const nodes = [];
      if (weeks) nodes.push(cell('cv-wd', this.getAttribute('week-label') || weekLabel(locale)));
      for (const w of weekdayNames(locale, ws)) nodes.push(cell('cv-wd', w));
      for (let i = 0; i < g.rows * 7; i++) {
        const d = g.day(i), iso = isoOf(d);
        if (weeks && i % 7 === 0) nodes.push(cell('cv-week', isoWeek(d)));
        const day = this.createElement('div', ['cv-day', d.getMonth() !== g.m - 1 && 'out', weekend.has(d.getDay()) && 'off', iso === today && 'today'].filter(Boolean));
        const num = this.createElement('span', ['cv-num']);
        num.textContent = d.getDate();
        day.appendChild(num);
        for (const row of byDay[iso] || []) {
          const item = this.createElement('button', ['cv-item'], { type: 'button', 'data-id': row.id ?? '' });
          const title = titleCol ? String(row[titleCol.key] ?? '') : '';
          item.textContent = row.icon ? `${row.icon} ${title}` : title;
          day.appendChild(item);
        }
        nodes.push(day);
      }
      this._grid.replaceChildren(...nodes);
    }

    _goMonth(month) {
      if (month === this._month) return;
      this._month = month;
      this._render();
      this.emit('nk-month', { month });
    }

    setupEvents() {
      this._onClick = (e) => {
        if (e.target.closest('.cal-nav') === this._todayBtn) { this._goMonth(this._todayIso().slice(0, 7)); return; }
        if (e.target.closest('.cal-nav') === this._prev) { this._goMonth(stepMonth(this._month, -1)); return; }
        if (e.target.closest('.cal-nav') === this._next) { this._goMonth(stepMonth(this._month, 1)); return; }
        const item = e.target.closest('.cv-item');
        if (item) { const row = this._rowById(item.dataset.id); this.emit('nk-select', { row, id: row?.id, value: row?.id }); }
      };
      this._box.addEventListener('click', this._onClick);
    }

    teardownEvents() { this._box?.removeEventListener('click', this._onClick); }

    _rowById(id) { return this._rows.find(r => String(r.id) === String(id)); }

    onAttributeChanged(name) {
      if (name === 'month') this._month = this._attrMonth() ?? this._month;
      this._render();
      if (name === 'label' || name === 'badge' || name === 'count') this.closest('nk-database')?.requestSync?.();
    }

    get name() { return this.getAttribute('name') || 'calendar'; }
    /** The month shown, YYYY-MM. */
    get month() { return this._month; }
    set month(v) { if (!/^\d{4}-\d{2}$/.test(v)) return; if (this._grid) this._goMonth(v); else this._month = v; }
    get columns() { return this._columns; }
    set columns(v) { this._columns = Array.isArray(v) ? v : []; this._render(); }
    get rows() { return this._rows; }
    set rows(v) { this._rows = Array.isArray(v) ? v : []; this._render(); }
    set data({ columns, rows }) { this.setData(columns, rows); }
    setData(columns, rows) { this._columns = columns || []; this._rows = rows || []; this._render(); }
    refresh() { this._render(); }
  }

  customElements.define('nk-calendar-view', NkCalendarView);

  // <nk-filter-bar add no-filter no-sort></nk-filter-bar>
  // bar.filters = [{ key: 'status', value: 'done', op: 'is-not', label: 'Status: Open' }];
  // The filters in effect as NotionKit's filter pills (1.7.0), no inline style:
  // → <div class="nk-filter-row">[Filter][Sort]
  //     <span class="nk-filter-pill active"><button>Status: Open</button><button class="fp-remove">×</button></span>…
  //     <button class="nk-filter-pill add">＋ Filter</button> …slot… [search]</div>
  // Filter and Sort are .nk-db-tool buttons (hide them with no-filter / no-sort
  // when the database toolbar carries its own); they fire nk-action. A pill's
  // label fires nk-action { action: 'edit', index, filter }, the add pill
  // nk-action { action: 'add' } – each with the clicked button as `anchor`, to
  // open a menu under it. × removes the filter; every change to filters or
  // search fires nk-change { filters, search }. In slot="filters" of
  // <nk-database> the row sits right under the view tabs. The texts are
  // attributes, for other languages: filter-label, sort-label, add-label,
  // remove-label (the × reads "<remove-label>: <pill>") and placeholder.
  class NkFilterBar extends NkElement {
    static get observedAttributes() { return ['search', 'placeholder', 'no-filter', 'no-sort', 'value', 'add', 'add-label', 'filter-label', 'sort-label', 'remove-label']; }

    render() {
      this._row = this.createElement('div', ['nk-filter-row']);
      this._filterBtn = this.createElement('button', ['nk-db-tool'], { type: 'button', 'data-action': 'filter' });
      this._sortBtn = this.createElement('button', ['nk-db-tool'], { type: 'button', 'data-action': 'sort' });
      this._pillStart = document.createComment('pills');
      this._addBtn = this.createElement('button', ['nk-filter-pill', 'add'], { type: 'button', 'data-action': 'add' });
      this._input = this.createElement('input', ['nk-input'], { type: 'search', autocomplete: 'off' });
      this._row.append(this._filterBtn, this._sortBtn, this._pillStart, this._addBtn, document.createElement('slot'), this._input);
      this._wrapper.appendChild(this._row);
      this._filters = [];
      this._sync();
    }

    _sync() {
      this._filterBtn.textContent = this.getAttribute('filter-label') || 'Filter';
      this._filterBtn.hidden = this.getBoolAttr('no-filter');
      this._filterBtn.classList.toggle('active', this._filters.length > 0);
      this._sortBtn.textContent = this.getAttribute('sort-label') || 'Sort';
      this._sortBtn.hidden = this.getBoolAttr('no-sort');
      this._addBtn.textContent = this.getAttribute('add-label') || '＋ Filter';
      this._addBtn.hidden = !this.getBoolAttr('add');
      this._input.hidden = !this.getBoolAttr('search');
      this._input.placeholder = this.getAttribute('placeholder') || 'Search …';
      if (this.hasAttribute('value') && this._input.value !== this.getAttribute('value')) this._input.value = this.getAttribute('value');
      for (const old of this._row.querySelectorAll('[data-pill]')) old.remove();
      const removeLabel = this.getAttribute('remove-label') || 'Remove filter';
      this._pillStart.after(...this._filters.map((f, i) => {
        const pill = this.createElement('span', ['nk-filter-pill', 'active'], { 'data-pill': i });
        const label = this.createElement('button', [], { type: 'button', 'data-edit': i });
        label.textContent = f.label ?? `${f.key}: ${f.value}`;
        const remove = this.createElement('button', ['fp-remove'], { type: 'button', 'data-remove': i, 'aria-label': `${removeLabel}: ${label.textContent}` });
        remove.textContent = '×';
        pill.append(label, remove);
        return pill;
      }));
    }

    setupEvents() {
      this._onClick = (e) => {
        const remove = e.target.closest('[data-remove]');
        if (remove) { this._filters.splice(Number(remove.dataset.remove), 1); this._sync(); this._emitChange(); return; }
        const edit = e.target.closest('[data-edit]');
        if (edit) { const index = Number(edit.dataset.edit); this.emit('nk-action', { action: 'edit', index, filter: this._filters[index], anchor: edit }); return; }
        const btn = e.target.closest('[data-action]');
        if (btn) this.emit('nk-action', { action: btn.dataset.action, anchor: btn });
      };
      this._onInput = () => { clearTimeout(this._debounce); this._debounce = setTimeout(() => this._emitChange(), 150); };
      this._row.addEventListener('click', this._onClick);
      this._input.addEventListener('input', this._onInput);
    }

    teardownEvents() {
      this._row?.removeEventListener('click', this._onClick);
      this._input?.removeEventListener('input', this._onInput);
      clearTimeout(this._debounce);
    }

    _emitChange() { this.emit('nk-change', { filters: [...this._filters], search: this._input.value }); }
    onAttributeChanged() { this._sync(); }

    get filters() { return this._filters; }
    set filters(v) { this._filters = Array.isArray(v) ? v : []; if (this._row) this._sync(); }
    get value() { return this._input?.value ?? ''; }
    set value(v) { if (this._input) this._input.value = v ?? ''; }
    /**
     * Applies filters and search to rows: a filter keeps rows where row[key]
     * equals value – or differs from it with op: 'is-not'; the search looks
     * for the text in every string field (a person's name).
     */
    apply(rows) {
      const q = this.value.trim().toLowerCase();
      return rows.filter(r => this._filters.every(f => (f.op === 'is-not' ? r[f.key] !== f.value : r[f.key] === f.value))
        && (!q || Object.values(r).some(v => String(typeof v === 'object' && v ? v.name ?? '' : v ?? '').toLowerCase().includes(q))));
    }
  }

  customElements.define('nk-filter-bar', NkFilterBar);

  // <nk-comments placeholder="Comment …" send-label="Send">
  //   <nk-comment …>…</nk-comment>
  // </nk-comments>
  // → <div class="nk-comments">…<div class="nk-comment-input"><input class="nk-input"><button class="nk-btn primary small">Send</button></div></div>
  // Enter or the button fires nk-submit { text } and clears the field.
  class NkComments extends NkElement {
    static get observedAttributes() { return ['placeholder', 'send-label', 'no-input', 'disabled']; }

    render() {
      const box = this.createElement('div', ['nk-comments']);
      box.appendChild(document.createElement('slot'));
      this._row = this.createElement('div', ['nk-comment-input']);
      this._input = this.createElement('input', ['nk-input'], { type: 'text', 'aria-label': 'Comment' });
      this._btn = this.createElement('button', ['nk-btn', 'primary', 'small'], { type: 'button' });
      this._row.append(this._input, this._btn);
      box.appendChild(this._row);
      this._wrapper.appendChild(box);
      this._sync();
    }

    _sync() {
      this._input.placeholder = this.getAttribute('placeholder') || 'Comment …';
      this._btn.textContent = this.getAttribute('send-label') || 'Send';
      this._row.style.display = this.getBoolAttr('no-input') ? 'none' : '';
      this._input.disabled = this._btn.disabled = this.getBoolAttr('disabled');
    }

    setupEvents() {
      this._onSend = () => this.submit();
      this._onKey = (e) => { if (e.key === 'Enter') { e.preventDefault(); this.submit(); } };
      this._btn.addEventListener('click', this._onSend);
      this._input.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._btn?.removeEventListener('click', this._onSend);
      this._input?.removeEventListener('keydown', this._onKey);
    }

    submit() {
      const text = this._input.value.trim();
      if (!text) return;
      const ok = this.emit('nk-submit', { text });
      if (ok) this._input.value = '';
    }

    onAttributeChanged() { this._sync(); }
    focus(o) { this._input?.focus(o); }
    get value() { return this._input?.value ?? ''; }
    set value(v) { if (this._input) this._input.value = v ?? ''; }
  }

  customElements.define('nk-comments', NkComments);

  // <nk-comment author="Sara Lindt" time="1 hr ago" avatar="SL" color="green">The board view feels close.</nk-comment>
  // → <div class="nk-comment"><span class="nk-avatar green">SL</span><div><div class="c-head"><b>Sara Lindt</b> · 1 hr ago</div><div class="c-body">…</div></div></div>
  // color: one of Notion's nine names, or any CSS background; without it the avatar gradient.
  // slot="head" adds content after the name (e.g. an <nk-tag>).
  class NkComment extends NkElement {
    static get observedAttributes() { return ['author', 'time', 'avatar', 'color']; }

    render() {
      const box = this.createElement('div', ['nk-comment']);
      this._avatar = this.createElement('span', ['nk-avatar']);
      const avatarSlot = this.createElement('slot', [], { name: 'avatar' });
      avatarSlot.appendChild(this._avatar);
      const text = document.createElement('div');
      this._head = this.createElement('div', ['c-head']);
      this._author = document.createElement('b');
      this._time = document.createTextNode('');
      this._head.append(this._author, this.createElement('slot', [], { name: 'head' }), this._time);
      const body = this.createElement('div', ['c-body']);
      body.appendChild(document.createElement('slot'));
      text.append(this._head, body);
      box.append(avatarSlot, text);
      this._wrapper.appendChild(box);
      this._sync();
    }

    _sync() {
      const author = this.getAttribute('author') || '';
      this._author.textContent = author;
      const time = this.getAttribute('time');
      this._time.data = time ? ` · ${time}` : '';
      this._avatar.textContent = this.getAttribute('avatar') || initialsOf(author);
      paintAvatar(this._avatar, this.getAttribute('color'));
    }

    onAttributeChanged() { this._sync(); }
  }

  customElements.define('nk-comment', NkComment);

  // <nk-ai-thread><nk-ai-msg …>…</nk-ai-msg>…</nk-ai-thread>  →  <div class="nk-ai-thread">
  class NkAiThread extends NkElement {
    render() {
      const box = this.createElement('div', ['nk-ai-thread'], { role: 'log', 'aria-live': 'polite' });
      box.appendChild(document.createElement('slot'));
      this._wrapper.appendChild(box);
    }
  }

  customElements.define('nk-ai-thread', NkAiThread);

  // <nk-ai-msg role="user" name="You" avatar="MK">Summarise the open tasks.</nk-ai-msg>
  // <nk-ai-msg role="assistant" name="Mona" badge="· AI" avatar="✨">
  //   Two tasks are open …
  //   <button slot="actions" value="copy">📋 Copy</button><button slot="actions" value="retry">↻ Rephrase</button>
  // </nk-ai-msg>
  // <nk-ai-msg role="user" bubble>What is still open?</nk-ai-msg>   → a grey bubble on the right, as in Notion's AI chat
  // → <div class="nk-ai-msg user"><span class="mini-avatar">MK</span><div class="a-body"><div class="a-name">You</div>…<div class="nk-ai-actions">…</div></div></div>
  // Action buttons stay in the light DOM (slotted twins) and fire nk-action { action: value }.
  class NkAiMsg extends NkElement {
    static get observedAttributes() { return ['role', 'name', 'badge', 'avatar', 'color', 'bubble']; }

    render() {
      this._box = this.createElement('div', ['nk-ai-msg']);
      this._avatar = this.createElement('span', ['mini-avatar']);
      const avatarSlot = this.createElement('slot', [], { name: 'avatar' });
      avatarSlot.appendChild(this._avatar);
      this._avatarSlot = avatarSlot;
      const body = this.createElement('div', ['a-body']);
      this._name = this.createElement('div', ['a-name']);
      this._nameText = document.createTextNode('');
      this._badge = document.createElement('span');
      this._name.append(this._nameText, this._badge);
      this._actions = this.createElement('div', ['nk-ai-actions']);
      this._actionSlot = this.createElement('slot', [], { name: 'actions' });
      this._actions.appendChild(this._actionSlot);
      body.append(this._name, document.createElement('slot'), this._actions);
      this._box.append(avatarSlot, body);
      this._wrapper.appendChild(this._box);
      this._sync();
    }

    _sync() {
      const user = this.getAttribute('role') === 'user';
      const bubble = this.getBoolAttr('bubble');
      this._box.classList.toggle('user', user);
      this._box.classList.toggle('bubble', bubble);
      const name = this.getAttribute('name');
      this._nameText.data = name || '';
      const badge = this.getAttribute('badge');
      this._badge.textContent = badge ? ` ${badge}` : '';
      // A bubble stands without avatar and name; the text says who asked.
      this._name.style.display = name && !bubble ? '' : 'none';
      this._avatarSlot.style.display = bubble ? 'none' : '';
      this._avatar.textContent = this.getAttribute('avatar') || (user ? (name || 'U').slice(0, 2).toUpperCase() : '✨');
      const color = this.getAttribute('color');
      this._avatar.style.background = color || '';
      this._actions.style.display = this._actionSlot.assignedElements().length ? '' : 'none';
    }

    setupEvents() {
      this._onSlot = () => this._sync();
      this._onClick = (e) => {
        const btn = e.target.closest?.('button');
        if (btn && this._actionSlot.assignedElements().includes(btn)) this.emit('nk-action', { action: btn.value || btn.textContent.trim(), value: btn.value || btn.textContent.trim() });
      };
      this._actionSlot.addEventListener('slotchange', this._onSlot);
      this.addEventListener('click', this._onClick);
      this._sync();
    }

    teardownEvents() {
      this._actionSlot?.removeEventListener('slotchange', this._onSlot);
      this.removeEventListener('click', this._onClick);
    }

    onAttributeChanged() { this._sync(); }
  }

  customElements.define('nk-ai-msg', NkAiMsg);

  // <nk-ai-input-row placeholder="Ask Mona something …"></nk-ai-input-row>
  // → <div class="nk-ai-input-row"><span>✨</span><input><button class="nk-ai-send">↑</button></div>
  // Enter or the send button fires nk-submit { text } and clears the field.
  class NkAiInputRow extends NkElement {
    static get observedAttributes() { return ['placeholder', 'value', 'disabled', 'icon']; }

    render() {
      this._row = this.createElement('div', ['nk-ai-input-row']);
      this._icon = document.createElement('span');
      this._icon.style.fontSize = '14px';
      this._input = this.createElement('input', [], { type: 'text', 'aria-label': 'Message' });
      this._btn = this.createElement('button', ['nk-ai-send'], { type: 'button', 'aria-label': 'Send' });
      this._btn.textContent = '↑';
      this._row.append(this._icon, this._input, this._btn);
      this._wrapper.appendChild(this._row);
      this._sync();
    }

    _sync() {
      this._input.placeholder = this.getAttribute('placeholder') || 'Ask something …';
      if (this.hasAttribute('value') && this._input.value !== this.getAttribute('value')) this._input.value = this.getAttribute('value');
      this._input.disabled = this._btn.disabled = this.getBoolAttr('disabled');
      const icon = this.getAttribute('icon') ?? '✨';
      this._icon.textContent = icon;
      this._icon.style.display = icon ? '' : 'none';
    }

    setupEvents() {
      this._onSend = () => this.submit();
      this._onKey = (e) => { if (e.key === 'Enter') { e.preventDefault(); this.submit(); } };
      this._btn.addEventListener('click', this._onSend);
      this._input.addEventListener('keydown', this._onKey);
    }

    teardownEvents() {
      this._btn?.removeEventListener('click', this._onSend);
      this._input?.removeEventListener('keydown', this._onKey);
    }

    submit() {
      const text = this._input.value.trim();
      if (!text || this.getBoolAttr('disabled')) return;
      const ok = this.emit('nk-submit', { text });
      if (ok) this._input.value = '';
    }

    onAttributeChanged() { this._sync(); }
    focus(o) { this._input?.focus(o); }
    get value() { return this._input?.value ?? ''; }
    set value(v) { if (this._input) this._input.value = v ?? ''; }
  }

  customElements.define('nk-ai-input-row', NkAiInputRow);

  exports.NkAiInputRow = NkAiInputRow;
  exports.NkAiMsg = NkAiMsg;
  exports.NkAiThread = NkAiThread;
  exports.NkApp = NkApp;
  exports.NkAvatar = NkAvatar;
  exports.NkAvatarGroup = NkAvatarGroup;
  exports.NkBanner = NkBanner;
  exports.NkBlockHost = NkBlockHost;
  exports.NkBoardView = NkBoardView;
  exports.NkBookmark = NkBookmark;
  exports.NkBreadcrumb = NkBreadcrumb;
  exports.NkBtn = NkBtn;
  exports.NkCalendar = NkCalendar;
  exports.NkCalendarView = NkCalendarView;
  exports.NkCallout = NkCallout;
  exports.NkCheck = NkCheck;
  exports.NkCmdk = NkCmdk;
  exports.NkCode = NkCode;
  exports.NkComment = NkComment;
  exports.NkComments = NkComments;
  exports.NkCopyField = NkCopyField;
  exports.NkDangerZone = NkDangerZone;
  exports.NkDatabase = NkDatabase;
  exports.NkDialog = NkDialog;
  exports.NkDivider = NkDivider;
  exports.NkElement = NkElement;
  exports.NkEmojiPicker = NkEmojiPicker;
  exports.NkEmpty = NkEmpty;
  exports.NkField = NkField;
  exports.NkFields = NkFields;
  exports.NkFilterBar = NkFilterBar;
  exports.NkFormElement = NkFormElement;
  exports.NkHeading = NkHeading;
  exports.NkImagePicker = NkImagePicker;
  exports.NkInput = NkInput;
  exports.NkKbd = NkKbd;
  exports.NkListView = NkListView;
  exports.NkMemberList = NkMemberList;
  exports.NkMemberRow = NkMemberRow;
  exports.NkMention = NkMention;
  exports.NkMenu = NkMenu;
  exports.NkMenuItem = NkMenuItem;
  exports.NkModal = NkModal;
  exports.NkModelCard = NkModelCard;
  exports.NkPage = NkPage;
  exports.NkPageActions = NkPageActions;
  exports.NkPageCover = NkPageCover;
  exports.NkPageTitle = NkPageTitle;
  exports.NkPanel = NkPanel;
  exports.NkPanels = NkPanels;
  exports.NkPeek = NkPeek;
  exports.NkPop = NkPop;
  exports.NkProfileRow = NkProfileRow;
  exports.NkProgress = NkProgress;
  exports.NkProp = NkProp;
  exports.NkProps = NkProps;
  exports.NkQuote = NkQuote;
  exports.NkRadio = NkRadio;
  exports.NkSectionLabel = NkSectionLabel;
  exports.NkSegmented = NkSegmented;
  exports.NkSelect = NkSelect;
  exports.NkSettingsPane = NkSettingsPane;
  exports.NkSettingsUser = NkSettingsUser;
  exports.NkSheet = NkSheet;
  exports.NkSidebar = NkSidebar;
  exports.NkSkeleton = NkSkeleton;
  exports.NkSlider = NkSlider;
  exports.NkStat = NkStat;
  exports.NkStats = NkStats;
  exports.NkSteps = NkSteps;
  exports.NkSwitch = NkSwitch;
  exports.NkSynced = NkSynced;
  exports.NkTab = NkTab;
  exports.NkTabBar = NkTabBar;
  exports.NkTabBarItem = NkTabBarItem;
  exports.NkTableView = NkTableView;
  exports.NkTabs = NkTabs;
  exports.NkTag = NkTag;
  exports.NkTemplateBtn = NkTemplateBtn;
  exports.NkTextarea = NkTextarea;
  exports.NkThemeToggle = NkThemeToggle;
  exports.NkToast = NkToast;
  exports.NkTodo = NkTodo;
  exports.NkToggle = NkToggle;
  exports.NkTooltip = NkTooltip;
  exports.NkTopbar = NkTopbar;
  exports.NkTree = NkTree;
  exports.NkTreeItem = NkTreeItem;
  exports.NkWorkspaceSwitcher = NkWorkspaceSwitcher;
  exports.compareBy = compareBy;
  exports.componentsSheet = componentsSheet;
  exports.renderPropertyCell = renderPropertyCell;
  exports.tagFor = tagFor;

  return exports;

})({});
